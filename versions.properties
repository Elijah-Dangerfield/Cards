applicationId=com.dangerfield.cards
# The markers below are load-bearing. release-please updates arbitrary files
# only where it finds them, and it fails silently when they're missing: the
# v0.2.0 release cut a tag and a changelog while this file stayed at 0.1.0, so
# both stores shipped 0.2.0's code labelled 0.1.0. Keep them wrapped around the
# version, on their own lines — a trailing `# x-release-please-version` comment
# would become part of the value, since `#` mid-line is not a comment here.
# x-release-please-start-version
versionName=0.2.0
# x-release-please-end
versionCode=1
releaseChannel=local
buildNumber=1

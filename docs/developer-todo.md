# Developer TODO

Anything only the human (Elijah) can do — credentials, GitHub settings, dashboard / external-service config, device QA, content writing, deferred product decisions. Not part of the engineering punch list ([todo.md](./todo.md) is for that). Automated **workers** must never touch this file. The nightly **reviewer** may append a one-line entry when a PR creates a new human-only follow-up, but may not edit or delete existing entries.

For per-cycle items tied to a specific PR (visual deltas to eyeball, fixes that need device verification *this* cycle), see the PR's "Heads up" section instead — those don't belong here.

Check items off as you do them; delete when the whole section is empty.

---
## Launch readiness — legal & business

Non-engineering gates that block a public launch. None are worker-pickable.

- [x] **Set up the LLC before you take real money.** Done 2026-07-24 — LLC + business banking in place; App Store / Play payouts point to the business account.
- [ ] **Launch day: switch prod Apple purchases to Production.** When the app goes live on the App Store, run:
  `fly secrets set APPLE_STORE_ENVIRONMENT=Production -a cards-server-prod`
  After this, TestFlight purchases stop working (that's expected) — test future chip packs on a debug build against dev. Background: [decisions.md](./decisions.md) 2026-07-07.
- [ ] **iOS sells nothing right now — fix the chip packs in App Store Connect.** On the live App Store build (`cards@0.1.0+3`), StoreKit returns none of `com.cards.iap.chips.small` / `.medium` / `.large`, so the shop hides all three and iOS revenue is zero. Real users have hit it since 2026-07-23. Check, in order: the three IAPs exist under exactly those product ids, they're Approved and attached to the live version, the Paid Applications agreement + tax/banking are active, and no region is blocked (one affected user is `en_SG`). Nothing in the repo can fix this. Case: [`agent/feedback-cases/CARDS-8V.md`](agent/feedback-cases/CARDS-8V.md); the engineering half (alerting + honest empty-shop state) is ENG-43 in [todo.md](./todo.md). *(Appended 2026-08-18 by observability-triage — append-only, no existing entries touched.)*
- [ ] **Read the app's numeric App Store id off App Store Connect and paste it here.** The iOS "Leave a review" button currently opens `apps.apple.com/app/id0000000000`, which goes nowhere for real users. Steps: (1) open the app in App Store Connect, (2) copy the 10-digit id from the App Information page or the listing URL, (3) drop it in this entry or straight into `APP_STORE_ID_PLACEHOLDER` in `features/home/impl/.../StoreReviewLink.kt`. No agent can get this — the iTunes lookup API returns nothing for our bundle id, checked across six storefronts. Once it lands the code fix is one constant: [backlog.md](./backlog.md) "iOS 'Leave a store review' dead-ends on a placeholder App Store id". *(Appended 2026-08-20 by nightly reviewer — append-only, no existing entries touched.)*
- [ ] **Register the Play app + signing key for Android developer verification before Sep 30, 2026.** Google's "[Final reminder]" (2026-08-31, `googleplay-noreply@google.com`): any Play app/signing-key pair not registered by that date is removed from Google Play globally, and from Sep 30 on, unregistered apps stop installing on participating stores/certified devices in select regions even if already installed. Google says >99% of apps were auto-registered, but that still needs confirming for this app — check Play Console → Android developer verification page (`play.google.com/console/android-developer-verification`) for this package's registration status; only unregistered apps need action. No agent can do this — it's a Play Console account action. *(Appended 2026-09-06 by observability-triage — append-only, no existing entries touched.)*
- [ ] **Finish the App Store Connect "developer information" / legal-entity update.** Apple's "Complete the process of updating your developer information" mail (2026-08-26, `no_reply@email.apple.com`) says a request to update the developer info was received, and there are open tasks for the legal entity in Business (`appstoreconnect.apple.com` → Contracts) still to complete before the agreement is processed. No date is stated and this reads as self-initiated (likely part of the LLC move logged in `decisions.md` 2026-07-24), but it's an open item only a human can close in App Store Connect. No agent can do this. *(Appended 2026-09-07 by observability-triage — append-only, no existing entries touched.)*
- [ ] **Reconnect the Sentry MCP connector.** It shows `needs_reconnect` (not `connected`) in this account's connector list, and there's no `SENTRY_AUTH_TOKEN` in the environment either, so unattended triage runs in this sandbox cannot see Sentry at all — confirmed the direct REST API is also blocked by the outbound proxy policy (`us.sentry.io:443` → 403). This is the second night running (2026-09-06, 2026-09-07) observability-triage has had zero Sentry/Grafana visibility; only the store-inbox channel has been getting swept. Reconnect via claude.ai Settings → Connectors, or set `SENTRY_AUTH_TOKEN` for this environment. No agent can do this. *(Appended 2026-09-07 by observability-triage — append-only, no existing entries touched.)* **[Update 2026-09-14: Sentry/Grafana/Gmail MCPs all reachable this run and every run since — connector appears reconnected. Worth confirming and striking this line.]**
- [ ] **Downcard's iOS resubmission has been "Waiting for Review" since 2026-09-11T02:26 with no further status mail as of 2026-09-14 (~3.3 days).** Not a confirmed block — no new rejection, and Apple review can run long over a weekend — but this is longer than the app's typical 24-48h turnaround, and the 09-10 rejection's mail body carried no itemized reason (Apple puts specifics only in App Store Connect). Worth a glance at App Store Connect to confirm it's still progressing normally, not silently stuck. No agent can check App Store Connect status directly. *(Appended 2026-09-14 by observability-triage — append-only, no existing entries touched.)* **[Update 2026-09-15: still "Waiting for Review", still no further status mail — now ~4.4 days. Trending past the typical turnaround further; no new rejection either, so still not a confirmed block, but worth a closer look if it clears another day with no update.]** **[Update 2026-09-16: still "Waiting for Review", still zero status mail since 09-11T02:26 — now ~5.4 days, more than double the app's typical 24-48h turnaround. Still no rejection or suspension notice, so still short of a confirmed block, but the silence itself is now the notable part — worth checking App Store Connect directly rather than waiting for another mail that may not come.]** **[Update 2026-09-17: still "Waiting for Review", still zero status mail since 09-11T02:26 — now ~6.2 days, the longest wait yet and nearly triple the app's typical 24-48h turnaround. Still no rejection or suspension notice, so still short of a confirmed shipping block (not emailed per the skill's bar), but this has now gone long enough that it's worth checking App Store Connect directly rather than waiting on another night's mail sweep.]**
- [ ] **Reconnect the Sentry MCP connector.** It shows `needs_reconnect` (not `connected`) in this account's connector list, and there's no `SENTRY_AUTH_TOKEN` in the environment either, so unattended triage runs in this sandbox cannot see Sentry at all — confirmed the direct REST API is also blocked by the outbound proxy policy (`us.sentry.io:443` → 403). This is the second night running (2026-09-06, 2026-09-07) observability-triage has had zero Sentry/Grafana visibility; only the store-inbox channel has been getting swept. Reconnect via claude.ai Settings → Connectors, or set `SENTRY_AUTH_TOKEN` for this environment. No agent can do this. *(Appended 2026-09-07 by observability-triage — append-only, no existing entries touched.)*

- [x] **Approve the two stuck prod server deploys, or decide the gate should go.** *(Done 2026-09-21: both stale runs were fully superseded by current `main`, so they were cancelled rather than approved — approving them would have deployed Sep-4 and Sep-5 code. A fresh dispatch from `main` (`d55a65d8`, run 35641008256) was approved instead. The gate itself was kept.)* `server-deploy-prod` runs `33922369090` (PR #152, 2026-09-04) and `33975509833` (PR #155, 2026-09-05) are sitting on the `production` environment approval gate, `waiting` and `pending`. Prod has therefore not deployed since 2026-09-02 and is running code that predates the OTel trace-root fix — poisoned `trace_id=57f45c70...` is still in prod logs as of 2026-09-21. Approve at https://github.com/Elijah-Dangerfield/Cards/actions, or remove the gate if it is not actually wanted. No agent can approve a GitHub environment. *(Appended 2026-09-21 by observability-triage.)*

- [x] **Cut an app release — 72 commits on `main` have reached no user.** *(Done 2026-09-21: v0.3.0 / build 1209 shipped Android-only to the Play production track at a 10% staged rollout. iOS deliberately skipped so the pending App Store submission was not disturbed. ENG-49's verification clock starts now.)* The last release ran 2026-09-03, before PR #152 merged. Real Play users are still on `cards@0.1.0+1135`, which predates the ENG-49 RenderThread fix, and CARDS-C9 (2026-09-20) is that ANR still hitting them. Unshipped alongside it: R8/minification, baseline profiles, the Sentry mapping upload, and the play-screen crash fix. ENG-49's four-week verification clock cannot start until this happens. *(Appended 2026-09-21 by observability-triage.)*

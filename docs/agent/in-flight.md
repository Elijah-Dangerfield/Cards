# In-flight (2026-06-23 cycle)

## fix(server): hard-delete users on account deletion

**Problem:** `DELETE /v1/me` called Supabase admin delete-user with no body, so GoTrue soft-deleted by default — the `auth.users` row stayed, the credential kept authenticating, and the design's `ON DELETE CASCADE` never fired (a tester re-logged-in after "deleting"). CARDS-1T.
**Approach:** `HttpSupabaseAdminClient.deleteUser` now sends `{"should_soft_delete": false}` as a JSON body on the DELETE, forcing GoTrue to hard-delete the row + cascade. Added a body assertion to the success test (the existing tests only checked URL/headers).
**Reviewer notes:** None — body shape matches the GoTrue admin API. Can't end-to-end verify against a live Supabase from here; covered by the MockEngine body assertion.

## feat(rooms): gate private room join on wallet balance

**Problem:** `POST /v1/rooms/{code}/join` had no wallet-vs-buy-in check (unlike matchmaking's find path), so a tester joined a room whose buy-in exceeded their chips and was silently demoted to spectator by escrow. CARDS-1G.
**Approach:** Server gate added to the join route — fetch the room, and if the caller isn't already a member and `room.buyIn > balance`, reject `400 insufficient_balance` (mirrors `MatchmakingRoutes`; re-joins skip the check since they already sat down). Client maps the code to a new `JoinRoomOutcome.OverBalance(message)` → `LobbyError.JoinOverBalance`, surfacing the server's message inline like `CreateInvalidMaxSeats` does (no new string resource needed). Tests on both sides.
**Reviewer notes:** The over-balance message is rendered server-side and shown verbatim (server-supplied copy is the AGENTS.md exception to the resources rule, same as `CreateInvalidMaxSeats`). Unknown codes still fall through to join()'s 404 — the gate only fires when the room exists.

## fix(room): land on Home when leaving a private MP game

**Problem:** Leaving a private multiplayer game dropped the player back into the dead lobby instead of Home. CARDS-1Y.
**Approach:** A private game pushes `PlayMultiplayerRoute` on top of its `LobbyRoute`, so the old `goBack()` (pop one) + `switchTab(Home)` left the lobby underneath. The leave `onBack` now branches on `route.kind`: Private pops the whole chain with `popBackTo(LobbyRoute(), inclusive = true)` (lands on the Home tab root), Public keeps the existing `goBack + switchTab(Home)` since it has no lobby beneath it. Verified both Private entry paths (lobby start + Home active-room banner) route through a `LobbyRoute`, so the pop target always exists.
**Reviewer notes:** Entry-point routing lambda — no VM, so not unit-testable here (it's pure `Router` calls). The `OpponentsLeft` event still intentionally routes a private game to `LobbyRoute(prefilledCode)` (lone player can re-invite); only the explicit-leave path changed.

## chore: remove the redundant Neon table theme

**Problem:** The `table_neon` table-theme product is visually identical to a plain felt in V1 (both just recolor the felt); the owner asked to delete it (CARDS-18), mirroring the V64 Sunset table-theme removal.
**Approach:** Append-only `V70__remove_neon_table_theme.sql` deletes the product row (it was already unlock-only from V51, so no shop row to pull). Dropped the `table_neon` arm from `feltForProductId` and removed `EquippedFelt.Neon` entirely (no surviving felt mapped to it, unlike Sunset which keeps `felt_sunset_weekend`) — plus its `feltSurfaceColor` / `feltAccentSurface` arms and the screen preview. Tests that used `table_neon` as a generic felt/personal product id were repointed to surviving ids (`felt_charcoal`, or `table_classic` for the prefix-only cosmetic-category test); `EquippedFeltMappingsTest` now asserts both removed table themes fall back to Default.
**Reviewer notes:** Kept `CardBackStyle.Neon` / `cardback_neon` untouched — that's a card back, a different product from the table theme. `PostgresProductCatalogSourceTest` only needed a comment update (table.neon was already absent from the catalog as unlock-only); it's a DatabaseTest so it didn't run in the local pass.

## fix(profile): show play-more empty state instead of a fake play style

**Problem:** The profile stats banner fabricated a "Sharp & Steady" style label and a hardcoded "1,284 won" chips value before the user had enough hands to derive a real style, mis-selling fake data as theirs. CARDS-2A.
**Approach:** Lifted the Stats page's private `PlayStyleEmptyCard` into a shared `:libraries:ui` DS component (the todo asked for reuse) and pointed StatsScreen at it. The profile banner now drops the fabricated chips-won entirely and, below the `PlayStyleAxes.MIN_SAMPLE` gate, renders an honest "Play more hands to build your style" subtitle (the decorative radar mark already stood in for the shape); above the gate it shows the real derived style + real win rate. Removed the now-dead `profile_play_style_example` / `profile_stats_banner_subtitle` / `_no_games` strings, added `_style_win` + `_pending`.
**Reviewer notes:** Pure presentation — no VM logic, so covered by previews (added a `PlayStyleEmptyCardPreview`) rather than a unit test. Left the room player-card pending state (`room_player_profile_style_pending_body`) on its own `PublicHeroCard` shape — it's already honest and a different layout; not worth forcing onto the shared card this pass. Fixed the reused `stats_play_style_empty_blurb` string in passing — it shipped a literal `we\'ll` (renders the backslash in Compose-MP) plus an em dash, both AGENTS.md string violations; now plain `we will` + a hyphen.

## fix(matchmaking): slow + tighten the searching/bot-fallback copy

**Problem:** Two CARDS-20 copy directives on the public-search screen: the rotating reassurance lines flipped too fast to read and the "fake a crowd" line drew attention to the thing it denied; and the disclosed-bot fallback explainer was too long.
**Approach:** Bumped `ROTATE_INTERVAL_MS` 3,500 → 5,000 so a full line is readable through the crossfade. Reworked the weak rotate lines: dropped the "fake a crowd" framing for a plainer "every seat at your table is a real person", and turned the share nudge into "got friends who play?". Trimmed `public_searching_offer_body` from four sentences to two — the title already says we couldn't find anyone, so the body now leads straight with "play bots for real chips while you wait" and keeps the load-bearing honesty (keep your winnings up to a daily limit, bots step aside for real players). Ran the unslop-text scanner on the new copy (clean, 0 findings).
**Reviewer notes:** Copy-only, no layout change — the bot-fallback's `PublicHeroCard` + 3-button stack already read fine once the body shrank, so I left the UI structure (the item also mentioned "review the layout"; judged it didn't need restructuring). Both CARDS-20 sub-items (rotation/lines + explainer) landed in this one commit since they're the same screen + same feedback report.

## chore: delete the orphaned PublicLobby / PublicNextRound screens

**Problem:** Public matchmaking ships routing Searching straight to the live play screen, leaving `PublicLobbyScreen` / `PublicNextRoundScreen` (+ their routes, still registered in `RoomsFeatureEntryPoint`) dead — nothing in the live flow navigates to them.
**Approach:** Chose deletion over repurposing `PublicNextRound` as a mid-hand-join interstitial — the server already drives mid-hand joins via the scrubbed spectate path on the live table, so a separate screen would duplicate that. Removed both screen files, their `screen<>` registrations + imports in `RoomsFeatureEntryPoint`, the `PublicLobbyRoute` / `PublicNextRoundRoute` classes in `PublicRoutes.kt`, and the now-orphaned `public_lobby_*` / `public_next_*` / `public_leave_table` strings (grep-confirmed 0 kt references for each before deleting). Updated the entry-point + route-family KDocs that described the shells.
**Reviewer notes:** Confirmed the only `navigate`/`popBackTo` targets in the public flow are `PublicFindRoute`, `PublicSearchingRoute`, and `PlayMultiplayerRoute` — nothing reached the deleted routes. `ReaperGracePolicyTest.formingPublicLobby_getsTheShortGrace` is a server-side test about room grace policy (unrelated to the client screen) and was left untouched.

## fix(room): real-chip framing on subsidized bot tables

**Problem:** A public disclosed-bot table is a real-chip game (keep what you beat the bots for, up to a daily limit), but busting there showed the practice "chips against bots don't count for keeps" copy and the stack explainer said the same — the fake-vs-real framing was mis-keyed. CARDS-1C / CARDS-20.
**Approach:** Threaded the existing `TableUiState.Active.subsidizedBotTable` flag into `BustDialog` and `StackExplainer`, swapping the body/note onto real-chip copy when subsidized (two new strings: `room_bust_subsidized_body`, `room_stack_explainer_subsidized_note`). The auto-rebuy mechanic is unchanged — only the framing differs; on a subsidized bots-only table `isRealMultiplayer` is still false (it gates the terminal Leave/Buy MP dialog, which doesn't apply since the server auto-refills), so the `BustDialog` branch is correct and just needed the right words. The join-time framing (the auto-opened `PracticeTierExplainer`) already picked its subsidized variant, so the only gaps were the bust + stack copy.
**Reviewer notes:** Pure presentation — body selection covered by new `@Preview`s (`BustDialogPreview_Subsidized`, `StackExplainerPreview_Subsidized`) per the previews convention; `RemotePokerSessionFactoryTest` already asserts `subsidizedBotTable` is set for public lone-human-vs-bots tables, which is the upstream signal these dialogs now read. Removed the CARDS-20/1C todo bullet — both halves (join framing already handled, bust/stack now fixed) are addressed.

## feat(stats): show distinct-opponents-played count on the Stats page

**Problem:** The server slice for the distinct-opponents stat shipped (`GET /v1/me/stats` → `{distinctOpponentsPlayed}`) but nothing read it client-side — the count never reached the Stats page. CARDS-P.
**Approach:** Added a `LifetimeStatsRepository` interface in `:libraries:cards` with an `authedCall`-backed impl in `:libraries:cards:impl` (idempotent retry — read-only). Picked a dedicated interface over folding into `ProgressionRepository` because progression has a local write-through ledger this server-only stat has no counterpart for (the todo offered either). `StatsViewModel` fetches once on init (always-network — no local source to project from) and carries `distinctOpponentsPlayed: Long?`; a failure leaves it null and the tile self-hides. Rendered a full-width `StatTile` ("Players faced") at the bottom of `LifetimeStatsGrid` only when the count is present.
**Reviewer notes:** New string `stats_lifetime_opponents_label` is a resource per AGENTS.md even though the surrounding tiles use inline literals (pre-existing; not touched). VM fetch + failure paths are unit-tested; the tile render is covered by the `_Populated` preview (now seeded with `distinctOpponentsPlayed = 17`). Count is MP-only by the server's contract (bot hands don't populate the opponents table) — no client gating needed since the value just reflects what the server returns.

# In-flight

## feat(stats): recent-XP rows get source emoji, type-colored points, relative time

**Problem:** Recent-XP rows showed a plain label + uncolored `+N` and never said when the XP was earned.
**Approach:** Each row now leads with a per-`XpSource` emoji (🃏/🪙/👀/💪/🎯), colors the `+N` by source (content/teal/coral/info/success), and stacks an `earnedAgo(...).label()` relative timestamp under the points — reusing the existing day-granular `EarnedAgo` DS util the hint pointed at rather than building a parallel minutes/hours formatter. Source label strings were left as the pre-existing inline literals (out of scope; not new violations).
**Reviewer notes:** `EarnedAgo` buckets at day granularity, so several same-day events all read "today" — acceptable per the "earnedAgo-style" hint, but if the mock wants minute/hour precision that's a follow-up formatter. No new tests: the maps are exhaustive `when`s over `XpSource` (compiler-checked) and the relative-time factory is already covered by `FormatEarnedAgoTest`. Verified by module compile + `:features:progression:impl:testDebugUnitTest`.

## refactor(profile): converge shelf headers onto the DS SectionHeader

**Problem:** Home shelves used the DS `SectionHeader` (neutral secondary "See all", whole-row tap); Profile shelves used a private local `SectionHeader` with an accent-primary CTA action that only the action text was tappable — same pattern, divergent chrome.
**Approach:** Deleted Profile's local `SectionHeader` and routed both call sites (cosmetic shelves + the achievements grid) through the DS one. Chose to converge on Home's neutral treatment (one component, one visual: secondary trailing label, whole-row tap, `Heading.H800` title) rather than add an accent-CTA flag to the DS component — a flag would re-introduce the two-treatment divergence the item asks to remove. Trade-off: the "Shop ›" link loses its accent-primary color and becomes a neutral secondary label (still reads as a shop shortcut via the "›" + whole-row tap); the reviewer can course-correct if the spend-CTA was meant to pop. The achievements header's separate count + "See all" fold into one `"%1$d / %2$d · See all"` trailing label, matching Home's `"%d online · See all"` idiom.
**Reviewer notes:** Profile titles go `H700 → H800` as part of the unification (bigger, matches Home). Dead strings `profile_achievements_count` / `profile_achievements_see_all` removed (no other refs); added `profile_achievements_count_see_all`. Shop's own bespoke header (`ShopScreen.kt`) is a Column with NO see-all/CTA — a static section title, a different role — so it's intentionally left out of this convergence. No new tests (header rendering is UI-only; existing profile VM tests pass). Verified by `:features:profile:impl:compileDebugKotlinAndroid` + `testDebugUnitTest`.

## feat(stats): play-style quadrant blob + reusable DS component

**Problem:** Nothing visualised the user's poker style; the todo wanted a soft blob on a TIGHT↔LOOSE × PASSIVE↔AGGRESSIVE grid with a name pill + blurb, built as a reusable `:libraries:ui` primitive.
**Approach:** New `PlayStyleBlob` DS component (`:libraries:ui/components/PlayStyleBlob.kt`) — a Canvas quadrant (crosshair + soft radial-gradient blob + a solid core dot at the plotted point) with the four axis labels drawn in-canvas via `TextMeasurer` (matches `RadarChart`'s label approach so the block stays exactly `gridSize` square), stacked above a `StatusPill` name + a centered blurb. Chose a self-contained square grid with in-canvas labels over an external-label Box layout so long words ("Aggressive") can't collide with sibling content and the component drops cleanly into any slot. `tightness`/`aggression` are caller-supplied 0..1 (clamped); axis labels live in the component (fixed for this chart type) in `:libraries:resources`. Wired into Stats as its own "Play style" section.
**Reviewer notes:** Shipped against **placeholder** position + copy (`tightness=0.74, aggression=0.78`, "Tight-Aggressive") per the item's "ship against the example until a real metric exists" — every user sees the same blob today, exactly like Profile's existing "Sharp & Steady" / example-chips banner. When the real tightness/aggression metric lands (separate data item), feed it in and unify the two placeholder style names. **Visual not verified in Studio** (no render env): the blob radius (0.30·plotW), label inset (0.18), and `Caption.C200` axis labels are my best-guess proportions — worth an eyeball against `docs/todo-assets/stats-style.png` on a visual pass. No unit test: the component is pure Canvas drawing (position math is a trivial `(1-t)·w` lerp); two `@Preview`s cover the TAG and loose-passive corners.
**Deferred:** `PlayStyleBlob` could replace Profile's `StatsStyleBanner` `DecorativeBlob` and land in the seat-tap sheet (the item's "many places") — left for a follow-up; nothing filed yet, reviewer please triage.

## feat(profile): relabel Avatars shelf + avatar-pack edit pointer

**Problem:** The profile cosmetic shelf read "Avatars" and the avatar-pack detail sheet showed the bundled emojis but never told the user *how* to actually wear one.
**Approach:** Relabeled the shelf string to "Avatar packs" and added an edit-pointer note ("Edit your avatar from the pencil button on your profile.") under the pack-contents grid for `avatars_` items. Guarded the equip-button branch with `!isAvatarPack` so an avatar pack never shows a contradictory equip CTA alongside the edit pointer regardless of the catalog's `isEquippable` flag — avatar selection lives in Edit profile, not this sheet.
**Reviewer notes:** The pack-contents emoji grid was already rendering (`PackContents`), so this item was really just the label + the pointer note. No new tests — pure string + conditional Composable; `:features:profile:impl:compileDebugKotlinAndroid` is green.

## feat(home): reactive active-room banner via observeActiveRooms flow

**Problem:** Home's active-game banner read from a one-shot `getActiveRooms()` on init/refresh, so it went stale until something re-triggered the fetch — a forfeit or a join elsewhere didn't reflect live.
**Approach:** Added `RoomRepository.observeActiveRooms(): Flow<List<Room>>`, backed by an in-memory `MutableStateFlow` in `RoomRepositoryImpl` that's seeded by `getActiveRooms()` and kept current by the local create/join/leave mutations (join upserts, leave/not-found/not-in-room removes). `HomeViewModel` now subscribes to it (pure projection: newest room → banner) instead of one-shot fetching; the seed `getActiveRooms()` runs in its own init launch, NOT through the action pipeline, because calling it inside `handleAction` drives the same collector re-entrantly and strands the emission under the unconfined test dispatcher. Forfeit dropped its optimistic state mutation — the leave clears the banner via the flow on success and correctly leaves the room visible on failure (no re-query needed). Chose a client-side projection over wiring the durable server source now: the durable/server-pushed source is gated on B2 (persisted membership), and the client projection already satisfies the "reflect my own join/forfeit live" case the item is about. Sliced the todo to the remaining off-device-reflection gap.
**Reviewer notes:** **Scope cut — dropped the proactive multi-room stale-leave.** The old `reconcileToSingleRoom` auto-left every room but the newest when >1 active room slipped through. Reactively re-issuing those leaves fed back through the same observed flow and deadlocked the consumer under `UnconfinedTestDispatcher` (the leave synchronously mutates the StateFlow the collector is on). I kept the *user-visible* behavior — the banner still shows exactly the newest room via a pure projection — but no longer proactively leaves the stale ones; the server's seat-grace timer reaps them. If we want the proactive leave back, it belongs server-side or behind a non-reentrant one-shot, not on the reactive path. The home test fake's `getActiveRooms`/`leaveRoom` now `yield()` before mutating the observed flow to mirror a real network round-trip (synchronous mutation under unconfined is what caused the reentrancy). Tests: home VM (incl. new `liveUpdate_reflectsWithoutRefetch`, rewritten `forfeit_networkError_keepsRoomVisible`), `RoomRepositoryImpl` flow seeding/leave/join, lobby + room fakes updated for the new interface method. `:apps:compose:assembleDebug` green.
**Deferred:** Off-device room-change reflection (host closes room elsewhere, server-side forfeit) — needs the durable source; rewritten into the todo bullet, gated on B2.

## feat(profile): ceremonial detail sheet for earned/prestige items

**Problem:** Earned `unlock_only` grants (Founding Member) arrive without catalog metadata, so the detail sheet showed a prettified id + bare 🎁 and said nothing about what the item is or how it was earned.
**Approach:** Added a client-side `KnownEarnedItems` metadata map (`EarnedItemMetadata.kt`) keyed by product id → name / description / how-earned / optional thanks, all strings in `:libraries:resources`. `CosmeticDetailSheet` consults it: overrides the hero glyph + title + description and renders a "How you earned it" story block with the thank-you line. **Chose a client map over a server field:** the set is tiny, only changes when we mint a new prestige grant (a code change regardless), and the copy is user-facing so it belongs next to every other string in `:libraries:resources` rather than on the wire — documented the "promote to a server field when it grows past a handful / needs per-cohort copy" trigger in the file KDoc. Also collapsed `ProfileScreen`'s separate `KnownItemEmoji` map to derive from `KnownEarnedItems` so the shelf tile and the sheet can't drift on the glyph.
**Reviewer notes:** Only `badge_founding_member_1000` has copy today (the one unlock-only grant shipped); the infrastructure means a new earned item is a map entry + four strings. No unit test — the sheet is UI-only and the map is a compile-checked literal; `:features:profile:impl:testDebugUnitTest` + `compileDebugKotlinAndroid` green. **Not verified in Studio** — the H700 title / Label "How you earned it" heading / Body story proportions are best-guess; worth an eyeball on a visual pass. No `@Preview` added (the file has none today; didn't want to half-introduce the pattern for one component).

## feat(room): you-have banner + current-hand highlight on cheat sheet

**Problem:** The in-game "Hand rankings" cheat sheet was a flat static list — no you-have banner, no indication of which ranking the player currently holds, and a generic "Hand rankings / Strongest on top." heading instead of the mock's plain "What beats what".
**Approach:** Added a `YouHaveBanner` at the top (success-tinted card: "You have" label → the player's current hand name in green → a tight Mini-card row of hole cards + a gap + board cards), computed `currentHandCategory(hole, board)` via the existing `HandEvaluator` (only when hole=2 and total ∈ 5..7 and cards distinct — null otherwise, e.g. preflop), and highlighted the matching `RankingCard` green (2dp success border + 0.14α success fill + green title). Tagged each `RankingEntry` with its `HandCategory` so the highlight is a compile-checked `entry.category == currentCategory` match. Tightened the rows (padding 16/14→14/11, inter-card spacing 4→3, inter-row D500→D300) and renamed the heading string to "What beats what", dropping the now-redundant "Strongest on top." subtitle. Hole+board flow in from the human `SeatView` at the `PlayPokerScreen` call site (works for both solo + MP since both project a human seat).
**Reviewer notes:** **Not eyeballed in Studio** — the success-tint alphas (0.14), the H800 banner name size, the 14/11 row padding, and the hole/board gap (D400) are best-guess proportions; worth a pass against `docs/todo-assets/hand-rankings.png`. The 0.14α-on-success fill follows the same alpha-tint precedent already in this file (`ActionRow`'s `accent.color.copy(alpha = 0.18f)` symbol circle), not a raw white alpha. Two previews now exercise it (MidHand = flush-draw board, River = trips); `:features:room:impl` compile + `testDebugUnitTest` green. No unit test — UI-only; the one bit of logic (`currentHandCategory`) is a thin guard over the already-tested `HandEvaluator`.

## feat(profile): swap-only equip CTA for card backs + felts

**Problem:** The cosmetic detail sheet offered an "Unequip" button on equipped card backs / felts, but those slots are single-equip — there's always one active and you change it by equipping a different one, so unequip would leave the table with no card back / felt.
**Approach:** Extracted the equip CTA into an `EquipButton` composable. For the swap-only slots (`CosmeticSlot.CardBack` / `Felt`) an equipped item now renders a disabled "Equipped" button instead of "Unequip"; slots that can legitimately sit empty (titles) keep the unequip affordance. This is the slot-aware-equip slice of the larger "Cosmetic detail/purchase sheets" item — sliced the todo to the remaining buyable-tap→purchase-sheet + richer felt/emote preview work.
**Reviewer notes:** "Equipped" reuses the existing `profile_items_equipped` string. Whether `Title`/`Tool`/`Badge` should be unequippable is a product call — I left them with the unequip button (only card back + felt are named swap-only in the todo). No unit test (UI-only branch); `compileDebugKotlinAndroid` + profile `testDebugUnitTest` green. Not eyeballed in Studio.

# In-flight (this cycle)

## feat: thread server-tunable level curve through display sites

**Problem:** Display sites derived level off the bundled `DefaultLevelCurve` (`levelProgressFor(xp)`, no curve arg), so a server-retuned `progression.levelCurve` would make the shown level disagree with the granted one.

**Approach:** Added a `LocalLevelCurve` `staticCompositionLocalOf` in `:libraries:ui` (default `DefaultLevelCurve` so previews/tests render without a provider) and provide it once at the `App.kt` root from `ProgressionConfig.levelCurve()`, recomputed when app-config rolls in. Composable display sites (`StatsScreen`, `ProfileScreen`, `QaMenuScreen`, DS `LevelPill` xp-overload) read the local; the curve-owning ViewModels (`HomeViewModel`, `ShopViewModel`, `EditProfileViewModel`) pass `progressionConfig.levelCurve()` to `levelProgressFor`. Chose the composition-local for leaf composables (todo's stated direction) over DI-per-leaf, and one-time-per-config-change read over a per-frame flow since the curve is a rare server retune.

**Reviewer notes:** Sliced — the multiplayer-table seat/opponent level projection (`PlayPokerViewModel` / `RemotePokerSessionFactory.occupantsFor` / `TableUiState` badge) is *not* in this commit: it needs the curve threaded through the `PokerSessionFactory` projection interface, which is load-bearing MP code I kept out of a P2 display fix. `docs/todo.md` was rewritten to scope that remainder + the part-(b) server reconcile. Added a `ShopViewModelTest` case proving a steep configured curve keeps 100 XP at level 1 where the default curve shows level 2. Preview-only `levelProgressFor` call sites (`HomeScreen`, `HomeHeader`) left on the default — sample data, exempt.

## test: cover late-mount gameplay replay at the VM level (B6)

**Problem:** B6's subscribe-after-action gap — tests subscribed before the action, the app subscribes after — left the gameplay flow without VM-level late-subscriber coverage, and `FakeRoomConnectionHandle` couldn't catch the regression because it flattened the whole gameplay flow into one `replay=0` SharedFlow.

**Approach:** Made `FakeRoomConnectionHandle.gameplayFrames` faithful to production (`ReconnectingRoomSocket`): the latest `StateSnapshot` rides a replay-1 `MutableStateFlow` merged with the replay-0 events `SharedFlow`, so a late subscriber replays the live table while mid-hand joiners don't re-fire stale event animations. Added a reusable `startHandBeforeJoinerMounts()` harness scenario and a VM-level test asserting a play screen mounting *after* the deal renders the table instead of sitting on Loading.

**Reviewer notes:** Test-only, no production change. The fake-fidelity fix touches the shared `FakeRoomConnectionHandle` used by both `RemotePokerSessionTest` and the integration suite; ran the full `:features:room:impl` test suite green to confirm the StateFlow-conflation semantics don't break existing before-subscribe tests. The socket-level replay test (`gameplayState_replayReachesLateCollector`) already existed — that part of the item's premise was stale; this adds the missing VM-level guarantee + the reusable harness. The todo was re-scoped to the remaining "audit other hot flows" sweep rather than deleted.

## feat: thread the tunable level curve through the MP table projection

**Problem:** The multiplayer-table seat/opponent level projection (`PlayPokerViewModel` self-level derivation, `RemotePokerSessionFactory.occupantsFor`, `TableUiState.badgeFor`, the solo `seatToOccupant`) still derived levels off the bundled `DefaultLevelCurve`, so a server-retuned `progression.levelCurve` made at-table levels disagree with the granted/displayed level — the remaining slice (a) of the curve-threading item.

**Approach:** Injected `ProgressionConfig` into `PlayPokerViewModel` and added a live `levelCurve` accessor (cheap config read, so a mid-session retune reflects on the next projection). Threaded a `curve: LevelCurve` parameter (defaulting to `DefaultLevelCurve` for previews/tests) through the `PokerSessionFactory.occupantsFor`/`tableFor` interface, `TableUiState.fromGameState`/`badgeFor`, and the `seatToOccupant` helper; the VM passes `progressionConfig.levelCurve()` at every projection + its own three `levelProgressFor` sites. Chose a threaded param over injecting `ProgressionConfig` into each assisted-injected factory — keeps the factories config-free and matches the todo's stated direction (VM owns the config, projection takes the curve).

**Reviewer notes:** Part (b) — the server-side reconcile of offline `levelup_<level>` grants against the same curve — is **not** in this commit; the todo was rewritten to scope only that server remainder. Added `FakeProgressionConfig` to the room test `Fakes.kt` (the module depends on `:libraries:cards`, not its impl, so the impl's fake isn't visible) and two `RemotePokerSessionFactoryTest` cases proving a steep configured curve (`baseXp = 10_000`) keeps a 2,500-XP opponent at level 1 where the bundled curve shows level 4. Full `:features:room:impl` test suite green; `:apps:compose:assembleDebug` green (confirms the new `ProgressionConfig` constructor dep resolves in the DI graph).

## fix: replay the terminal room-close to a late subscriber

**Problem:** The B6 hot-flow sweep ("audit remaining MP flows for the late-subscriber symmetry rule") turned up one genuine gap: `RemotePokerSession.roomClosed` was a `MutableSharedFlow(extraBufferCapacity = 1)`, not `replay = 1`. A terminal close fanned out by `collectConnection` before the VM's sibling-launched `roomClosed` collector attaches is dropped — `extraBufferCapacity` with `replay = 0` evicts a buffered value once it has no subscriber, it never replays — so a close racing session bootstrap would leave the user on a Disconnected banner with nothing popping the screen.

**Approach:** Changed `_roomClosed` to `MutableSharedFlow(replay = 1)`. The reason is terminal + idempotent (the VM collects it once to pop the screen), so replaying it to a late collector is exactly right. Added `roomClosed_replayReachesLateCollector` (subscribe *after* the close emits, assert the reason replays) — it fails on the old `extraBufferCapacity = 1` and passes on `replay = 1`, so it both proves the bug and guards the fix.

**Reviewer notes:** Completes (and removes) the "audit remaining hot flows" todo. The rest of the swept flows are already correct: `gameStateFlow`/`connectionState` (session) and `activeRooms` (`RoomRepositoryImpl`) are `StateFlow`s (implicit replay-1, late subscriber always gets the current value); `events` is `replay = 16` with an existing late-subscriber test; `emoteBlasts` is intentionally `replay = 0` (a live reaction must not replay stale blasts); the socket-level `connection`/`gameplayFrames` have their own replay-reaches-late-collector tests. So `roomClosed` was the lone real gap. Full `:features:room:impl` unit suite green; `:apps:compose:assembleDebug` green.

**Deferred (evaluated this run, passed — reviewer please triage):**
- **Reconcile level-up grants server-side against the curve (P2):** not a clean self-contained slice. The server (`apps/server`) doesn't depend on `:libraries:cards`, so it has no `LevelCurve`/`levelProgressFor` — reconcile needs either a new shared progression-model module or duplicated curve math (a real module-boundary call, drift risk if duplicated). And the levelup grants don't flow through `progression/sync`: chip grants ride the wallet sync (`idempotencyKey = "levelup_<level>"`), cosmetics go through `POST /v1/me/grants/level-cosmetic/{id}` — so the reconcile spans multiple surfaces and would change a response contract + add grant-voiding logic touching the chip ledger. Wants human eyes on the module boundary + contract; left the todo scoped to this server remainder.
- **Degraded account-creation pending UX polish (P2):** the "mirror Retry near `SaveProgressBanner`" idea conflates two orthogonal banners — `SaveProgressBanner` is a sign-in nudge for *anonymous* guests, `AccountSetupBanner` is the *creation-in-progress/failed* state and already has a Retry button + auto-retry on reconnect. The rest (richer dialog, device-verify copy/placement) needs visual/device verification. No confident headless slice; directional UX call left for the human.

**Deferred (cycle scoping — reviewer please triage):** I scoped out several high-value items this cycle because each hit a real prerequisite rather than a directional judgment call — flagging so the next worker doesn't re-investigate:
- **Per-turn MP timer (B3, P1):** not a clean slice. Enforcement is server-only, but a useful version needs (a) a new `turnDeadlineMs` field on the `GameState` wire type, (b) a cancel-on-action background scheduler in `GameSession` (needs an injected scope + clock for deterministic tests — neither is wired today), and (c) a client countdown render. Shipping enforcement *without* the visible countdown would auto-fold a player with no warning — worse than nothing. Wants the human's eyes on the wire/scheduler design.
- **Banned/suspended enforcement (P2):** the "minimum slice" isn't self-contained — the server validates JWT signature/exp only and has no access to ban state (`auth.users.banned_until` is a DB field, not a JWT claim). Needs an app-level moderation table + a ban query in `Authentication.kt` (the todo even notes this). Schema migration → human territory.
- **B4 spectator (P2):** `SubmitIntent` already rejects a non-seated actor and `scrubbedFor(-1)` already hides hole cards, so 2 of 3 acceptance criteria hold for a hypothetical seatless viewer — but `RoomSocketRoutes` hard-closes non-members and the join path always allocates a seat, so the real work is a seatless-membership model change, not a one-line auth tweak.
- **Session-expiry blocking screen (P1):** `onSessionRejected` already calls `gateway.signOut()` and tears down to `Unauthenticated` before any UI shows, so "Retry recovers in place, identity intact" needs the rejection flow restructured to pause-before-teardown — a load-bearing auth change I can't verify without device testing.
- **detekt framework + verifyStrings (P1):** self-contained and worth doing, but landing a custom-rule `RuleSetProvider` classpath + a generated baseline + a pre-push hook green-on-day-one is fiddly to verify in a headless run where I can't iterate fast; left for a session that can run detekt across all modules interactively.

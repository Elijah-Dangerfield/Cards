**Stashed WIP (popped clean):** Started the cycle with dirty WIP touching the post-purchase-snackbar item (`ProfileFeatureEntryPoint.kt` + `MyItemsScreen.kt` + `MyItemsRoute.kt` + `ShopFeatureEntryPoint.kt` + the `shop_snackbar_redeem_succeeded_action` string) **and** the password-reset flow (`AuthRepository.kt` / `AuthOutcomes.kt` + the Supabase gateway/impl trio + a new test + one Onboarding fake). Stashed as `worker-presweep-20260528-131044` to keep my tree clean — that's why I steered around both `docs/todo.md` items this cycle. End-of-run pop ran with one auto-merge on `strings.xml` (my emoji-tray empty-state strings near line 622 vs the user's `shop_snackbar_redeem_succeeded_action` near line 17 — non-overlapping; merge clean, no conflict markers).

**Stashed WIP — resolved:** A prior worker stashed mid-flight test edits to `ChipsRepositoryImplSyncTest.kt` as `stash@{0}: worker-postsweep-chips-lifecycle-tests-mystery-20260528-124013` (note in the previous in-flight version). Those edits were my work-in-progress on the ChipsRepository lifecycle-path todo from a concurrent run; the proper, working version ships in the `test(chips)` block below. The stash is superseded — reviewer can `git stash drop stash@{0}` after confirming the new tests cover the same surface (`onColdBoot` triggers sync; `onForeground(isColdBoot=true)` no-ops; `syncMutex` serializes).

**Stashed WIP — resolved:** Earlier in this cycle a concurrent worker stashed my in-flight `feat(profile): surface "Member since ..."` edits twice (`worker-presweep-20260528-124901`, `worker-presweep-retry-20260528-125132`). I popped both stashes and the work shipped via commit `39bb714` (the `feat(profile)` block below). No outstanding `*-presweep-*` stash labels remain from this cycle's concurrent activity — anything left in `git stash list` predates today's worker runs.

## test(config): pin ConfigOverrideRepositoryImpl persistence + dedup

**Problem:** `ConfigOverrideRepositoryImpl` is the QA-menu / per-session override surface ([`features/profile/impl/.../QaMenuScreen.kt`](../../features/profile/impl/src/commonMain/kotlin/com/cards/features/profile/impl/QaMenuScreen.kt) reads it via `FeatureConfig`) and one of the four cascade legs in `OfflineFirstAppConfigRepository`'s `mapOverrides` join, but had no test sibling — the dedup-by-path on `addOverride`, the mutex serialization, the malformed-JSON fallback in `decodeOverrides`, and the `clearAll`-nulls-the-JSON contract were all unverified. `OfflineFirstAppConfigRepositoryTest` exercises the *integration* between an override repo and a config repo (with a `FakeConfigOverrideRepository`), not the impl itself.
**Approach:** New `ConfigOverrideRepositoryImplTest` extending `CoroutineTest`, with an in-file `FakeConfigCache` mirroring the shape `OfflineFirstAppConfigRepositoryTest` already established (MutableStateFlow → `updates`, sync `get` / `set` / `clear`, `seed(configJson, overridesJson)` helper for hydration cases). Nine tests pin: hydrate-from-cached-JSON on init, empty-cache → empty overrides, malformed-JSON → fall back to empty (NOT crash — `decodeOverrides`' `onFailure` swallow is the safety net), `addOverride` writes through to cache, same-path re-add replaces (the "toggling a setting back doesn't pile up entries" comment in the impl), unrelated paths preserved, `clearAll` nulls the persisted JSON field (verified end-to-end, not just an empty array round-trip), `getOverridesFlow` emits on every mutation, and the `addOverride` path preserves the unrelated `configJson` field on the snapshot (a regression here would clobber the cached app config every time the QA menu toggled a flag).
**Reviewer notes:** **Source:** worker-hydrated this cycle (not human-curated). Falls in Lane A (test coverage) — picked because the impl is small (74 LOC), the contracts are mechanical, and a regression in the dedup-on-add path would silently double-write the same override into the persisted JSON, while a regression in the snapshot-field-preservation path would clobber the cached app config on every QA-menu toggle. No production code touched. Uses `AppCoroutineScope(dispatchers)` from the test-base — same construction pattern `OfflineFirstAppConfigRepositoryTest` uses for the production-shape `appScope` parameter, so the eager `stateIn` initialization runs against the test scheduler.

## feat(auth): add forgot-password flow with reset-email send

**Problem:** [`AuthRepository`](../../libraries/identity/src/commonMain/kotlin/com/cards/libraries/identity/auth/AuthRepository.kt) exposed every auth verb except `sendPasswordResetEmail`, so a claimed-account user who forgot their password had no in-app recovery path. Supabase already supports `resetPasswordForEmail` — only the wiring was missing.
**Approach:** End-to-end vertical slice. Added `SendResetOutcome` (`Sent` / `RateLimited` / `NetworkError` / `Unknown`) — narrower than the prior-cycle `ResendOutcome` because Supabase's reset endpoint doesn't expose retry-after seconds. Wired `AuthRepository.sendPasswordResetEmail` through the same gateway seam as `resendVerificationEmail`, including no mutex (doesn't mutate session state) and the same `RestException(429) → RateLimited` mapping. UI side: new `ForgotPasswordRoute` (arg-less `class`, not `data object` — see the routing rule in memory), `ForgotPasswordViewModel` (SEAViewModel; submit gated on `email.contains('@')`, sent-state short-circuits re-submit), and `ForgotPasswordScreen` reusing the existing `AuthShell` + `EmailField` helpers. Inline "Forgot password?" text-button on `SignInScreen` between the password-field errors and the submit row. **Direction call:** the todo asked for an `UnknownEmail` outcome — deliberately dropped because Supabase doesn't differentiate at the API layer (account-enumeration mitigation); the sent-state body reads "If %s is registered, we sent a link to reset your password." regardless. Extended the shared `EmailField` helper with optional `imeAction` + `onSubmitImeAction` params so the single-field forgot-password form can fire on the Go key — defaults keep both existing SignIn/SignUp callers unchanged.
**Reviewer notes:** The recovery deep-link target (`ResetPasswordScreen` reading the access token from the URL fragment) is explicitly out of scope per the todo — gated on the Supabase email-confirm redirect-URL dashboard entry the human still owns in `developer-todo.md`. Until that lands, tapping the reset link in the inbox lands on localhost (same hole `cc766b55` flagged for sign-up verification). Touched seven `AuthRepository` fake implementations across feature test modules (lobby/shop/profile/progression/identity-impl/cards-impl + the onboarding AuthViewModelFakes) to add `sendPasswordResetEmail = error("unused")` overrides — no behavioural change. `ForgotPasswordEvent` is an empty sealed interface (the screen self-pops via `onBackToSignIn = router.goBack()` rather than routing through a VM event) — required by SEAViewModel's `<S, E, A>` shape, harmless at the call site. Note: human's pre-existing WIP on the post-purchase-snackbar item was sitting in my working tree from a prior worker's `git stash pop` (see top-of-file note) — left those four files unstaged so they stay handed back to the human.
**Deferred:** ResetPasswordScreen (recovery deep-link landing surface) — append to `docs/backlog.md` once the Supabase dashboard redirect-URL entry resolves; the access-token-from-fragment screen is a clean follow-up against this same `ForgotPasswordRoute` entry surface.

## test(progression): pin XpEventRepositoryImpl projection + enum fallbacks

**Problem:** `XpEventRepositoryImpl` is the projection layer between the Room-backed `xp_events` ledger and the domain `XpEvent` (every Home / Stats / Profile recent-XP surface reads through it), but had no test sibling. The entity→domain mapping for `id` / `deltaXp` / `handId` / `description` and the `parseSource` / `parseMode` enum-parse fallback (forward-compat for unknown values getting sync'd from a future server build) were all unverified.
**Approach:** New `XpEventRepositoryImplTest` extending `CoroutineTest`, with an in-file `FakeDao : XpEventDao` mirroring the shape `ProgressionRepositoryImplTest` already established (MutableStateFlow → asStateFlow for both `observeRecent` / `observeSince`, last-cursor capture on `observeSince` so the cursor pass-through is assertable). Five tests: domain-mapping happy path for both observe methods, unknown source → BASE fallback, unknown mode → BOTS fallback (chosen because MP credit rules gate on `mode == MULTIPLAYER` so the safer default is BOTS — a misparsed row should NEVER silently grant MP credit), and an exhaustive `XpSource × XpMode` round-trip so a future rename of an enum symbol that forgets to grep for the string serialization gets caught.
**Reviewer notes:** **Source:** worker-hydrated this cycle (not human-curated). Falls in Lane A (test coverage) — picked because the impl is small, the contracts are mechanical, and a regression in the enum-parse path would silently misclassify ledger rows after a server-side enum addition. No production code touched.

## feat(room): celebration sheet plays full reveal-with-haptics per unlock

**Problem:** `AchievementCelebrationSheet` (bot-mode hand-end unlock celebration) showed each earned achievement as a static `Display.D1200` emoji glyph above the name + reward summary — no entry animation on the medallion, no haptics, no tactile beat that something *happened*. The tutorial-complete `AchievementUnlockedDialog` already used the `AchievementUnlockReveal` DS primitive (anticipation pause → shake with per-peak haptic ticks → burst + particles → medallion slam-in with bounce-back haptic) and the contrast made the bot sheet feel comparatively flat.
**Approach:** Direction (b) from the todo — borrow the dialog's haptic + reveal-animation pattern rather than collapsing the sheet into a dialog. The sheet's multi-unlock stacking is its load-bearing feature (a single hand can earn multiple achievements; the dialog can't stack), so the wrapper stays. Inside `CelebrationCard`, swapped the static icon `Text` for a 140 dp `Box { AchievementUnlockReveal(achievement) }`. The existing per-card `AnimatedVisibility(fadeIn + scaleIn(0.85f))` entry stagger still gates the card chrome at +90 ms per index — the reveal animation inside each card fires on first composition (LaunchedEffect keyed on revealKey = Unit), so each medallion plays its sequence as its card lands. 140 dp instead of the dialog's 180 dp so multi-unlock stacks stay scrollable without dwarfing the reward / cosmetic-attribution text below.
**Reviewer notes:** **Direction call vs (a):** rejected "replace sheet with dialog" because the sheet's stack is the right shape for the common "first hand of the game → 3 achievements at once" path; the dialog would have to either queue N sequential dialogs (5+ tap dismisses, breaks flow) or stack medallions inside the dialog (which is exactly the sheet, just renamed). **Optional polish deferred:** click-to-reveal-next on multi-unlock (the original todo's optional bullet). The current cascade staggers card entrances by 90 ms each which already gives a "one-by-one" feel; the truly gated tap-to-reveal would need a per-card mystery → revealed state machine. Punted as a new todo item, see slice rewrite above. **Build / test:** assembleDebug clean; `:features:room:impl:test` green (no behavioral change — the swap is presentational, and `AchievementUnlockReveal` is already covered by its own DS-level previews).
**Deferred:** Click-to-reveal-next polish — sliced into a new `[P1]` todo entry replacing the original celebration item.

## feat(room): emoji tray renders empty-state shop hint when no pack owned

**Problem:** Brand-new users (no `emotes_*` pack) lost the emoji affordance entirely — `PlayPokerScreen.kt`'s `availableEmojis.isNotEmpty()` guard hid the trigger and `TopBarEmojiButton`'s own `if (emojis.isEmpty()) return` early-exit doubled the gate. The spec (§5.5, locked 2026-05-28) says the trigger must always render and tapping it must surface a path to start sending. Receiving emotes already works for everyone.
**Approach:** Three pieces. (1) `EmojiPackCatalog.SamplePreview = listOf("💃", "🥺", "😤", "👑")` — four glyphs sampled from distinct shipping packs (drama / cute / fierce / royal) so the empty-state hints at the visual range without privileging one pack. (2) `TopBarEmojiButton` now always renders the trigger; the popup body swaps between `EmojiPickerRow` (owns ≥1 pack) and a new `EmptyEmojiPopup` (greyed-out preview row at 0.4 alpha + caption "Buy emoji packs in the shop to send these" + small primary CTA "Shop emote packs"). The popup chrome (Surface, Radii.Card-ish radius, felt-accent fallback) matches `EmojiPickerRow` so the visual continuity reads as "this is the same tray, just empty for you right now." (3) New `onOpenShop` callback threaded from `PlayPokerFeatureEntryPoint` → `PlayPokerScreen` → `TopBar` → `TopBarEmojiButton`, wired to `router.switchTab(ShopGraph)`. The shop's existing top-of-grid placement already shows emote packs prominently, so a `switchTab` lands the user where they need to be without a category-deep-link infrastructure (none exists yet — see Out-of-scope below).
**Reviewer notes:** **Direction call:** todo asked to "tap-through to the shop's emote-pack category" but the shop today doesn't surface category-deep-link routing — `ShopRoute` is arg-less and there's no `ShopCategoryRoute`. Building one would have been scope creep. `switchTab(ShopGraph)` lands the user on the shop grid where emote packs are already visible; if the human wants category-pinning later, that's a separate item against the shop module. (Alternative I rejected: chain `switchTab(ShopGraph)` + `navigate(ShopProductSheetRoute(productId = "emotes_drama"))` — too opinionated about which pack to push.) `:features:room:impl` picked up `:features:shop` as an api dep for the `ShopGraph` reference — that's the only new module-graph edge.
**Deferred:** Category-deep-link to shop emote-packs (would need new `ShopCategoryRoute` + grid auto-scroll). Reviewer can triage — nothing tracked yet.

## refactor(tutorial): step-counter pill goes inline + drops per-hero label

**Problem:** Basics narration steps showed a top-center pill labeled "Your goal" / "Your hand" / "Your turn" (from `NarrationHero.topBarLabel`) with a progress dot row underneath. The placement competed with the status bar / back button and didn't read as "where am I in the flow" — users missed the section structure entirely.
**Approach:** Two-axis change. (1) Pill text unified to the generic "Step X of Y · Section" format that tableau steps already use (the data was always on `TutorialState` — `sectionStepIndex` + `sectionTotalSteps` + `section.displayName`). (2) On narration, render the pill **inline** above the gold headline inside `NarrationStep`'s Column, between the weighted hero block and the title; on tableau, keep the existing top-center overlay because the play-screen surface leaves no other room. Dropped `NarrationHero.topBarLabel` + the three `tutorial_hero_*_label` resources + the now-unused `ProgressDots` helper and the dual-mode `customLabelRes` branch on `StepCounterPill`. Picked inline-above-headline over a top-of-content placement because the former groups the pill visually with the title block (same eye-line) while the latter would have read as a second top-bar.
**Reviewer notes:** None — pure UX refactor, no model changes. `tutorial_step_counter` resource untouched. Tutorial VM test sibling didn't reference the dropped fields so no test churn.

## fix(auth): make verify-email button read as a server check, not a self-assert

**Problem:** The verify-email screen's primary CTA was "I confirmed, continue" — which reads as "the user is asserting they confirmed and the app trusts them." The system *does* server-verify (`refreshSession()` only proceeds when Supabase returns `email_confirmed_at`), but the wording makes the user think tapping the button bypasses confirmation when the underlying redirect (localhost) silently succeeded.
**Approach:** Three small copy swaps in `strings.xml` — button "I confirmed, continue" → "Check verification"; progress label "Checking…" → "Checking with server…"; body sentence appended "and we'll check with our server" so the contract is explicit at first read. No model / VM changes (the data was already correct — only the language was misleading).
**Reviewer notes:** Out of scope per the todo: the underlying Supabase dashboard redirect URL fix lives in `developer-todo.md`; the model is already correct.

## fix(ui): bump earned-badge text contrast to WCAG AA via accentEarned token

**Problem:** OwnershipBadge ("Earned" variant) and EarnedTag rendered `accentSecondary` (Purple600 = #8E24AA) on `surfaceSecondary` (Gray800 = #2D2D2D) — measured ~2.0:1, below WCAG AA's 4.5:1 floor for body text.
**Approach:** Extended the `Colors` interface with a new semantic token `accentEarned` (defaulted to `Purple100` = #E1BEE7) and routed both badge text colors through it. Picked (a) "brighten the purple" over (b) "drop to status.okay" because the earned-vs-purchased visual distinction in OwnershipBadge relies on the purple cue, and (c) "lighten background" would have shipped a `surfaceTertiary` pill that visually competes with neighboring `LastActionPill` chrome. Purple100 on Gray800 measures ~8.5:1, comfortably AA. Picked a semantic token over a raw `ColorResource.Purple100` because the DS already deprecates direct ColorResource use (compiler warning fires on every callsite).
**Reviewer notes:** Token is currently single-callsite; if no other surface needs an "earned" affordance it could collapse back to `accentSecondary` once the palette gets reshuffled. Out of scope here: auditing every other accent-on-surface combo for contrast (the todo entry called that out as a separate pass).

## feat(profile): surface "Member since {Month YYYY}" on ProfileHeader

**Problem:** Spec §6.2 lists "Member-since date" as a profile field, and `Profile.Authenticated.createdAt: Instant` already carries it, but `ProfileHeader` rendered avatar + name + LevelSummary with no member-since affordance.
**Approach:** Threaded `authenticated?.createdAt` into a new `ProfileSettings.memberSince: Instant?` and rendered a subdued `Body.B400` / `textSecondary` caption between the display name and LevelSummary when the field is non-null. Formatter follows the existing `SeatTenure` pattern: `Instant.toLocalDateTime(TimeZone.currentSystemDefault())` → `Month` resource + year, dropped into a new `profile_header_member_since` string ("Member since %1$s %2$d"). Fallback profiles naturally skip the row (no `createdAt`), matching the spec's "degrade gracefully" guidance. Updated the Claimed preview to seed a fixed `Instant` so the row renders in Studio.
**Reviewer notes:** The `monthResource(Int) → StringResource` switch is duplicated from `features/room/impl/.../SeatTenure.kt` — both call sites are tiny, so the lift wasn't worth pulling into a shared module right now; if a third call site lands, lift it. Flagging here so it's visible.
**Deferred:** Could lift `monthResource` + `Instant→"Month Year"` into `:libraries:ui` or `:libraries:cards` once a third caller appears — noting inline, nothing tracked yet.

## test(chips): pin ChipsRepositoryImpl lifecycle hooks + sync mutex

**Problem:** `ChipsRepositoryImplSyncTest` pinned the per-outcome sync reconciliation but left the `AppEventListener` surface uncovered — `onColdBoot` launching a sync, `onForeground(isColdBoot=true)` defer-to-cold-boot guard, and the `syncMutex`'s serialization contract had no tests.
**Approach:** Three additions to the existing test file. Lifecycle tests pass an explicit `AppCoroutineScope` through a new `buildRepoWithScope` overload so the test can `join()` the launched coroutine after firing the event — `runUnitTest`'s scope doesn't otherwise see appScope's children, and `testScheduler.advanceUntilIdle()` alone doesn't drive the MockEngine handler once it hops to the engine's IO dispatcher. The mutex test fires two parallel `async { repo.sync() }` calls against a handler that records start/end markers around a `delay(50)`; with the mutex serializing, the log is `start-1, end-1, start-2, end-2` (without the mutex it would interleave to `start-1, start-2, end-1, end-2`). Required widening `buildRepo`'s handler param to `suspend` so `delay` is legal inside.
**Reviewer notes:** None — see the resolved-stash note above for context on the duplicated WIP.

## refactor(shake): route ShakeHandler through DispatcherProvider.main

**Problem:** `ShakeHandler` constructed its `CoroutineScope` with raw `Dispatchers.Main`, violating the repo's dispatcher-injection rule (production code consumes `DispatcherProvider.*` so tests can swap a `TestDispatcher`).
**Approach:** Added `dispatchers: DispatcherProvider` to the `@Inject` constructor and routed the scope through `dispatchers.main`. `apps:compose` already depends on `:libraries:flowroutines`.
**Reviewer notes:** No new test — the todo entry explicitly noted this dispatcher swap doesn't change observable behavior and no test sibling exists.

## test(tutorial): pin TutorialViewModel state machine

**Problem:** `TutorialViewModel` drove the entire onboarding flow (`advance` / `goBack` / `skipBasics` / `restartBasics` / `submit`) plus the `wasFirstCompletion` race against `recordTutorialComplete()`, but had no test sibling — every other feature VM does.
**Approach:** New `TutorialViewModelTest` extending `CoroutineTest`. Local `TutorialAchievementsFake` (configurable `tutorialEarned` + `throwOnRecord`) so the three completion outcomes (first-time / replay / grant-failure → fallback to `true`) are individually pin-able; a local `TutorialAppCacheFake` keeps the test isolated from `Fakes.kt`. The script-walk tests don't hardcode step indices — they read `TutorialScript.steps` for the first basics / non-basics / advance-gated step so the test survives script edits. Used `UnconfinedTestDispatcher` (the `CoroutineTest` default) so `viewModelScope.launch` in `recordCompletion()` drains inline without `advanceUntilIdle`.
**Reviewer notes:** None.

## test(server): pin /v1/avatars route contract

**Problem:** `avatarRoutes()` is the unauthenticated endpoint the avatar picker hits before the Supabase JWT lands, but it had no route test — the "anon ok / full registry / palette / Cache-Control / unlock id presence" contract was unverified despite sibling routes (Equipment, Wallet, Inventory, Me) all having one.
**Approach:** New `AvatarRoutesTest` using Ktor `testApplication` against the route in isolation (no auth plugin installed since the endpoint is intentionally anon). Five tests pin: 200 without auth, pack count + ordering matches `AvatarPacks.all`, `backgroundPalette == AvatarPalette.values`, `Cache-Control: public, max-age=60`, premium packs carry `unlockProductId` while starter is null.
**Reviewer notes:** None.

## fix(room): pin emote-tray trigger to a square footprint

**Problem:** The emote-tray trigger on the play-poker screen rendered as an ellipse instead of a perfect circle — `EmojiButton` uses `defaultMinSize` on its inner Box and emoji glyphs measure wider than tall, so the Surface grew horizontally even though `Radii.IconButton` (= `Radii.Round`, `percent=50`) wants a 1:1 aspect ratio to read as a circle.
**Approach:** Lifted the existing `triggerFootprint = iconSize + 2 × padding` computation out of the popup branch and applied `Modifier.size(triggerFootprint)` to the outer wrapping `Box`. Both the live `EmojiButton` and the `CooldownChip` Surface inherit the fixed square, and the popup's vertical offset still keys off the same value so the picker still anchors cleanly under the trigger.
**Reviewer notes:** Considered fixing this inside `EmojiButton` itself but that would touch every caller and the size-vs-glyph trade-off is documented as intentional on the primitive. Pinning at the call site is the smaller, targeted change.

## test(level): pin XP curve + LevelProgress derived props

**Problem:** `Level.kt` (quadratic XP curve, `levelProgressFor` resolver, `LevelProgress` derived fraction with `coerceIn(0f, 1f)` + divide-by-zero guard) is consumed by Home / Stats / Profile / Shop / Room VMs but had no test pin; the `MAX_LEVEL=100` clamp, the negative-XP clamp, and the fraction-fallback were all unverified.
**Approach:** Added `LevelTest` covering the curve at known levels, the `<1` clamp on `xpToLevelUpFrom`, the level-from-XP boundaries (0, 99, 100, negative, beyond MAX_LEVEL), the three derived `LevelProgress` properties, plus a monotonicity sweep. New `commonTest.dependencies` block added to `:libraries:cards` (it had none) wired to `:libraries:flowroutines:testing` for source-set parity with the rest of the module graph; the tests themselves use plain `kotlin.test` because `Level.kt` is pure math.
**Reviewer notes:** None.

## fix(room): align win-odds card chrome across the flip

**Problem:** Flipping the player-info tile to the win/lose-odds back face visibly jumped — the front face was a `Column` with `fillMaxSize` modifier + `(horizontal = 10.dp, vertical = 4.dp)` padding, but the back face was called without `fillMaxSize` (so its Column sized to content) AND used `(horizontal = 8.dp, vertical = 8.dp)` padding. The outline of the card itself shifted mid-rotation, breaking the "same card flipped over" illusion.
**Approach:** Two-line fix. (1) Pass `Modifier.fillMaxSize()` to `PlayerInfoTileBack` from the wrapper so the back face's Column actually fills the same space as the front. (2) Match back-face padding to the front's `(10, 4)` token. The front face's vertical-4 padding is already tuned tight to fit inside the locked hole-card row height (per the existing comment in `PlayerInfoTile`), and the back's content (heading + two 44 dp dials + optional tie footer) fits comfortably in the same 4-dp vertical budget — no clipping under preview.
**Reviewer notes:** The todo also recommended moving the back face's `.clickable(onClick = onTapClose)` to the outer wrapper Box so both faces inherit it. I left that out: routing tap-anywhere through the wrapper would (a) introduce a new front-face tap-anywhere-to-flip affordance (today only the FlipAffordance icon + swipe flips forward) and (b) add another red-ripple flash on the front face (same M3 ripple issue we just patched for bottom sheets). Both are scope creep beyond the chrome-jump fix the todo flagged. Leaving the back-face clickable as-is keeps the change minimal.

## fix(ui): stop AvatarCircle twitching on profile-tab visits

**Problem:** The DS rule says identity content animates between states, not on mount, but `AvatarCircle` twitched every time the profile tab opened. The old `hasComposed` gate suppressed only the literal first composition; the tab mounts with the initial-fallback (emoji=null → first letter), profile data arrives a frame later with an emoji, and that second composition passed the gate — so the entrance transition fired for the "first real avatar value" on every visit.
**Approach:** Replaced the `hasComposed` gate with `hasShownAnEmoji`, keyed on `LaunchedEffect(content.isEmoji)` so it only flips to `true` after a real emoji content lands. The placeholder → real-data hand-off no longer animates; EditProfile (which mounts with the user's current emoji already resolved) still animates every picker swap because the gate has flipped by the time the user taps.
**Reviewer notes:** Edge case I considered but didn't try to engineer around: a user with no emoji set who edits their display name (initial letter changes A→B) won't get an animated letter swap, because `hasShownAnEmoji` never flips. This was acceptable: the symptom hits every user on every tab visit; the regression only hits the "no-emoji user renaming themselves" path which is rare. If we wanted to cover both, the right move is probably a separate "content has been stable for one frame" timer — overkill for this PR.

## fix(ui): kill red ripple on bottom-sheet drag handle

**Problem:** Long-pressing the top of any bottom sheet flashed red. M3's `ModalBottomSheet` wraps its drag-handle slot in `Modifier.clickable` (for the a11y expand/collapse), whose default ripple resolves to `MaterialTheme.colorScheme.primary` — and `AppTheme.MaterialWrapper` deliberately paints every Material color `Color.Red` as a tripwire for un-DS'd widgets. The ripple was the tripwire firing legitimately.
**Approach:** Wrapped the `ModalBottomSheet(...)` call inside `BaseBottomSheet` with `CompositionLocalProvider(LocalRippleConfiguration provides null)`. That disables the ripple specifically for the M3-internal clickable while leaving the a11y click intact. DS content inside the sheet does not use Material ripples, so scoping the override to this call site is a no-op for sheet content. Chose this over routing a custom DS ripple color through `RippleConfiguration` because the drag handle's affordance is gesture-first (drag), not click-first — visual feedback on press is decoration, not signal.
**Reviewer notes:** Left a short justification comment on the `CompositionLocalProvider` line — without it, a future reader sees a null-out and can't tell whether it's a workaround or a stylistic choice. AGENTS.md says default to no comments; the "workaround for a specific bug" carve-out fits here.

## fix(auth): dismiss keyboard when submitting sign-in / sign-up forms

**Problem:** Tapping the submit button or pressing the keyboard's "Go" action on the auth screens left the soft keyboard on screen, covering inline errors, loading state, and the claim-progress dialog.
**Approach:** Captured `LocalSoftwareKeyboardController.current` at each `SignInScreen` / `SignUpScreen` body, wrapped it in a local `submit = { keyboardController?.hide(); onAction(...Submit) }` lambda, and routed every Submit caller (the primary button + every `onSubmitImeAction` slot on the email/password fields) through it. Doesn't touch `VerifyEmailScreen` — that one has no text inputs to begin with.
**Reviewer notes:** None.

## test(progression): pin ProgressionRepositoryImpl counter deltas + ledger rows

**Problem:** `ProgressionRepositoryImpl` is the only thing standing between hand outcomes and the player's lifetime XP / hand counters, but had no test sibling. `XpCalculatorTest` pins the pure math; the repo's counter-delta + ledger-row composition (the five `handsWonDelta` / `handsFoldedDelta` / `handsLostAtShowdownDelta` / `botHandsPlayedDelta` derivations + `applyAchievementXp`'s `require(delta>0)` + `ACHIEVEMENT/BOTS/null-handId` tagging) was unverified.
**Approach:** New `ProgressionRepositoryImplTest` extending `CoroutineTest`, with in-file `FakeProgressionDao` + `FakeXpEventDao` matching the existing `Achievement` / `Inventory` test style. Nine tests pin the eight bullets the todo enumerated: win/loss/fold counter routing, bot-vs-MP `botHandsPlayed` gate, one-ledger-row-per-`XpCalculator`-award (asserted both on the inserted entities and on the returned `XpEvent`s), zero-aware insert when calculator returns nothing extra, `applyAchievementXp(0|negative)` throws, achievement-XP write tags `ACHIEVEMENT/BOTS/null-handId` and leaves hand counters at 0, `deleteAll` clears both DAOs. Reuses the `FixedClock` shape from `InventoryRepositoryImplTest`.
**Reviewer notes:** None.

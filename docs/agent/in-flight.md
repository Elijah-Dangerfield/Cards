# In-flight

## feat(catalog): pair REACH_LEVEL_25 with Felt Veteran title

**Problem:** The level-milestone capstone `REACH_LEVEL_25` ("Felt veteran" EPIC) was the only EPIC achievement without a cosmetic complement — every other EPIC in the registry already grants a title / emote pack / card back at unlock time, so the level-25 reveal felt thin.
**Approach:** Add an `unlock_only` `title_felt_veteran` product (server migration V35), wire `cosmeticRewardFor(REACH_LEVEL_25) → CosmeticReward("title_felt_veteran", "Felt Veteran title")` on the client mirror, `"REACH_LEVEL_25" → "title_felt_veteran"` in `ClientGrantableAchievements.Default`, and the display label in `titleForProductId`. The todo's recommendation floated "a card back rather than an emote pack" — I picked a title instead because the achievement name *is* "Felt veteran" and a title is one line in `titleForProductId` versus a new `CardBackStyle` enum value + `paletteFor` branch + `PlayingCardBack` renderer branch + draw layer for the card back path. Other level milestones (`REACH_LEVEL_5`, `REACH_LEVEL_10`) stay XP-only on purpose so the V35 capstone isn't diluted by a chain-of-three.
**Reviewer notes:** Departed from the todo's "card back" recommendation in favor of a title — flag if you'd rather I revert and ship the card back path; not married to the direction. Rewrote the §A catalog-gating bullet in `docs/todo.md` to reflect the V35 pairing + my reasoning so the next worker has the context.

## feat(shop): jump to just-unlocked item from redeem snackbar

**Problem:** After a cosmetic redeem the "Unlocked! X is yours." snackbar fires and the user has to navigate Profile → My Items themselves to actually equip the thing.
**Approach:** Add an action button on the `RedeemSucceeded` snackbar that calls `router.navigate(MyItemsRoute(highlightProductId = …))`. `MyItemsRoute` now carries an optional `highlightProductId`; `MyItemsScreen` reads it, scrolls the matching row into view, and pulses an accent border on the row for ~1.8s. Action label switches between "Equip" (for `Product.ChipOffer.isEquippable = true`) and "View" (avatar / emote packs that don't have an equip toggle — the row still surfaces the "Unlocked" badge). Chose this over a generic "View" for every case so the CTA copy reads honestly per the todo's "no equip target" caveat for non-equippables.
**Reviewer notes:** Per todo, AlreadyOwned + chips-added snackbars stay action-less even though the AlreadyOwned case arguably benefits from a "View" jump too — happy to extend if you want it. Highlight pulse + scroll-to is screen-side only (no VM state), so existing `MyItemsViewModelTest` cases pass unchanged; the new behavior would need a Compose UI test harness to assert on, which doesn't exist in this module yet. The DS chose accentPrimary border at alpha 0→1→0 over a background flash to avoid clipping the row's existing surface treatment.

## refactor(ds): swap raw 22.dp literals for D850/R850 in shop + cheat sheet

**Problem:** `ShopScreen.HeroProductIcon` hand-tuned `RoundedCornerShape(22.dp)` × 2 + a `padding(22.dp, 22.dp)` literal pair, and `HandRankingsCheatSheet` repeated the same `padding(22.dp, 22.dp)` shape on `CurrentHandCard` plus a `width = 22.dp` step-indicator literal. `Dimension.D850` already maps to 22.dp in the ladder so the padding literals were drift, and the corner-shape literal had no matching `Radii` token to point at — exactly the case AGENTS.md #6 calls out ("if you reach for `RoundedCornerShape(12.dp)`… add the missing token").
**Approach:** Add `Radii.R850 = Radius(CornerSize(DimensionResource.D850.dp))` to the Radii ladder so 22.dp corner radii have a token-shaped home (matches the existing D850 → 22.dp dimension naming). Then sweep the two feature callsites: `ShopScreen.HeroProductIcon` clip + border now use `Radii.R850.shape`; `FeaturedPackHero`'s padding and the cheat sheet's `CurrentHandCard` padding both lift to `Dimension.D850`; cheat sheet's StreetProgress active-step width also lifts to `Dimension.D850`. Dropped the now-unused `RoundedCornerShape` import from ShopScreen. Did not touch the four `:libraries:ui` internals (FeatureCard / BlindMarker / PermanentBadgeMarker / ChipCoin) that also use literal 22.dp — those are DS-internal callsites and a broader sweep belongs in its own commit if we want it.
**Reviewer notes:** No visual change — every literal lined up exactly with the existing D850 = 22.dp dimension or the new R850 = D850 token. `Radii.R850` slots between R800 and R900 in the ladder; the naming follows the existing irregular ladder (D850 / D750 / D950 etc. are already non-round-100 dimensions, so R850 matches that precedent). Open if you'd rather collapse `R850` into `Radii.Card` or one of the existing tokens — willing to revert if the position is "don't extend the ladder, snap to nearest" — but the AGENTS.md rule reads in the opposite direction so I went with extension.

## feat(catalog): pair BEAT_*_10 with five signature emote packs

**Problem:** The five `BEAT_*_10` per-bot achievements (Jane / David / Gina / Steve / Mike, RARE, bot-mode) carried XP-only — they were the largest unpaired achievement cluster left in the registry, and the V34 commit footer explicitly flagged them as deferred behind a "needs five new packs shipped together to avoid an inconsistent half-rollout" gate.
**Approach:** Ship all five in one commit (V36 inspector → V37 showstopper → V38 outsmarter → V39 marathoner → V40 tamer, ordered to match the registry's bot enumeration). Each pack is themed from the winner's vantage rather than the bot's — "out-analyzed the gatekeeper" / "out-showed the showman" / "outsmarted the trickster" / "out-waited the patient one" / "tamed the chaos" — so a player owning all five reads them as a coherent set of mastery flexes rather than five bot-knockoff packs. Pack icons paired narratively with each bot's medallion (🔍 vs 🧐, 🎤 vs 😎, 💡 vs 🦊, 🦥 vs 🐢, 🦁 vs 🤡) — same conceptual family, distinct glyphs so the unlock callout doesn't show one ambiguous icon. Each migration carries the same client-wiring trio as the prior eight emote-pack rows: `EmojiPackCatalog.PackEmojis`, `cosmeticRewardFor(...)`, `ClientGrantableAchievements.Default.clientGrantable`. Glyph collisions were checked against all 12 prior packs, all 5 sibling new packs, and every active achievement medallion icon in the registry (🧠 was the natural first pick for the outsmarter pack but is a registered medallion — swapped to 💡 in the same conceptual slot). I picked the five thematic names + glyph sets myself; the todo's only direction was "per-bot signature packs" so this is a directional call worth flagging.
**Reviewer notes:** The five pack names + emoji sets are my pick — happy to swap any of them if a name reads off. The strongest cases for revision are probably "Marathoner" (could also be "Endurer" / "Stoic") and "Tamer" (could also be "Ringmaster" / "Composer"). Glyphs are easier to swap one-at-a-time than pack names. Test coverage: `EarnableCosmeticsTest`, `EmojiPackCatalogTest`, and `GrantsRoutesTest` all pin every pack individually so a rename or remap on the server vs the client mirror trips loudly. Pack icon `🤓` (inspector slot 3) sometimes renders as "nerd face" on older system fonts — checked against the existing 12 packs and none reuse it, but it's the one glyph in the new five with the most font-rendering variance; willing to swap to ⚖️ (scales) or 🔬 (microscope) if the slightly inconsistent render is a concern. The 5-pack ship-together constraint resolves with this commit, so the §A catalog-gating bullet was rewritten to reflect the new 13-pack baseline; nothing on the todo blocks on this anymore.

## refactor(onboarding): type SignInState.error as sealed SignInError

**Problem:** SignInViewModel held four raw user-facing strings on `SignInState.error: String?` — "Email or password is incorrect.", "Couldn't reach the server. Check your connection.", "That sign-in option isn't available yet.", "Sign in failed. Please try again." Same shape the §A bullet flagged for the rest of the auth VMs after the Lobby commit landed it as the template; SignIn was the next-smallest pick.
**Approach:** Same shape as the Lobby commit, applied 1:1. `SignInState.error: String?` → `SignInError?` (4 data-object variants: InvalidCredentials / NetworkError / ProviderNotEnabled / Unknown — none need a payload, all four outcomes are sealed without dynamic args). Added the four `auth_sign_in_error_*` resource keys to `:libraries:resources`. `SignInError.message()` resolver lives in `AuthScreens.kt` directly next to the existing `ErrorText` Composable and the `bannerResource` mapper for VerifyEmail — same family of resolvers, same file. Preview's hard-coded error string lifted to `SignInError.InvalidCredentials`. `SignInViewModelTest` substring asserts (`error!!.contains("incorrect")` / `contains("reach", ignoreCase = true)` / `contains("isn't available", ignoreCase = true)`) lifted to typed equality (`assertEquals(SignInError.InvalidCredentials, last.error)` etc.); added one new `submit_unknown_surfacesUnknownError` case to cover the `Unknown` variant which the original suite didn't exercise. Dropped `assertTrue` import (no longer used). Network error key stays separate from a hypothetical SignUp-side `auth_sign_up_error_network` — even though the copy will be identical, the §A bullet on `:features:profile:impl` form prefixes is the precedent ("Each form keeps its own prefix even though several strings are identical today, so a future voice-pass can tune one without dragging the other"). Rewrote the §A strings bullet again to reflect SignIn done and signal SignUp as the next-biggest worked example.
**Reviewer notes:** `SignUp` is the natural next pick if you want to keep stacking these — ~7 variants since `linkEmailIdentity` + `signUpWithEmail` share the same outcome shape (EmailAlreadyRegistered / WeakPassword / InvalidEmail / NetworkError / Unknown + the local PasswordsDontMatch guard). `WeakPassword` carries a dynamic min-length arg in the current copy ("at least %d characters") so that variant would be `data class WeakPassword(val minLength: Int)`. I deliberately scoped this commit to SignIn only — the "Sign Up" surface has more variants and bundling them risks reviewer notes getting buried.

## refactor(lobby): type LobbyState.error as a sealed LobbyError

**Problem:** The §A strings sweep status block called out `LobbyViewModel`'s `LobbyState.error: String?` as needing typed `ErrorReason` or a `suspend` resolve before it could migrate from raw String to resource — the VM was holding inline user-facing copy ("Couldn't reach the server.", "No room with code $code.", etc.) which blocks both localization and the strings-sweep close-out.
**Approach:** Lift `LobbyState.error: String?` to `LobbyState.error: LobbyError?` as a sealed interface with 15 variants (4 create-path + 7 join-path + leave + 2 connection + start-game + the 1 blank-code-validation guard). `data class` for the two variants with payload (`CreateInvalidMaxSeats(message: String)` passes the server-side wire message through verbatim since the server format may evolve before V1.x; `JoinRoomNotFound(code: String)` carries the typed code for the resource format arg). Every other variant is `data object`. Added 14 `lobby_error_*` resource keys to `:libraries:resources`. The screen-side resolver is a private `@Composable LobbyError.message(): String` extension at the bottom of `LobbyScreen.kt` — same shape as the existing `bannerResource(Banner)` mapper in `AuthScreens.kt`. Updated `LobbyViewModelTest` to assert on the sealed type identity rather than `.contains("reach")` substring checks (cleaner, doesn't drift if copy is touched) and added 4 new cases (`NotFound` carries the code arg, `InvalidMaxSeats` carries the server message verbatim, `StartGame` surfaces ComingSoon, `DismissError` clears state). Rewrote the §A strings status block to reflect this lands as the worked example for the remaining VM-side error string lifts (Onboarding / SignIn / SignUp / ClaimAccount all in the same `String? → sealed Error?` shape).
**Reviewer notes:** `CreateInvalidMaxSeats.message: String` is a deliberate hole — the server-side wire shape for the maxSeats validation isn't typed today (`RoomRepositoryImpl` extracts and forwards the message), so the variant carries the raw String through. When that wire format gets typed, this variant collapses into a `data object` like the rest. Picked the `@Composable LobbyError.message(): String` resolver shape rather than a non-Composable `LobbyError.toStringRes(): StringResource` because the `NotFound` / `InvalidMaxSeats` variants need args — `stringResource(...)` is the natural shape for that and matches `bannerResource`'s precedent. The OAuth-style `bannerResource(Banner) -> (StringResource, ColorResource)` returns a pair so the caller can do its own typography; this resolver returns just the String because the call site is a single `Text` with fixed danger-color styling. Test now uses `assertIs<LobbyError.CreateInvalidMaxSeats>` rather than `assertTrue(...contains(...))`, so dropped the now-unused `assertTrue` import.

## refactor(ds): lift 22.dp literals in libraries/ui to Dimension.D850

**Problem:** The prior `refactor(ds)` commit this cycle swapped the *feature*-side 22.dp literals (shop hero + cheat sheet) for the existing `D850` token but explicitly deferred the four `:libraries:ui` internals it spotted (FeatureCard / BlindMarker / PermanentBadgeMarker / ChipCoin) into "its own commit if we want it." This is that commit.
**Approach:** Sweep all four DS-internal callsites in one go now that the prior cycle's `R850` ladder addition is in. `FeatureCard.kt:53` `padding(horizontal = 22.dp, vertical = 22.dp)` → `Dimension.D850 × Dimension.D850`. `BlindMarker.kt:76` and `PermanentBadgeMarker.kt:36` `.size(22.dp)` → `.size(Dimension.D850)`. `ChipCoin.kt` `coinSymbolTypographyFor`'s `when` ladder had a `size <= 22.dp` branch — lifted the whole ladder (16/22/32/48 → `D700/D850/D1050/D1300`) so the typography break-points reference the same token vocabulary as the rest of the DS, not a mix of literals and tokens. No `dp` import drops — every modified file still has small-offset literals (`1.dp` borders, `52.dp` glyph block, the `18.dp/28.dp/48.dp` border thickness ladder in `ChipCoin` which is genuinely a per-size weight ramp not a spacing decision). No visual change — every swap lined up exactly with its matching dimension token. Same posture as the prior cycle's feature-side swap: AGENTS.md #6 reads "extend it when tokens don't fit," but here the token already existed and the literal was drift.
**Reviewer notes:** The `ChipCoin` border-width ladder (`18.dp -> 1.dp / 28.dp -> 2.dp / 48.dp -> 3.dp`) stayed literal — these are a coin-diameter → border-weight ramp, not a spacing decision, and there's no `BorderWidth` token family today to lift them into. If the position is "add a `BorderWidth` ladder and lift these too," I'm happy to follow up.

## feat(profile): combine claim screen with email/password form

**Problem:** The Claim-Account screen showed three CTAs (Google / Apple / Continue with email) and the email tap routed to a second screen (`SignUpRoute`) that asked for email + password — one screen too many for the locked-design "single combined sign-up" shape.
**Approach:** Collapsed both into the existing `ClaimAccountScreen`: email + password + confirm-password fields at the top, big primary "Create account" submit, "or" divider, then the gated OAuth buttons (Google / Apple `Outlined` style so the form's primary submit reads as the dominant action). The `ClaimAccountViewModel` now owns the full email path — local validation (≥6 chars, match), the constructive `awaitingClaimConfirm` dialog (same dialog SignUp has, same `auth_sign_up_claim_dialog_*` strings reused), then `linkEmailIdentity` with the same `NotAnonymous → signUpWithEmail` fallback the SignUp VM does (covers the rare case of the session rolling over mid-flow). New `ClaimAccountEvent.NavigateToVerifyEmail(email)` event maps to `router.navigate(VerifyEmailRoute(email))` at the `ProfileFeatureEntryPoint` seam — same Verify Email screen the onboarding flow uses for fresh signups, so a guest who claims by email walks the same verification path as a fresh user. OAuth path is untouched per the todo's out-of-scope (link / conflict / switch-to-existing still go through the same `linkOAuthIdentity` / `signInWithOAuth` calls). The `SignUpRoute` still exists for the OnboardingFlow's "Create account" CTA off SignInScreen — only the claim entry point stopped routing through it. Reused existing string keys (`auth_field_email_label`, `auth_field_password_label`, `auth_sign_up_confirm_password_label`, `auth_sign_up_password_helper`, `auth_sign_up_password_mismatch_helper`, `auth_sign_in_oauth_divider`, `auth_sign_up_submit_button{_progress}`, `auth_sign_up_claim_dialog_*`) — the strings are conceptually identical across the two surfaces, sharing the key keeps a future voice-pass from having to fix two places. Removed two dead string entries (`profile_claim_provider_email`, `profile_claim_coming_soon`) that the new layout doesn't need — the email form is the always-available path, no "coming soon" copy needed when OAuth isn't enabled. Kept the confirm-password field even though the spec hinted at "same shape as most modern sign-up flows" (many of which drop confirm) — for a claim flow where a typo permanently binds guest chips/XP to the wrong email, the friction is worth it; same reasoning as the existing SignUp screen's confirm field.
**Reviewer notes:** I picked `ButtonStyle.Outlined` on the OAuth buttons (vs `Filled` previously) so the email "Create account" submit reads as primary — happy to flip back if you want OAuth visually co-equal. Kept the confirm-password field; willing to drop it if you want the modern-shape single-password flow per the spec hint, but I'd want a more visible password-reveal toggle if we do (currently the field is always masked). Reused `auth_sign_up_claim_dialog_*` keys across both surfaces; if the voice for the guest-claim dialog should differ between fresh-signup and from-Profile-Claim, the keys would need to split. `ClaimAccountViewModelTest` extended with 9 new email-path cases (below-min, mismatch, confirm-dialog gating, dismiss, success → NavigateToVerifyEmail event, EmailAlreadyRegistered, WeakPassword, InvalidEmail, NotAnonymous → fallback to signUpWithEmail, NetworkError) plus the FakeAuthRepository grew `linkEmailIdentity` + `signUpWithEmail` stubs (previously `error()`-stubbed since the prior VM didn't touch them). Inline VM error strings stay raw `String` for now — the in-flight note on the §A strings sweep flags the broader `OnboardingViewModel`/`SignUpViewModel`/`ClaimAccountViewModel` VM-error-string lift as needing typed `ErrorReason` or a `suspend` resolve before they can migrate, so the new error strings I added follow the same raw-String shape as the prior ones in this file.

## refactor(onboarding): type SignUpState.error as sealed SignUpError

**Problem:** SignUpViewModel held six raw user-facing strings on `SignUpState.error: String?` — the local `Passwords don't match.` guard plus five outcome-mapped variants ("That email is already in use…", "Pick a stronger password (at least N characters).", "That email doesn't look right.", "Couldn't reach the server.", "Sign up failed."). Same raw-String shape the prior Lobby + SignIn commits this cycle flagged as the remaining VM-side error string lifts; SignUp was the next-biggest worked example.
**Approach:** Same template as the SignIn commit, applied 1:1 with one variant carrying a payload. `SignUpState.error: String?` → `SignUpError?` as a sealed interface — 5 `data object`s (PasswordsDontMatch / EmailAlreadyRegistered / InvalidEmail / NetworkError / Unknown) plus `data class WeakPassword(val minLength: Int)` so the `auth_sign_up_error_weak_password` resource's `%1$d` format arg comes from a typed field rather than a string-interpolated callsite. The `linkEmailIdentity` and `signUpWithEmail` outcome shapes collapse 1:1 onto the same six variants — both paths emit `WeakPassword(SignUpState.MIN_PASSWORD_LENGTH)`, `EmailAlreadyRegistered`, etc. — so the six resource keys cover both call sites without duplication. Six new `auth_sign_up_error_*` keys live in `:libraries:resources`. Screen-side resolver is `@Composable SignUpError.message(): String` next to the existing `SignInError.message()` in `AuthScreens.kt` — same shape, same file. Preview's hard-coded error string lifted to `SignUpError.Unknown`. `SignUpViewModelTest` substring asserts (`error!!.contains("signing in")` / `contains("stronger")` / `contains("doesn't look right")` / `contains("reach")`) lifted to typed equality (`assertEquals(SignUpError.EmailAlreadyRegistered, last.error)` / `assertEquals(SignUpError.WeakPassword(SignUpState.MIN_PASSWORD_LENGTH), last.error)` etc.); added one new `submit_unknown_surfacesUnknownError` case to cover the `Unknown` variant which the original suite didn't exercise; dropped the now-unused `assertTrue` import. Network/Unknown keys stay separate from the SignIn-side `auth_sign_in_error_network` / `_unknown` even though the copy is identical today — same precedent as the in-flight note on the §A bullet for the `:features:profile:impl` feedback/bug-report forms: each surface keeps its own prefix so a future voice-pass can tune one without dragging the other. Rewrote the §A strings bullet again to reflect SignUp done and signal ClaimAccount as the next-biggest worked example.
**Reviewer notes:** `WeakPassword.minLength` always reads `SignUpState.MIN_PASSWORD_LENGTH` today — every outcome that surfaces it currently constructs it with that constant. If the server ever wires a `min_length` field back in the WeakPassword outcome, the field on `WeakPassword` is already shaped to carry it through (and the resource format arg is already in place). Same `assertEquals` pattern as the Lobby + SignIn commits — substring matches were drifty against copy edits; typed equality holds across them. `ClaimAccountViewModel` is the natural next pick if you want to keep stacking these — the OAuth-path variants need a `data class ProviderNotEnabled(provider: OAuthProvider)` so the "{Provider} sign-in isn't available yet" line can resolve the provider name from a field, plus the `AlreadyOnAnotherAccount` outcome stashes a sibling `conflictingProvider` so the new sealed `ClaimAccountError` would coexist with that — meatier than SignIn/SignUp but still a clean lift. `OnboardingViewModel`'s `authError` is the meatiest of the four — `handleContinueAsGuest` calls `describeFailure(cause)` which builds the user-facing string from substring-matching the underlying exception message plus a debug-build `$friendly\n\nDEBUG: $raw` suffix; a sealed lift there needs to either carry the `rawMessage: String?` through as a `data class` arg (and gate the suffix at the resolver) or split the resource format into two strings. Worth a focused commit later, not stacked here. I deliberately scoped this commit to SignUp only so the reviewer notes don't bury anything.

## refactor(profile): type ClaimAccountState.error as sealed ClaimAccountError

**Problem:** ClaimAccountViewModel held nine raw user-facing strings on `ClaimAccountState.error: String?` across three internal paths (OAuth-link, ConfirmSwitchToExisting, email-claim) — the OAuth-link `${provider.label} sign-in isn't available yet` line in particular mixed business logic (which provider failed) with rendering concerns (the surrounding copy). Same raw-String shape the prior Lobby + SignIn + SignUp commits this cycle flagged; Claim was the next-biggest worked example after the SignUp commit landed.
**Approach:** Same template as the prior three commits, with one new shape choice: combining adjacent surfaces onto a shared variant when the copy is identical. `ClaimAccountState.error: String?` → `ClaimAccountError?` as a sealed interface with 10 variants — 8 `data object`s (PasswordsDontMatch / AlreadyOnAnotherAccount / NotSignedIn / NetworkError / Unknown / SwitchFailed / EmailAlreadyRegistered / InvalidEmail), plus 2 `data class`es (`ProviderNotEnabled(val provider: OAuthProvider)` carrying the provider name for the `%1$s sign-in isn't available yet.` resource format arg; `WeakPassword(val minLength: Int)` mirroring the SignUp shape). 10 new `auth_claim_error_*` resource keys in `:libraries:resources`. Screen-side resolver is `@Composable ClaimAccountError.message(): String` private to `ClaimAccountScreen.kt` (same shape as `SignInError.message()` / `SignUpError.message()` in AuthScreens.kt — kept here rather than lifted to the auth feature because Claim is a `:features:profile:impl` surface). The OAuth-link path and ConfirmSwitchToExisting path collapse onto a shared `NetworkError` (identical copy across both surfaces in the prior raw shape — "Couldn't reach the server. Check your connection.") and a shared `ProviderNotEnabled(provider)` (the only difference in the prior raw shape was a trailing "Please try again later." on the OAuth path; I picked the more informative version for the shared key — small voice unification, not a regression). The OAuth-switch path's three non-network/non-cancel branches (InvalidCredentials + EmailNotConfirmed + Unknown) collapse onto a single `SwitchFailed` variant — they all shared the "Couldn't sign in. Please try again." copy in the prior shape, and the user can't act on a finer-grained distinction here. The "Couldn't claim your account..." Unknown stays separate from the "Couldn't sign in..." SwitchFailed because those are different VM-side intents (claim path vs switch path). Conflict preview's hard-coded error string lifted to `ClaimAccountError.AlreadyOnAnotherAccount`. `ClaimAccountViewModelTest` substring asserts (`.contains("already in use")` / `.contains("Google")` / `.contains("reach")` / `.contains("stronger password")` / `.contains("doesn't look right")` / `.contains("isn't available")`) lifted to typed equality (`assertEquals(ClaimAccountError.X, last.error)`); added two new tests covering `confirmSwitch_signInProviderNotEnabled` (pins that the provider name carries through via `ProviderNotEnabled(OAuthProvider.Apple)` on the switch path too) and `confirmSwitch_signInUnknown` (pins that the InvalidCredentials/EmailNotConfirmed/Unknown branches all collapse onto `SwitchFailed`). Dropped now-unused `assertTrue` and `assertNotNull` imports. Rewrote the §A strings status block to reflect Claim done and signal `OnboardingViewModel` as the last remaining auth VM-error-string lift.
**Reviewer notes:** Three trade-offs worth flagging: (a) collapsed the OAuth-link "Please try again later." trailing line into the shared `ProviderNotEnabled(provider)` resource — small voice nudge to unify the two surfaces, willing to split back to `ProviderNotEnabledLink(provider)` / `ProviderNotEnabledSwitch(provider)` if you want them strictly distinct. (b) `SwitchFailed` collapses three SignInOutcome branches (InvalidCredentials / EmailNotConfirmed / Unknown) onto one variant because they all rendered identical copy in the prior raw shape and the user can't act on the distinction; if you want to surface a finer message for `EmailNotConfirmed` (route to VerifyEmail?), that needs a `SwitchFailedEmailNotConfirmed(email)` variant pulling the email through — happy to add it. (c) `OAuthProvider.label` stayed as a non-Composable `String` extension at the bottom of `ClaimAccountViewModel.kt` — it's read by `ClaimAccountScreen.kt`'s conflict-switch button (existing call) and by the new `ProviderNotEnabled.message()` resolver; if a future voice-pass needs the provider name localized, lift `label` to `StringResource` (same shape as the prior `BotPlayingStyle.label` lift) and resolve at the callsite. `OnboardingViewModel` remains the last VM-error-string lift; the `describeFailure(cause)` shape there genuinely warrants its own commit per the earlier reviewer note. I deliberately scoped this commit to Claim only so the reviewer notes stay focused.

## refactor(onboarding): type Onboarding VM errors as sealed Onboarding{Auth,Save}Error

**Problem:** OnboardingViewModel held the messiest raw-String error surface of the cycle's lift sequence — `authError: String?` was set by two different paths (OAuth shortcuts on Welcome + the guest-tap continue flow), and the guest-tap path called `describeFailure(cause)` which built the user-facing string by substring-matching against the underlying exception message and tacking on a debug-build `\n\nDEBUG: $raw` suffix. The `saveError: String?` field on PickIdentity also held raw copy for the two name-validation outcomes. This was the load-bearing exception left when the prior four commits this cycle (Lobby + SignIn + SignUp + Claim) closed out the rest of the VM-side error string surface.
**Approach:** Two sealed types in one VM commit because both `authError` and `saveError` live on `OnboardingState`. `OnboardingAuthError` has 7 variants — 3 `data object`s on the OAuth side (OAuthProviderNotEnabled / OAuthNetworkError / OAuthFailed, where the last collapses InvalidCredentials + EmailNotConfirmed + Unknown since they shared "Sign in failed. Please try again." in the raw shape) and 4 `data class`es on the guest side each carrying `debugDetails: String?` (AnonymousSignInDisabled / CaptchaRequired / InvalidConfig / GuestSignInFailed, where the last absorbs both NetworkError and the catch-all else from `describeFailure` since both rendered identical "Couldn't reach the server. Check your connection and try again." copy in the raw shape). `OnboardingSaveError` has 2 trivial `data object`s (DisplayNameTaken / InvalidDisplayName). 10 new `onboarding_{auth,save}_error_*` resource keys plus an `onboarding_auth_error_debug_suffix` with format arg `%1$s` for the DEBUG suffix. **The interesting architectural call** here is where the BuildInfo.isDebug branching lands: the previous shape put it inline in `describeFailure(cause)`, which made the VM's String output depend on the build type — bad for tests (they'd have to assert with-or-without the DEBUG suffix depending on the build), bad for separation of concerns (rendering decisions inside the VM). The new shape moves the BuildInfo check to the resolver: the VM's `describeGuestFailure` *always* populates `debugDetails` with `cause?.message?.take(200)`, and the `@Composable OnboardingAuthError.message()` in `OnboardingScreen.kt` reads the main resource key, then conditionally appends the `_debug_suffix` resource when `BuildInfo.isDebug && debugDetails != null`. Tests assert on the typed variant + its payload deterministically (no build-type dependency); the suffix render is a screen-side concern. `OnboardingViewModelTest`: three guest-path substring asserts (`.contains("Anonymous sign-in")` / `.contains("Providers")` / `.contains("connection")` / `.contains("anon key")` / `.contains("IdentityConfig")` / `.contains("taken")`) lifted to typed equality (`assertEquals(OnboardingAuthError.AnonymousSignInDisabled(debugDetails = "Anonymous sign-ins are disabled"), vm.state.authError)` etc.); added 4 new test cases covering `CaptchaRequired` (the previously-untested guest-path branch — every other guest-path branch had a test), `signInWithOAuth_providerNotEnabled`, `signInWithOAuth_networkError`, and `signInWithOAuth_unknownLikeOutcome` (the OAuth-path branches were previously only covered by Success / Cancelled — the three error branches had zero coverage). The hardcoded `authError` and `saveError` preview strings in `OnboardingScreen.kt` lifted to `OnboardingAuthError.GuestSignInFailed(null)` and `OnboardingSaveError.DisplayNameTaken`. Rewrote the §A strings sweep status block to reflect Onboarding done — with this commit the entire `:features:onboarding:impl` + `:features:profile:impl` (claim) + `:features:lobby:impl` VM-side error-string surface is on sealed types, closing out the VM-side lifts. What's left in the §A strings sweep is the `EditProfileState.displayNameError + state.error` raw-String holes flagged in the prior `:features:profile:impl` migration note — those are still raw `String?` from the VM, same shape the auth VMs had before this commit sequence.
**Reviewer notes:** The DEBUG-suffix-at-the-resolver pattern is new this cycle — happy to migrate the OAuth path's variants over to also carry `debugDetails: String?` if you want a consistent shape (currently the 3 OAuth `data object`s don't carry it because the prior raw shape didn't suffix-append on the OAuth path, so the OAuth variants don't have a Throwable to extract from). Strongest direction trade-off: I collapsed describeFailure's NetworkError and the else catch-all onto a single `GuestSignInFailed` since they shared copy ("Couldn't reach the server. Check your connection and try again."); if you'd rather distinguish them — even just for telemetry granularity later — I can split into `GuestNetworkError(debugDetails)` + `GuestUnknown(debugDetails)` and the resource keys map 1:1. The `assertEquals(OnboardingAuthError.AnonymousSignInDisabled(debugDetails = "..."), ...)` test pattern asserts on the raw exception message verbatim (after `.take(200)`); if the underlying exception message format ever shifts (e.g. Supabase rewords "Anonymous sign-ins are disabled"), the tests will need updating — but the same is true of the prior `.contains(...)` shape, and the typed shape catches the case where the wrong variant gets matched even if the substring would still hit. With this commit the four auth-VM error lifts are done and the §A bullet's VM-side sweep is closed; this is the natural stopping point for the cycle's strings-sweep stack — the `EditProfileState.displayNameError + state.error` holes flagged in the migration note are the next-biggest pick if you want to keep going, but they're a different shape (the `displayNameError` comes from local field validation, the `state.error` is a generic save-failure catch-all) and might warrant its own commit.

## refactor(profile): type EditProfileState.displayNameError as sealed EditProfileDisplayNameError

**Problem:** EditProfileViewModel held two raw user-facing strings on `displayNameError: String?` — "That name is taken. Try another." and "That name isn't allowed. Use letters and numbers." — the last remaining VM-side raw error-string surface flagged in the §A strings sweep migration note after the auth-VM lifts closed out earlier in this cycle.
**Approach:** Smallest-possible mechanical commit following the same template as the prior five commits. `displayNameError: String?` → `EditProfileDisplayNameError?` (sealed interface, 2 `data object` variants — `Taken` / `Invalid` — mirroring the `UpdateProfileOutcome.DisplayNameTaken` / `InvalidDisplayName` outcomes the VM maps from). Two new `profile_edit_display_name_error_{taken,invalid}` resource keys in `:libraries:resources`. Resolver is `@Composable EditProfileDisplayNameError.message(): String` private to `EditProfileScreen.kt`, sitting just above the previews block. The screen-side helper-text composition where `state.displayNameError != null -> state.displayNameError` was used as a String now reads `state.displayNameError.message()` at the resolution callsite. `EditProfileViewModelTest`'s existing `submit_nameChanged_waitsForServer_andSurfacesDisplayNameTakenInline` substring assert (`error.contains("taken", ignoreCase = true)`) lifted to typed equality (`assertEquals(EditProfileDisplayNameError.Taken, vm.state.displayNameError)`); added one new test case (`submit_nameChanged_invalidName_surfacesInvalidInline`) that mirrors the existing Taken test but with `UpdateProfileOutcome.InvalidDisplayName` — `InvalidDisplayName` previously had zero test coverage, only `DisplayNameTaken` did. The preview's hardcoded error string (`"That name is taken. Try another."`) lifted to `EditProfileDisplayNameError.Taken`. Rewrote the §A strings status block to reflect EditProfile's displayNameError done and flag the remaining `EditProfileState.error: String?` field as out-of-scope.
**Reviewer notes:** The `EditProfileState.error: String?` field is currently dead — the VM never assigns to it (only nulled on `DismissError`); the screen renders it but only the `EditProfileScreenPreview_GenericError` preview hardcodes a value. The field's comment says "Distinct from [displayNameError] so the UI can place it at the field rather than as a generic banner" — implying the intent was a generic-banner channel for non-field errors. Either it should be wired to the existing snackbar's failure messages (`toFailureMessage()` currently fires `showSnackBar(it)` for non-validation failures — that copy could land on `state.error` instead/additionally for a more prominent treatment), or it should be removed entirely as dead state. Both directions are a product decision more than an engineering one — flagged in §A as out-of-scope for the strings sweep. **Deferred:** see above. New strings always go straight to the resource — no inline. With this commit every shipping VM in the repo (Lobby + SignIn + SignUp + ClaimAccount + Onboarding + EditProfile) reads through typed sealed errors.

## feat(catalog): pair SHOW_ROYAL_FLUSH with Royalty title

**Problem:** The two LEGENDARY mystery showdown achievements (`SHOW_STRAIGHT_FLUSH`, `SHOW_ROYAL_FLUSH`) were the only rarity-EPIC-or-above entries in the registry still XP-only after V35 / V36-V40 closed out the EPIC chain. SHOW_ROYAL_FLUSH is the rarer of the two (the rarest hand in poker) and the more natural lead — the moment a player shows a royal flush is the one V1 unlock that deserves a permanent vanity flex under their name.
**Approach:** Pair `SHOW_ROYAL_FLUSH` with a new unlock-only `title_royalty` product (V41) in the same shape as V35's `title_felt_veteran` — single-row migration, `chip_offer` kind, `unlock_only = TRUE` + `is_equippable = TRUE`, sort order 770 (sits directly below `title_felt_veteran`'s 760 to preserve the unlock-only title cluster). Client wiring lands in the same commit: `cosmeticRewardFor(SHOW_ROYAL_FLUSH) → CosmeticReward("title_royalty", "Royalty title")`, `ClientGrantableAchievements.Default.clientGrantable` gains `"SHOW_ROYAL_FLUSH" → "title_royalty"`, `titleForProductId("title_royalty") → "Royalty"`. Picked "Royalty" single-word over alternatives — "Crowned" reads as a transient verb-form not a permanent flex, "Royal Flush" repeats the achievement label verbatim, "Royalty Owner" matches the verbose two-word precedent of "Pot Magnet" / "Felt Veteran" but reads redundant for this concept. The shape isn't novel — V17's `title_shark` / `title_high_roller` are also single-word distinct titles. `EarnableCosmeticsTest` gains a new `showRoyalFlush_mapsToRoyaltyTitle` case and the `achievementsWithoutCosmeticRewards_returnNull` spot-check swaps SHOW_ROYAL_FLUSH for SHOW_STRAIGHT_FLUSH (the remaining unpaired LEGENDARY mystery). `GrantsRoutesTest` gains a `defaultPolicy_grantsRoyaltyTitle_forShowRoyalFlush` case matching the per-bot pack precedent.
**Reviewer notes:** "Royalty" is my pick; happy to swap to "Crowned" / "Royal Flush" / or a longer form if it reads off. Mode resolution: SHOW_ROYAL_FLUSH carries the default `AchievementMode.EITHER` — fires on both bot and MP showdowns. Sits in `clientGrantable` (not `serverWitnessed`) because hand resolution is client-driven today; Phase 4.2 server-authoritative gameplay will migrate the entry if the server takes ownership of showdown resolution. The §A todo bullet was rewritten to reflect V41 done and signal SHOW_STRAIGHT_FLUSH as the next-natural pick (LEGENDARY mystery, same shape) — that's queued as the next commit this cycle.

## feat(catalog): pair SHOW_STRAIGHT_FLUSH with Suited Run title

**Problem:** Follow-up to the V41 pairing — `SHOW_STRAIGHT_FLUSH` was the second LEGENDARY mystery showdown still XP-only after V41 paired its rarer sibling. With this row landing, every rarity-EPIC-or-above achievement in the registry has a cosmetic complement, closing the LEGENDARY-mystery pairing pair.
**Approach:** Same shape as V41, applied 1:1. New unlock-only `title_suited_run` product (V42 migration), sort order 780 (sits directly below `title_royalty`'s 770 to preserve the unlock-only title cluster). Client wiring: `cosmeticRewardFor(SHOW_STRAIGHT_FLUSH) → CosmeticReward("title_suited_run", "Suited Run title")`, `ClientGrantableAchievements.Default.clientGrantable` gains `"SHOW_STRAIGHT_FLUSH" → "title_suited_run"`, `titleForProductId("title_suited_run") → "Suited Run"`. Picked "Suited Run" over alternatives — "Pure Run" ("pure" is generic across many concepts), "Hot Streak" (could apply to any win streak — too non-specific), "Straight Flush" / "Straight Flusher" (literal hand name — same redundancy reason V41 avoided "Royal Flush"), or "Sequence Master" (verbose, three syllables on the second word). "Suited Run" uses two poker-specific terms — "suited" exclusively means same-suit in poker, "run" means a sequence of consecutive cards in many card games — so the pair captures both halves of the straight-flush concept without repeating the literal hand name, mirroring V41's "Royalty" / "Royal Flush" split. `EarnableCosmeticsTest` gains a `showStraightFlush_mapsToSuitedRunTitle` case and the `achievementsWithoutCosmeticRewards_returnNull` spot-check swaps SHOW_STRAIGHT_FLUSH for SHOW_FULL_HOUSE (the remaining unpaired EPIC showdown). `GrantsRoutesTest` gains a matching `defaultPolicy_grantsSuitedRunTitle_forShowStraightFlush` case. §A bullet rewritten to reflect V42 done and acknowledge the EPIC showdowns (SHOW_FULL_HOUSE / SHOW_FOUR_OF_KIND) as fair-game-but-lower-priority follow-ups.
**Reviewer notes:** "Suited Run" is my pick; happy to swap if it reads off — the strongest case for revision is probably "Pure Run" (shorter, simpler) or a single word like "Pure" / "Suited" if you want symmetry with V41's single-word "Royalty". I went two-word to mirror "Felt Veteran" / "Pot Magnet" precedent and avoid the ambiguity of either "Pure" (too generic standalone) or "Suited" (reads as adjective, not a player descriptor). Same mode/grantable rationale as V41 — SHOW_STRAIGHT_FLUSH is EITHER-mode and sits in `clientGrantable`. With this commit the LEGENDARY-mystery showdown pair is complete and the §A bullet's next-natural picks (EPIC showdowns) are explicitly flagged as lower-priority than the rarer ones we just closed out.

## chore(profile): drop dead EditProfileState.error field

**Problem:** `EditProfileState.error: String?` was flagged in the prior cycle's in-flight notes as "dead — VM never assigns to it, only nulls it on DismissError; only the `EditProfileScreenPreview_GenericError` preview hardcodes a value to drive the screen-side render block." The previous worker called it "a product decision" (wire it up vs remove) and left it out of scope. Re-reading the code confirms it's truly dead engineering surface — no consumer, no producer — so removing it is unambiguous cleanup, not a product call. If a generic banner is wanted later, it'll be added then; today the field just adds noise (every `copy(...)` in the VM nulls a field that's already null).
**Approach:** Remove the dead field + every callsite. `EditProfileState.error` removed from the data class; all five `copy(..., error = null, ...)` lines in the VM (DisplayNameChanged / AvatarSelected / AvatarBackgroundColorSelected / DismissError / Submit) trimmed to drop the redundant null. `EditProfileScreen.kt` loses the `state.error?.let { Spacer + Text }` render block (the dead screen-side branch) and the `EditProfileScreenPreview_GenericError` preview function (the dead preview that only existed to exercise the dead state). The `displayNameError` field's KDoc previously referenced `[error]` for "distinct from the generic banner"; that reference goes too — there's no peer field left to be distinct from. No tests changed — none referenced the field.
**Reviewer notes:** If the eventual answer is "yes, we want a generic banner for non-validation failures on Edit Profile," the field is one `copy()` away from coming back; this commit doesn't burn that bridge. The current behavior (non-validation failures fire `showSnackBar(it)` via `appScope.launch` while the user has already navigated away from Edit Profile) stays unchanged. **Deferred:** the larger question of whether `toFailureMessage()` non-validation copy should land somewhere more prominent than a fire-and-forget snackbar is still on the table — but that's a product decision and out of scope here. Nothing added to backlog; the question is implicit in the original §A strings sweep note and doesn't need its own bullet.


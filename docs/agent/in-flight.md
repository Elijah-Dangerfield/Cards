## refactor(room): migrate PlayPokerScreen chrome strings to resources

**Problem:** The `:features:room:impl/PlayPokerScreen.kt` chrome still held four inline user-facing strings — connection-lost banner, back-icon a11y, cheat-sheet question-icon a11y, "Dealing in…" loading copy — keeping the strings-sweep §A item one screen short of finishing the play surface.
**Approach:** Lifted the four to `:libraries:resources/strings.xml` under `room_connection_lost_banner` / `room_top_bar_back_a11y` / `room_top_bar_hand_info_a11y` / `room_loading_dealing_in`, resolved via `stringResource(...)` at the existing callsites. `OpponentsRow.kt` was scanned at the same time and carries no user-facing strings beyond the "▼" chevron typography character — noted in the todo so a future cycle doesn't re-derive that.
**Reviewer notes:** None — straight one-to-one swap, build green via `./gradlew :apps:compose:assembleDebug`.

## refactor(room): migrate tutorial narration + scripted-hand copy to resources

**Problem:** The scripted tutorial (`TutorialScript`, `TutorialModel`, `TutorialNarrationStep`, `TutorialPokerScreen`) still owned the bulk of the user-facing tutorial copy as inline `String` literals — three basics narration cards × {title, body, cta}, eleven tableau coach marks × {title, body, cta}, the HandRanks hero rows, the Actions hero rows, the per-section step-counter format, and the "Skip basics" text-link button. Without lifting these, the strings sweep can't claim `:features:room` is done even after the rest of the room migrated last night.
**Approach:** Lifted `CoachMark.title/body/ctaLabel/bullets`, `TutorialSection.displayName`, and `NarrationHero.topBarLabel` from `String`/`String?` to `StringResource`/`StringResource?` so the static `TutorialScript.steps` catalog stays a non-Composable `val` — same shape as the prior `BetPreset.labelResource` / `RankingEntry.name`+`tagline` migrations earlier in the cycle. Resolved via `stringResource(...)` at the `NarrationStep` / `CoachMarkBanner` / `StepCounterPill` / `BulletList` callsites. The per-section step-counter format `"Step %1$d of %2$d · %3$s"` became `tutorial_step_counter` with positional args. Two `CoachMarkBanner` previews that used inline literals now reach into `TutorialScript.steps` for canned coach data — they were illustrative anyway and the script has equivalent narration + action-prompt examples already. Bot tutorial names (Ada / Ben / Cleo) and the human "You" displayName stay inline because §D parks bot-name localization for V1.x/V2. The tutorial-side `humanHandLabel: String?` ("Pair of aces" etc.) also stays inline since that field's downstream consumer in real gameplay is the engine-computed hand-evaluator output, and migrating the field across both paths is its own slice.
**Reviewer notes:** The `TutorialSection` enum constructor parameter changed from `String` to `StringResource` — clean if you read it as "the displayName is *the resource key*, not the resolved text." If anywhere in nightly Reviewer flows wants the resolved display name in a non-Composable context (analytics tag, log line) they'd need `getString(section.displayName)` from a suspend scope. I grepped and found no such consumer today.
**Deferred:** Tutorial leftover surfaces (`AchievementUnlockedDialog`, `TutorialLeaveDialog`, `TutorialReadyScreen`) — those carry their own dialog copy and were out of scope for this slice; logged in `docs/todo.md` as the next tutorial chunk.

## refactor(room): migrate tutorial dialog + ready-screen copy to resources

**Problem:** The three remaining tutorial surfaces — `AchievementUnlockedDialog` (also reused by future non-tutorial unlock celebrations), `TutorialLeaveDialog`, `TutorialReadyScreen` — still owned every Text node inline. Closing them out makes the tutorial family's strings sweep complete and means the only remaining `:features:room` inline copy is the structural `SeatTenure`/`TableUiState.badgeFor()` deferrals already noted in the todo.
**Approach:** Lifted twelve strings to `:libraries:resources/strings.xml` (`achievement_unlocked_dialog_*` ×3, `tutorial_leave_dialog_*` ×6, `tutorial_ready_*` ×3) and replaced each `Text(text = "…")` with `Text(text = stringResource(...))`. The leave dialog's two-vs-three-button branching keeps its `if (showBackToBasics)` shape — only the literal strings changed. `achievement.name` and `achievement.description` come from the static `Achievement` registry in `:libraries:cards` and stay inline; migrating them is part of an achievements-registry-side string lift that's well out of scope here.
**Reviewer notes:** None — straight one-to-one swap, build green via `./gradlew :apps:compose:assembleDebug`. Tutorial copy is now entirely resource-resolved end-to-end.

## refactor(room): migrate SeatTenure to resource-id shape

**Problem:** `SeatTenure.kt` owned six user-facing strings inline (singular/plural hand-count headline, human-vs-other supporting line, "Playing since {Month Year}" headline, the matching supporting line, and the 12 month names). The todo flagged it as deferred because the non-Composable `String` API had unit-test coverage on its formatters, and moving to `stringResource(...)` required either a Compose UI test runtime or a different shape.
**Approach:** Lifted the `tenureRows` function into a two-layer split that mirrors `BotPlayingStyle` / `BotDifficultyTier`: a pure `tenureSpecs(seat)` returns a sealed `TenureHeadline` family carrying `StringResource` references plus any numeric args (`HandsSingle`, `HandsMulti(count)`, `PlayingSince(monthResource, year)`); a thin `@Composable tenureRows(seat)` maps each spec into a `ListSectionItem`. Month names + `room_seat_tenure_*` keys live in `:libraries:resources/strings.xml`; the 12 calendar months use a generic `month_january`…`month_december` (+ `month_unknown` sentinel) family rather than a tenure-specific prefix, so any future Month-Year row can reuse them. `playingSinceComponents(epochMs)` and `monthResource(monthNumber)` stay non-Composable testable helpers. `SeatTenureTest` updated to assert on resource-id identity and sealed-variant equality — same pattern as `BotPlayingStyleTest`.
**Reviewer notes:** None — `./gradlew :features:room:impl:testDebugUnitTest` + `./gradlew :apps:compose:assembleDebug` both green. The only behavioral shift is that the headline-text assertions in the old test are now replaced by the sealed-variant + month-resource assertions on the pure helpers, which is the same correctness gate phrased structurally.
**Deferred:** `TableUiState.badgeFor()` — the other §A "deferred as its own slice" sibling. Same Composable-vs-pure tension, same fix likely applies; left in `docs/todo.md` for a later cycle so this commit stays one logical change. The "Playing since" headline currently passes the resolved month string as `%1$s` and the year as `%2$d` — that's `String.format`-style positional substitution, which Compose Multiplatform resolves consistently across Android + iOS today; no special handling needed.

## refactor(room): migrate SeatView.seatBadge to sealed SeatBadge

**Problem:** `TableUiState.Companion.badgeFor()` formatted three user-facing strings inline ("Lvl $level", "Bot · $label", "Bot") and stored them on `SeatView.seatBadge: String?`. The todo flagged it as the next deferred slice after `SeatTenure` — same non-Composable-vs-Composable tension, same trick applies.
**Approach:** Same shape as the `SeatTenure` migration that landed earlier this cycle. `SeatView.seatBadge` lifted from `String?` to a sealed `SeatBadge?` (`Level(level)` / `BotWithDifficulty(difficulty)` / `BotPlain`). `TableUiState.Companion.badgeFor()` returns the typed shape (still non-Composable, still pure). The renderer in `PlayerProfileSheet.kt` resolves via a new `@Composable SeatBadge.label()` extension that hits `stringResource(...)`. Bot-difficulty label stays a raw `String` because it's the wire-key from `SoloBotsPokerSessionFactory.difficultyName` (same call as the home `OptionPillRow` deferral). Resources: `room_seat_badge_level` (with %1$d), `room_seat_badge_bot_with_difficulty` (with %1$s), `room_seat_badge_bot_plain`. Visibility on `badgeFor` lifted from `private` to `internal` so the new test cases can hit it; otherwise the call site is identical.
**Reviewer notes:** None — `./gradlew :features:room:impl:testDebugUnitTest` + `./gradlew :apps:compose:assembleDebug` both green. `TableUiStateTest` extended with six new `badgeFor_*` assertions covering the four real variants plus the two null cases (empty seat, remote human in MP). The four `PlayerProfileSheet` preview helpers swap their inline `"Bot · Standard"` literals for `SeatBadge.BotWithDifficulty("Standard")` etc — same rendered text, typed source.
**Deferred:** Nothing room-side — with this commit every user-facing string in `:features:room:impl` either reads through `:libraries:resources` or stays inline only for the documented "wire-key / typography character / engine-output" reasons captured in the todo prose.

## feat(catalog): seed Tactician emote pack for CHALLENGING_10_WINS

**Problem:** §A catalog-gating todo flagged `CHALLENGING_10_WINS` as a remaining unpaired emote-pack candidate, deferred in V32/V33 commit footers as "toughness-tier theme — risk of emoji collisions with `emotes_fierce`." It was the last unpaired EPIC achievement: every other EPIC (BOT_WHISPERER, POT_5000, NO_BUST_100, TRIPLE_UP, HANDS_1000, COMEBACK_FROM_5BB, DONT_CALL_IT_COMEBACK) carries a cosmetic complement.
**Approach:** Resolved the collision risk by re-archetyping the pack — `emotes_fierce` (😤🔥💀😎) is *aggression*; the new `emotes_tactician` (♟️🦅🥷🏹, pack icon ♟️) is *patience* / *strategy* / *read-the-table*. Two distinct emotional registers around the same difficulty-tier moment so a player owning both feels them as additive, not redundant. Rejected combat-coded glyphs (⚔️🥊💪🏔️) because they tonally collapsed back into `emotes_fierce`; rejected 🧠 (already on `GOOD_FOLD_FIRST`) and 🗡️/⚔️ (the two challenging-tier achievement medallions themselves) to keep the pack-vs-achievement icon namespace clean. Followed the V32/V33 recipe end-to-end: migration V34, EmojiPackCatalog, ClientGrantableAchievements, cosmeticRewardFor, plus tests in EmojiPackCatalogTest / EarnableCosmeticsTest / GrantsRoutesTest. Sort order 410 continues the unlock-only emote-pack neighborhood (V32: 390, V33: 400). Mode stays BOTS (Challenging is a bot-difficulty knob, no MP sibling to migrate).
**Reviewer notes:** None. Build green via `./gradlew :apps:compose:assembleDebug`; tests pass via `:features:room:impl:test`, `:libraries:cards:impl:test`, and `:apps:server:test --tests 'com.dangerfield.cards.server.routes.GrantsRoutesTest'`.
**Deferred:** `BEAT_*_10` per-bot signature packs (still rejected — needs five new packs shipped together to avoid an inconsistent half-rollout — stays as a candidate in the todo footer) and `REACH_LEVEL_25` (the level capstone — the todo notes it's "a natural fit for a card back rather than an emote pack," so left for whichever cycle has the appetite for a new `CardBackStyle` variant + `PlayingCard` rendering branch).

## refactor(ui): migrate ShakeDialog strings + wire :libraries:resources into :libraries:ui

**Problem:** `ShakeDialog` (shake-to-send-feedback prompt) held four inline user-facing strings — "Send feedback?" / "Shake any time to share a bug or idea." / "Send" / "Cancel" — and `:libraries:ui` had no dependency on `:libraries:resources`, so no DS-layer Composable could read the centralized catalog. The strings sweep todo flagged `:libraries:ui` snackbar / dialog / banner copy as remaining work and noted "each module needs `implementation(projects.libraries.resources)` added to its `build.gradle.kts`."
**Approach:** Added `implementation(projects.libraries.resources)` to `libraries/ui/build.gradle.kts` (the first such wiring on the DS module — comments at the dep call out why it lives there now), then lifted the four `ShakeDialog` strings into `:libraries:resources` under a `ui_shake_dialog_*` prefix and replaced each inline `Text(text = "…")` with `stringResource(...)`. Picked `ui_*` as the resource prefix family for `:libraries:ui`-owned copy so future DS dialogs/banners/snackbars all land under a stable namespace and don't collide with feature-scoped keys.
**Reviewer notes:** `./gradlew :libraries:ui:assembleDebug :apps:compose:assembleDebug` both green. The new dep makes `:libraries:resources` transitively available to every feature module already pulling in `:libraries:ui`, which is the entire app — feature modules that still want a direct `implementation(projects.libraries.resources)` for explicit intent should keep theirs. There's no cycle risk: `:libraries:resources` only depends on `:libraries:core`, never on `:libraries:ui`.
**Deferred:** The rest of `:libraries:ui`'s user-facing copy — other dialogs / banners / snackbars / a11y labels (e.g. the `Icons.X("Close")` dismiss-button a11y label that's wired into every `Snackbar` instance). Same migration pattern applies; left as separate slices so this commit stays one logical change and the new dep is its own reviewable.

## refactor(ui): migrate LevelPill + TopBar + AchievementMedallion strings to resources

**Problem:** Three `:libraries:ui` DS components still held inline user-facing strings — `LevelPill` ("Level $level"), `TopBar` back-icon a11y ("Navigate back"), and `AchievementMedallion`'s front + back faces ("Locked" / "Earned" / "Earned · $time" / "How to earn" / "Unlocks · $cosmetic.label"). The strings-sweep todo's `:libraries:ui` rump explicitly called these out as "other DS surfaces with inline user-facing copy still need lifting" after the ShakeDialog bootstrap landed earlier in the cycle.
**Approach:** Lifted the seven strings to `:libraries:resources/strings.xml` under `ui_level_pill_label` / `ui_top_bar_back_a11y` / `ui_achievement_medallion_{locked_label,earned_label,earned_back,how_to_earn,unlocks}` and replaced each inline literal with `stringResource(...)`. Each component was already `@Composable`, so no `String` → `StringResource` schema lift was needed — the `formatEarnedAgo(it)` non-Composable formatter still returns a `String` and gets interpolated into `ui_achievement_medallion_earned_back`'s `%1$s` slot at the callsite (the same `String` arg + `%1$s` shape the prior `room_seat_tenure_playing_since` migration used). Picked these three because they're the high-leverage DS-layer surfaces with inline copy — `LevelPill` renders in every Home header, `TopBar`'s a11y label fires on every screen with a back button, and `AchievementMedallion` is the canonical achievement-card primitive reused across Stats / Home shelves / unlock celebrations.
**Reviewer notes:** `./gradlew :apps:compose:assembleDebug` green. Deliberately left `Snackbar.kt:194`'s `Icons.X("Close")` a11y label alone — the human's stashed WIP this cycle is starting on a shared `ui_close_button_a11y` key for that exact slot, so I kept clear of it to avoid a stash-pop conflict at end-of-run. `formatEarnedAgo` itself stayed inline because its six relative-time strings ("today" / "yesterday" / "Nd ago" / "Nw ago" / "Nmo ago" / "Ny ago") are emitted from a non-Composable function with `FormatEarnedAgoTest` pinning the literal outputs — migrating it cleanly needs the sealed-family / `tenureSpecs(...)` shape and a parallel test-rewrite. Both deferrals captured in the §A strings-sweep prose so a follow-up cycle has the next slice queued.
**Deferred:** `Snackbar` close-icon a11y label (deferred to human's lobby-WIP cycle; same migration shape — single `stringResource(...)` swap once their `ui_close_button_a11y` key lands). `formatEarnedAgo` six relative-time strings (needs sealed-family lift + `FormatEarnedAgoTest` rewrite — captured in §A strings-sweep prose as the next `:libraries:ui` slice).

## refactor(ui): lift formatEarnedAgo to sealed EarnedAgo + resource ids

**Problem:** The prior LevelPill/AchievementMedallion commit consciously deferred `formatEarnedAgo`'s six relative-time strings ("today" / "yesterday" / "Nd ago" / "Nw ago" / "Nmo ago" / "Ny ago") because the function is non-Composable and a tested public API (`FormatEarnedAgoTest` pinned the literal string outputs). A straight `stringResource(...)` swap wasn't viable from inside a non-Composable formatter, so this completes the slice using the proven sealed-family pattern from `tenureSpecs(...)` / `BotPlayingStyle.label`.
**Approach:** Replaced the public `fun formatEarnedAgo(earnedAtEpochMs: Long, nowEpochMs: Long): String` with `fun earnedAgo(earnedAtEpochMs: Long, nowEpochMs: Long): EarnedAgo` (pure factory returning a sealed family — `Today` / `Yesterday` / `DaysAgo(days)` / `WeeksAgo(weeks)` / `MonthsAgo(months)` / `YearsAgo(years)`) plus a thin `@Composable fun EarnedAgo.label(): String` resolver that resolves each variant via `stringResource(Res.string.ui_earned_ago_*)`. Sealed family lives in a new sibling file `EarnedAgo.kt` next to `AchievementMedallion.kt` because it's tightly coupled to the medallion's back face but the schema-vs-renderer split keeps the file's responsibility narrow. Migration recipe matches what `SeatTenure.kt` shipped earlier in the cycle. `FormatEarnedAgoTest` rewrote each `assertEquals("3d ago", ...)` into `assertEquals(EarnedAgo.DaysAgo(3), earnedAgo(...))` — same boundary cases (today / yesterday / week / month / year / future-clamp), structural-equality assertion instead of literal-text. The dropped private `formatEarnedAgo(Long): String` overload was inlined into the medallion's back face as `earnedAgo(it, kotlin.time.Clock.System.now().toEpochMilliseconds()).label()` — same clock + same `String` arg into the existing `ui_achievement_medallion_earned_back` slot. **Source:** picked up directly from §A strings-sweep prose (where the prior commit explicitly queued it as "the next `:libraries:ui` slice"), not worker-hydrated.
**Reviewer notes:** `./gradlew :apps:compose:assembleDebug :features:progression:impl:testDebugUnitTest` both green. Test file's class name stays `FormatEarnedAgoTest` despite asserting on `earnedAgo` — kept the historical name so `git log --follow` on the file traces back through the rename history cleanly. Renaming the class to `EarnedAgoTest` would be cosmetic-only and break that trail. The `EarnedAgo` sealed class uses `data object Today` / `data object Yesterday` not plain `object` so structural-equality `assertEquals(...)` works without a hand-rolled `equals` — same pattern the room-side `TenureHeadline` sealed family uses. With this commit every user-facing string on `AchievementMedallion`'s back face resolves through `:libraries:resources`; only the `Snackbar` close-icon a11y label remains in the `:libraries:ui` strings rump (still deferred to the human's lobby-WIP slice).
**Deferred:** None — this completes the AchievementMedallion strings story.

# In-flight (worker handoff log)

## feat(config): JsonConfigValue<T> for structured (non-scalar) config

**Problem:** The config system only resolved flat dotted-path scalars (`FlagConfigValue`/`IntConfigValue`/…); variable-length structured data (the level ladder + reward table, future loot tables) couldn't be flattened into fixed scalar paths.
**Approach:** Added `JsonConfigValue<T>` in `:libraries:config` — resolves one path to its parsed config subtree, converts it back to a kotlinx `JsonElement` (`toJsonElement` inverse of the parse), and `decodeFromJsonElement` into a `@Serializable` model via a constructor-supplied `KSerializer<T>`. Decode failure or missing path falls back to the bundled `default` (soft-fail, so a malformed server value never bricks the client). Chose the `KSerializer` constructor param over reified `serializer()` so the base stays a plain abstract class with no inline/reified plumbing. Hidden from the QA dashboard by default (a JSON blob can't round-trip the per-key toggles, per the todo note). Pulled in `kotlinx-serialization-json` (runtime only — no compiler plugin needed for the base since it declares no `@Serializable` types).
**Reviewer notes:** Tests live in `:libraries:config:impl` commonTest (where the serialization plugin is already on) since `:libraries:config` itself has no test source set. Covered: nested-object decode, array decode, missing-path/empty fallback, undecodable-payload soft-fall-back, unknown-key tolerance, model-default for omitted optional field. The level-ladder todo's "Depends on: the JsonConfigValue config-lib extension" is now satisfied — left that item's wording untouched (curation surface).

## feat(server): persist server-witnessed finished-hand count

**Problem:** MP achievement grants want to gate on a real hand count, but the server didn't expose a per-user `hands_finished` count — even though it now runs the authoritative hand loop.
**Approach:** Sliced the "MP achievement grants — server-witnessed hand-count signal" todo to its foundational half: the server now *witnesses* and persists the count. New append-only `hand_finished_events` ledger (`V56`, mirrors the `xp_events` pattern: `(user_id, idempotency_key)` PK is the dedup boundary, key shape `<sessionId>:<handNumber>:<userId>`). `GameSession` gained an `onHandFinished` callback that fires once on the transition into `BettingRound.Complete` with the human seats (bots excluded); `DefaultGameSessionRegistry` wires it to the new `HandsFinishedRepository`, parsing seat userIds to UUIDs and skipping non-UUIDs defensively. Counter writes are best-effort (`Catching`) so a DB hiccup can't break gameplay. Deliberately did **not** add an HTTP endpoint (no client consumer — the client tracks its own local count; server-side gating will read the repo internally) nor a summary row (gating reads are infrequent, so `COUNT(*)` over the PK prefix is enough). Left the actual MP-achievement *gating* (server-side evaluation + the MP-achievement→product mapping those ids lack) as the remaining todo slice.
**Reviewer notes:** Two judgement calls worth a glance: (1) the registry's `handsFinishedRepository` constructor param is **last + defaulted to `NoOpHandsFinishedRepository`** so the ~17 existing positional/named test constructions stay valid — DI always resolves the real binding into it, so the default is dead code on the wired path (same shape as the existing `NoOpSessionSnapshotStore`). (2) hand-finished fires from `GameSession.applyIntent`'s completion transition only; it can't double-fire (post-`Complete` intents are rejected) and `hydrate` (snapshot replay) doesn't fire it — plus the idempotency key dedups a replay anyway. Tests: `GameSessionTest` (callback fires once / excludes bots / not on a non-completing call), `GameSessionRegistryIntegrationTest` (real-UUID counting + per-hand keying via `InMemoryHandsFinishedRepository`), `PostgresHandsFinishedRepositoryTest` (DB idempotency / per-user scoping / delete). Full `:apps:server:test` green. `deleteAllForUser` wired into the `DELETE /v1/me` cascade for parity (FK cascade already covers the auth-delete path).

## feat(progression): re-hydrate recent-XP feed across switch / reinstall

**Problem:** The recent-XP detail-sheet feed reads the local `xp_events` rows, which are wiped on account switch / reinstall and never re-hydrated — after a switch the total was right but the "recent XP" list was empty until new hands landed.
**Approach:** Extended the existing `POST /v1/me/progression/sync` response with `recentEvents` (server's `recentEvents(userId, 50)`, newest-first) rather than adding a separate `GET /v1/me/progression/events` — one fewer round-trip and the sync already fires on exactly the re-hydrate triggers (`onUserChanged` / `onAccountClaimed` / foreground). Server only populates it on a **pure-hydrate sync** (the request carried no events): a steady-state sync still holds those rows locally, so re-shipping them would just bloat the hot path. Client inserts any echoed key it doesn't already hold, marked `synced = true`. Dedup is mandatory — the local `xp_events` table has no unique index on the idempotency key, so a blind re-insert would duplicate the feed; added `XpEventDao.existingKeys(keys)` for the filter.
**Reviewer notes:** The server's `xp_events` table doesn't store the free-text `description` (a client-only achievement-feed nicety), so re-hydrated achievement rows fall back to the source-based label in the feed — acceptable, the label degrades from "Achievement unlocked · {name}" to the generic source line only for rows recovered after a wipe. Tests: `ProgressionRoutesTest` (new — echoes on event-less sync, stays empty + doesn't even query `recentEvents` on a steady-state sync, 401 gate); `ProgressionRepositoryImplSyncTest` (re-hydrate seeds empty ledger as synced; already-held key not re-inserted). `:libraries:cards:impl` + `:apps:server` test slices green. Also verified `:apps:compose:assembleDebug`.
**Deferred (evaluated this cycle, not picked — reviewer/human triage):** Three meatier items I scoped out but did not ship, each for a confidence reason rather than a nibble:
  - **Level-up cosmetic reward kind (§A Stats & progression):** larger than the todo implies. Level-up cosmetics aren't tied to an achievement id, but the only cosmetic-grant endpoint today (`POST /v1/me/grants/achievement/{id}`) is keyed by achievement id and resolves the product server-side. Shipping it needs (a) a **new** allowlisted `POST /v1/me/grants/cosmetic/{productId}` (public API contract) with a server-side allowlist mirroring `ClientGrantableAchievements` so a client can't self-grant arbitrary cosmetics, (b) likely a catalog migration if the gifted product isn't already seeded, and (c) a product call on *which* cosmetic at *which* level (the todo itself flags the reward table as the human's tuning surface). Two flagged-risk surfaces (API contract + migration) + a content decision → left for human direction. The display half (`LevelReward.Cosmetic` + `HomeScreen.toDisplay` + `LevelUpReward`) is trivial once the grant path + product are decided.
  - **Per-turn time limit in MP (§B3, P1):** feasible — `RoomSettings.turnTimerSeconds` exists, `GameSession` has the mutex + the post-engine mutate seam, and auto-action could reuse the engine (Check-if-legal-else-Fold). Server-side enforcement is a bounded, unit-testable slice (schedule a generation-stamped timeout on a session scope; on expiry re-check actingSeat/hand under the mutex and auto-act). I deferred it because the full acceptance also wants the deadline **surfaced** to the table, which means adding a `turnDeadline` field to the shared `GameState` wire type (ripples through `scrubbedFor` + snapshot round-trip) and a countdown UI that genuinely needs Studio/playtest to tune — and a wrong auto-action on the load-bearing MP loop is exactly what the "bulletproof MP" top priority is guarding. Recommend the human take this with a playtest in the loop, or a future worker ship just the server-enforcement slice (no wire change) and rewrite the todo to leave the countdown.
  - **Spectator role (§B4, P2):** blocked on a real prerequisite, not judgment. The socket gate requires room membership and `applyIntent`/`requestNextHand` already reject non-seated actors, so the read-only mechanics are mostly there — but B4 says "friend rooms stay closed to non-members," which presumes a public-vs-friend room distinction. No room-visibility concept exists in the `Room` model (every room is code/friend-based); public rooms are unbuilt P0 friend-graph work. Without it there's no basis to decide who may spectate. Skip until public rooms land.

## feat(progression): resolve the level reward table from app-config

**Problem:** The `level → rewards` table (`LevelRewardTable` in `LevelReward.kt`) was a compile-time client constant — retuning reward generosity, the most-retuned number in a live progression game, needed a release.
**Approach:** Sliced the "level ladder + reward table → app-config" todo to its reward-table half (the higher-value, lower-risk part — two DI'd consumers, no pure-function ripple). New `ProgressionConfig` interface (`:libraries:cards`, grouped-interface shape per the decision doc, modelled on `IdentityConfig`) backed by `LevelRewardsConfigValue : JsonConfigValue<LevelRewardsConfig>` at `progression.levelRewards`, with the old table as the bundled `DefaultLevelRewards`. `LevelUpRewardGranter` (grant) and `HomeViewModel` (celebration reveal) both read `ProgressionConfig.rewardsForLevel` so they can't drift from each other or a server retune. Chose a flat per-level entry list (`{level, chips?, xpBoostMs?}`) over a string-keyed map / polymorphic array because both fight plain JSON — rationale + wire shape logged in `decisions.md` 2026-06-17. Removed the top-level `LevelRewardTable` / `rewardsForLevel`; rewrote the todo to leave the ladder/curve migration + the server-side confirm/void reconcile.
**Reviewer notes:** The XP-*curve* (`Level.kt` `N²×100`) deliberately did **not** move this cycle — its top-level `levelProgressFor`/`xpToLevelUpFrom` are called from ~10 sites incl. Composables/previews, so config-driving it without display↔grant divergence is its own multi-file refactor (left as todo part (a)). `LevelRewardsConfigValue` is hidden from the QA dashboard (`JsonConfigValue` default — a JSON blob can't round-trip the per-key toggles). Tests: `LevelRewardsConfigTest` (default table matches the shipped economy exactly + entry mapping), `DefaultProgressionConfigTest` (config override retunes the table end-to-end through a real `AppConfigMap`; missing path → bundled default), updated `LevelUpRewardGranterTest` + `HomeViewModelTest` with a `ProgressionConfig` fake. `:apps:compose:assembleDebug` green (DI graph resolves the new binding + multibinding); `:libraries:cards`, `:libraries:cards:impl`, `:features:home:impl` debug unit tests green.
**Deferred:** The ladder/curve → config migration and the server-side reconcile remain as the rewritten todo's parts (a)/(b) — reviewer please leave them for a dedicated cycle (the curve ripple wants care; the reconcile is anti-drift, not a correctness gate, since grants are already offline-idempotent). The **banned/suspended 403 enforcement** P0 was evaluated and **not** picked: the "minimum slice" still needs a ban data source that genuinely doesn't exist (the JWT auth plugin reads no DB; `auth.users.banned_until` has no server repo/migration), and the client refresh path collapses 401/403 to `AuthRejected` and discards the body, so distinguishing banned-from-expired means capturing the 403 body in the auth hot path — both a schema/data-source decision and a risky load-bearing-auth change. Recommend the human picks the ban data source (JWT custom claim vs. moderation table) before a worker takes it.

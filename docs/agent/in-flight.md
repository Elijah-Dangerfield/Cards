# In-flight log — current cycle

Workers append blocks per commit. Reviewer reads when writing the PR and deletes the file before the squash-merge.

## docs(todo): drop shipped Sentry wiring item

**Problem:** `docs/todo.md` §C Observability still tracks "Wire Sentry — single project, platform-tagged" as a `[P1]` action item, but Sentry is fully wired on both surfaces.
**Approach:** Removed the bullet. Verified the wiring on both surfaces: client init at [AppTelemetry.kt:73](../../libraries/cards/impl/src/commonMain/kotlin/com/cards/libraries/cards/impl/AppTelemetry.kt) (`Sentry.init` via `io.sentry.kotlin.multiplatform`, options.release + platform/build_type tags via `setExtra`, SentryLogTree planted), server init at [Sentry.kt:39](../../apps/server/src/main/kotlin/com/cards/server/plugins/Sentry.kt) (Sentry JVM, options.environment + release set, uncaught-exception handler enabled), versions catalog declares `sentryKmp = "0.26.0"` at [libs.versions.toml](../../gradle/libs.versions.toml). Implementation went per-platform DSN rather than the bullet's "single project" recommendation, but the acceptance criteria (platform-tagged exceptions on each surface) are met by the current wiring. The 2026-05-29 hydrator cycle's reconcile commit had landed the same removal; reset wiped it, so this re-lands it.
**Reviewer notes:** None — pure docs delete.

## refactor(ui): relocate FeatureCardAccents to system/color

**Problem:** AGENTS.md flags raw `Color(0xFF…)` literals outside `:libraries:ui/system/color/` as a DS-drift anti-pattern. `FeatureCardAccents` (`Green`/`Blue`/`Magenta`/`Gold` — used by Home's CTA tiles and the tutorial banner) was a top-level `object` declared inside [`FeatureCard.kt`](../../libraries/ui/src/commonMain/kotlin/com/cards/libraries/ui/components/FeatureCard.kt), in `:libraries:ui/components/`.
**Approach:** Moved the object into its own file at [`FeatureCardAccents.kt`](../../libraries/ui/src/commonMain/kotlin/com/cards/libraries/ui/system/color/FeatureCardAccents.kt) under `:libraries:ui/system/color/`, alongside [`PokerPalette`](../../libraries/ui/src/commonMain/kotlin/com/cards/libraries/ui/system/color/PokerPalette.kt). Hex literals unchanged; updated the three callers' imports (`HomeCtaCard.kt`, `HomeScreen.kt`, `TutorialBanner.kt`) and dropped the old declaration from `FeatureCard.kt`. Chose "new file" over "add to PokerPalette" because the accents are not poker-game-specific and PokerPalette's docstring scopes it to "physical poker objects" — wrong neighborhood. Source-only refactor; no rendered pixels change.
**Reviewer notes:** None.
**Source:** worker-hydrated this cycle (not human-curated). Lane D — pattern flagged by a prior hydrator run wiped during the start-of-cycle reset; re-derived from the same AGENTS.md rule.

## refactor(ui): move achievement rarity raw colors into system/color palette

**Problem:** Same AGENTS.md rule. [`AchievementRarityColors.kt`](../../libraries/ui/src/commonMain/kotlin/com/cards/libraries/ui/components/achievement/AchievementRarityColors.kt) (the `AchievementRarity.toAccentColor()` extension) hard-coded three raw `Color(0xFF…)` literals inside `:libraries:ui/components/achievement/`, with EPIC already routing through `PokerPalette.ChipGold`.
**Approach:** Added [`AchievementRarityPalette`](../../libraries/ui/src/commonMain/kotlin/com/cards/libraries/ui/system/color/AchievementRarityPalette.kt) under `:libraries:ui/system/color/` with `Common`/`Rare`/`Epic`/`Legendary` entries (Epic aliases `PokerPalette.ChipGold` so the chip-gold + Epic-glow single-source-of-truth is preserved). Rewrote `toAccentColor()` to map through the palette — same hex values, one indirection layer that satisfies the "raw literals live under system/color/" rule. Chose a sibling palette object (rather than appending to `PokerPalette`) because rarity colors aren't a "physical poker object" — keeping the palettes scoped by domain matches PokerPalette's docstring intent. Extension function stays in `components/achievement/` so the `AchievementRarity` ↔ `Color` adapter sits next to the components that consume it.
**Reviewer notes:** None.
**Source:** worker-hydrated this cycle (not human-curated). Same lane / same source as the prior block.

## test(config): pin MapOverrides.applyOverrides path merge

**Problem:** `MapOverrides.applyOverrides` (the `internal` extension `OfflineFirstAppConfigRepository` calls on every cache read at [`OfflineFirstAppConfigRepository.kt:83`](../../libraries/config/impl/src/commonMain/kotlin/com/cards/libraries/config/impl/repository/OfflineFirstAppConfigRepository.kt) to thread QA-menu overrides into the resolved server config) had no direct test. The repo test exercises the wider pipeline but never asserts the merge shape directly — a regression in the tail-recursive `setValueRecursive` path-builder would silently corrupt every `ConfiguredValue<T>` resolution. The interesting corner is the deliberate copy-on-write of inner maps in [`MapOverrides.kt:44`](../../libraries/config/impl/src/commonMain/kotlin/com/cards/libraries/config/impl/MapOverrides.kt) — JSON-deserialized child maps come back read-only on Kotlin/Native, so a naive `put` would crash mid-override.
**Approach:** Added [`MapOverridesTest`](../../libraries/config/impl/src/commonTest/kotlin/com/cards/libraries/config/impl/MapOverridesTest.kt) (7 cases) co-located with the impl, same package so `internal` symbols are reachable. Pins (1) empty-overrides returns the receiver unchanged (`assertSame`), (2) single top-level override applies and unrelated siblings survive, (3) nested dot-path into an empty parent builds the whole branch chain, (4) nested override into an existing nested map preserves siblings at every level, (5) the K/Native read-only-inner-map corner — passing a `Map.of`-shape read-only inner map doesn't throw and *the receiver's inner map remains untouched* (defensive against an in-place mutation regression), (6) last-write-wins when two overrides share a path, (7) multi-branch overrides all land in the same pass. Verified locally via `:libraries:config:impl:testDebugUnitTest` — 7/7 pass.
**Reviewer notes:** The K/Native edge case isn't perfectly reproducible from JVM-side test code (we can't materialize a true Native-read-only map cross-target), so the test stands in with `mapOf(...)` and asserts the receiver isn't mutated rather than asserting the put-on-read-only would have thrown. That's the contract worth pinning anyway — "the merge doesn't mutate inputs" — and it's the property the K/Native crash bug would have violated.
**Source:** worker-hydrated this cycle (not human-curated). Lane A — verifiable gap: `applyOverrides` is internal, no test mentions it, and the K/Native copy-on-write is exactly the kind of subtle invariant that drifts silently.

## test(cards): pin AppLifecycleTracker session-end stamping

**Problem:** `AppLifecycleTracker` is the single source of truth for `AppData.lastSessionEndedAt`, which `AppData.isFirstEverSession()` reads to gate the starter-grant welcome dialog (`hasSeenStarterWelcome` gating) and any future "you've been gone a while" surfaces. The repository had no paired `*Test.kt` — sibling impls in the same module (`AppEventDispatcher`, `ChipsRepositoryImpl`, `CachedInstallIdProvider`, `EquipmentRepositoryImpl`, …) all do.
**Approach:** Added [`AppLifecycleTrackerTest`](../../libraries/cards/impl/src/commonTest/kotlin/com/cards/libraries/cards/impl/AppLifecycleTrackerTest.kt) (3 cases) using the same `FakeAppCache` shape `CachedInstallIdProviderTest` already uses, paired with a `FixedClock` / `MutableClock` test-double for the injected `Clock`. Pins (1) one `onBackground` invocation writes `clock.now().toEpochMilliseconds()` to `lastSessionEndedAt` and flips `isFirstEverSession()` off, (2) subsequent invocations overwrite (last wins, not first wins) by advancing the mutable clock between calls, (3) the write goes through `AppCache.update { it.copy(...) }` so unrelated `AppData` fields round-trip — uses `feedbacksGiven = 7` as the sentinel so a future refactor that accidentally calls `set(AppData(...))` instead of `update {}` gets caught. Verified locally via `:libraries:cards:impl:testDebugUnitTest` — all 3 pass.
**Reviewer notes:** Test exercises only the `onBackground` listener path (the only one the impl overrides); the inherited no-op `AppEventListener` defaults aren't pinned. If we later move app-event side effects from the dispatcher into per-listener `onSignedOut` / `onForeground` overrides on this tracker, those need their own coverage.
**Source:** worker-hydrated this cycle (not human-curated). Lane A — verifiable gap (no `AppLifecycleTrackerTest.kt` existed; siblings all have one), high signal because the cached stamp is load-bearing for first-session gating.

## refactor(ui): promote SectionHeader to :libraries:ui

**Problem:** `:libraries:ui`-promotion sweep (P2 in §A) explicitly flagged the home-local `SectionHeader` ("the 'section header with see-all' pattern — should likely promote to `:libraries:ui` and verify cross-feature usage matches"). Internal at [`features/home/impl/.../SectionHeader.kt`](../../features/home/impl/src/commonMain/kotlin/com/cards/features/home/impl/SectionHeader.kt), used by `HomeScreen` ("Take a seat") plus the three home shelves (`FriendsStrip`, `RecentAchievementsStrip`, `RecentlyPlayedWithStrip`).
**Approach:** Lifted the composable verbatim — same `title` + `trailingLabel` + `onClick` API, same `H800` / `B500` typography, same `Row + SpaceBetween` skeleton, same two previews — to `:libraries:ui/components/header/SectionHeader.kt` alongside `TopBar`. Made `public` (was `internal`) and dropped the docstring's "lift to `:libraries:ui` if Shop / Profile / etc. want the same shape" note (no longer accurate). Updated the four home callsites (`HomeScreen`, `FriendsStrip`, `RecentAchievementsStrip`, `RecentlyPlayedWithStrip`) to import from the new package; deleted the feature-local file. Verified `:features:home:impl` + `:libraries:ui` both compile on `iosSimulatorArm64`.

Cross-feature audit: shop's local `private fun SectionHeader(title, subtitle: String?)` at [`ShopScreen.kt:295`](../../features/shop/impl/src/commonMain/kotlin/com/cards/features/shop/impl/ShopScreen.kt) is a **different shape** — title + optional second-line subtitle in a `Column`, `H700` typography, no trailing label or tap target. Intentionally NOT folded into the new DS primitive: merging would either require an API that supports both layouts (Row-with-trailing OR Column-with-subtitle) or visually re-tuning shop's section headers to `H800` + the trailing pattern. Both are real design calls the worker shouldn't quietly make. Reviewer can triage whether shop's variant graduates to a sibling DS primitive (e.g. `SectionHeader.Stacked(title, subtitle)` overload), or stays bespoke.

Todo: updated `[P2]` DS-sweep bullet in §A to mark the SectionHeader promotion done and reflect that the achievement-tile shape and hand-result rows are what's left on the survey.
**Reviewer notes:** None — pure refactor, no behavior change, no rendered pixels move on home. The "verify cross-feature usage matches" half of the original todo intentionally short-circuited to "shop's variant is a different shape — defer the merge" rather than forcing a consolidation in this commit.
**Source:** worker-hydrated this cycle from the explicit P2 hint in `docs/todo.md` (not human-curated as a standalone todo). Lane D — mechanical lift, revert is cheap.

## test(cards): pin ChipsRepositoryImpl local write path

**Problem:** [`ChipsRepositoryImplSyncTest`](../../libraries/cards/impl/src/commonTest/kotlin/com/cards/libraries/cards/impl/ChipsRepositoryImplSyncTest.kt) covers the server round-trip thoroughly but the offline write path (`addChips` / `subtractChips` / `setBalance` / `deleteAll` plus their `require()` guards and the optimistic-debit contract) has no coverage. Sibling repos in the same module (`InventoryRepositoryImplTest`, `EquipmentRepositoryImplTest`, `UserMessageRepositoryImplTest`) all pair the sync test with a local-write test; chips was the odd one out.
**Approach:** Added [`ChipsRepositoryImplTest`](../../libraries/cards/impl/src/commonTest/kotlin/com/cards/libraries/cards/impl/ChipsRepositoryImplTest.kt) (17 cases) using the same `FakeChipsDao` / `FakeWalletEventDao` shape as the sync test, but with `FakeChipsDao(initial: ChipsEntity?)` so cases can start with a missing row and exercise the `insertIfMissing → applyDelta` ordering. Pins: write-path happy-cases on add + subtract; the require() guards rejecting zero / negative amounts; idempotency-key supplied vs generated (UUID v4 shape); the optimistic-debit contract that allows the local balance to go negative (the server's `InsufficientChips` outcome reconciles via `setBalance`); `setBalance` seeds-vs-deltas-vs-no-op paths; `deleteAll` wipes both tables. Also pinned one subtle behavior — duplicate `idempotencyKey` calls dedup the wallet event row (Room `IGNORE`) but still double-apply the local delta — with a comment explaining why pinning it here makes future change deliberate.
**Reviewer notes:** The duplicate-key test (`addChips_withDuplicateIdempotencyKey_dropsTheSecondEvent_butStillAppliesDelta`) documents *current* behavior, not desired behavior. In production the corner is unreachable (callers don't re-issue a write with the same key — sync is the retry surface) but the local-side mismatch is a latent foot-gun. Not fixing this cycle; the fix is a one-line check before `applyDelta` and is worth a separate, intentional commit.
**Deferred:** Guard `applyDeltaInternal` against re-applying a delta whose idempotency key already exists in `walletEventDao` — flagged inline in the duplicate-key test's comment, no backlog entry yet (reviewer please triage whether this rises to a real fix or stays a documented sharp edge).
**Source:** worker-hydrated this cycle (not human-curated). Lane A — the gap is verifiably real (no `ChipsRepositoryImplTest.kt` existed; siblings all have one).

## test(billing): pin BillingAvailabilityImpl cache + connect + single-flight

**Problem:** [`BillingAvailabilityImpl`](../../libraries/billing/impl/src/commonMain/kotlin/com/cards/libraries/billing/impl/BillingAvailabilityImpl.kt) has no test despite being the synchronous-snapshot cache the products-repository's catalog reconciliation reads on every emit. Three non-obvious contracts ride on this class: (1) the cached product map must NOT get wiped on a transient `NotConnected` / `Failed` query (docstring explicitly: "a flaky store doesn't blow away the previous successful product map"); (2) the lazy-connect only fires when the underlying client is `Disconnected`, never re-fires on `Connected`; (3) the `mutex.withLock` is the only thing preventing a double-query when shop-mount + catalog-refresh race.
**Approach:** Added [`BillingAvailabilityImplTest`](../../libraries/billing/impl/src/commonTest/kotlin/com/cards/libraries/billing/impl/BillingAvailabilityImplTest.kt) (7 cases) using three small in-file `BillingClient` doubles (`RecordingBillingClient` for connect/query call-tracking, `StagedBillingClient` for response staging across multiple refreshes, `SuspendingBillingClient` for the gated single-flight test). Pins: (1) `refresh(emptySet())` returns `Success(emptyMap())` *and* doesn't touch the client at all (no connect, no query) — short-circuits before the mutex; (2) Disconnected client → `connect()` called once before `queryProducts`; (3) Connected client → `connect()` skipped; (4) Successful query updates both `products` flow and `snapshot()`; (5) `NotConnected` from a transient call preserves prior cached map; (6) `Failed` from a transient call preserves prior cached map; (7) concurrent refreshes serialize via the mutex — peak in-flight queries is 1, never 2. Verified locally via `:libraries:billing:impl:testDebugUnitTest` — 7/7 pass.
**Reviewer notes:** The single-flight test (7) uses a `CompletableDeferred` gate inside the fake's `queryProducts` to hold the first caller in-flight while the second arrives, then asserts `maxConcurrent == 1`. That pins the property the mutex actually protects (peak concurrency = 1) rather than asserting on serialization order, which is implementation-detail. The peak-concurrency measurement is post-hoc (we read `maxConcurrent` after both `await()`s return), so this won't catch a regression that allows concurrent entries followed by a serialized exit — but that failure mode doesn't exist with `mutex.withLock`.
**Source:** worker-hydrated this cycle (not human-curated). Lane A — verifiable gap: `BillingAvailabilityImpl` has no test, the impl module has no other tests at all (`commonTest` directory was empty before this commit), and the three load-bearing contracts above are exactly the kind of "next refactor breaks them silently" surface tests exist to pin.

## refactor(ui): promote celebration-tint ramp to DS rarity palette

**Problem:** Same AGENTS.md raw-`Color(...).copy(alpha = X)` drift the earlier two refactor commits this cycle (`FeatureCardAccents`, `AchievementRarityPalette`) cleaned up. The bot-mode achievement celebration sheet at [`AchievementCelebrationSheet.kt:305`](../../features/room/impl/src/commonMain/kotlin/com/cards/features/room/impl/AchievementCelebrationSheet.kt) had a private `rarityAccent(rarity)` mapping each rarity to a different alpha of `PokerPalette.ChipGold` — a deliberate "more rare = louder gold wash" ramp baked into the celebration's `CelebrationCard` background, but living inside a feature module and unreachable from any other surface that might want the same celebration feel.
**Approach:** Added an `AchievementRarity.toCelebrationTint()` extension to [`AchievementRarityColors.kt`](../../libraries/ui/src/commonMain/kotlin/com/cards/libraries/ui/components/achievement/AchievementRarityColors.kt) as a sibling to the existing `toAccentColor()`. Same four-rarity / four-alpha mapping verbatim (0.12 / 0.18 / 0.24 / 0.30 over `PokerPalette.ChipGold`); kdoc spells out the deliberate split — `toAccentColor()` picks the rarity's *identity* color, `toCelebrationTint()` is the "how loud should the celebration moment feel" knob. Updated `AchievementCelebrationSheet` to import + call the extension, deleted the local `rarityAccent` function. Kept the rendered tint identical (verified the four hex+alpha pairs match 1-for-1); future MP-mode celebrations or any other surface wanting the same ramp can call the same extension without re-deriving.
**Reviewer notes:** Chose `toCelebrationTint()` as a second extension over folding the ramp into `AchievementRarityPalette` proper because the ramp is rendering-intent ("how loud is this moment") rather than an identity-bearing palette entry — keeping it as a `rarity → tint` adapter next to `toAccentColor()` makes the parallel obvious. `PokerPalette` import + the `goldBubble`/`gold-text` `ColorResource.FromColor(PokerPalette.ChipGold, ...)` callsites in the same file are deliberately untouched — those reference the source-of-truth `PokerPalette.ChipGold` directly, which is already DS-correct (PokerPalette is the `:libraries:ui/system/color/` palette object).
**Source:** worker-hydrated this cycle (not human-curated). Lane D — pattern flagged by the same AGENTS.md rule that drove the earlier two refactor commits this cycle; mechanical lift, revert is cheap.

## test(navigation): pin DeepLinkBridgeImpl cold-launch buffering

**Problem:** [`DeepLinkBridgeImpl`](../../libraries/navigation/impl/src/commonMain/kotlin/com/cards/libraries/navigation/impl/DeepLinkBridgeImpl.kt) is the only thing standing between a cold-launch deep link (platform `onOpenURL` / `onNewIntent` fires before App composable mounts and subscribes) and a silently-lost URL. The `replay = 1` + `extraBufferCapacity = 4` + `DROP_OLDEST` triplet IS the entire load-bearing behavior of this class — and there was no test, so a "let me just bump replay to N" or "extraBufferCapacity isn't needed" change would silently regress first-launch deep linking. `libraries/navigation/impl` had no `commonTest` dir at all.
**Approach:** Added [`DeepLinkBridgeImplTest`](../../libraries/navigation/impl/src/commonTest/kotlin/com/cards/libraries/navigation/impl/DeepLinkBridgeImplTest.kt) (4 cases) directly against the public `DeepLinkBridge` contract (no fakes — the impl IS the unit). Pins: (1) emit-before-subscribe → late subscriber receives the URL via the replay slot (the cold-launch case the class exists for); (2) emit-after-subscribe → URL delivered immediately (warm case, notification tap); (3) two pre-mount emits → late subscriber sees only the most recent (`replay = 1` semantics — a bump to `replay = 2` is a deliberate decision, this test makes it visible); (4) overflow burst of 10 emits with no subscriber → `tryEmit` + `DROP_OLDEST` never stalls the caller, and the most recent URL still wins the replay slot. Bootstrap: added `commonTest.dependencies { implementation(projects.libraries.flowroutines.testing); implementation(projects.libraries.navigation) }` to `libraries/navigation/impl/build.gradle.kts` (navigation api needed for the `DeepLinkBridge` supertype reach). Verified locally via `:libraries:navigation:impl:testDebugUnitTest` — 4/4 pass.
**Reviewer notes:** None — pure black-box test against an interface this class fully implements. The overflow assertion (test 4) doesn't pin the *count* of dropped URLs (could be 5 or 9 depending on subscriber timing) — only the invariant that emit never suspends and the replay slot survives. That's the contract that matters; the exact drop count is buffer-implementation detail.
**Source:** worker-hydrated this cycle (not human-curated). Lane A — verifiable gap (no `DeepLinkBridgeImplTest`, no `commonTest` directory in the module), high signal because the buffer params are subtle and breaking them produces a "deep links sometimes work, sometimes don't" bug that's miserable to track down post-merge.

## test(storage): pin InMemoryCache supplier + flow contract

**Problem:** [`InMemoryCache`](../../libraries/storage/impl/src/commonMain/kotlin/com/dangerfield/cards/libraries/storage/impl/cache/InMemoryCache.kt) is the in-memory leg of the `:libraries:storage` cache pair (`CacheFactoryImpl.inMemory(name, default)`) and the load-bearing wrapper behind every `staticCompositionLocalOf`-less ambient subscription path that doesn't need persistence. Two non-obvious contracts ride on this class: (1) `clear()` re-invokes `initial()` to produce a *fresh* instance (not a memoised first-call result), so callers that hand in mutable wrappers don't see the wiped state pointing at a still-mutated object; (2) the `updates` flow is the same `MutableStateFlow` reference across reads, which downstream `stateIn` subscribers rely on. `:libraries:storage:impl` only had `SignedOutLocalDataCleanerTest` — no coverage on this class.
**Approach:** Added [`InMemoryCacheTest`](../../libraries/storage/impl/src/commonTest/kotlin/com/cards/libraries/storage/impl/cache/InMemoryCacheTest.kt) (8 cases) co-located with the impl, same package. Pins (1) construction seeds with `initial()`; (2) `set` round-trips through `get`; (3) `clear` re-invokes `initial()` — uses a counting supplier so a memoisation regression is loud; (4) `clear` returns a *new instance* via a private `Holder(value: Int)` + `assertNotSame` (covers the mutable-wrapper foot-gun); (5) `update {}` (the default on `Cache`) applies the transform and returns the new value; (6) `updates` flow emits initial + every set/update/clear via Turbine; (7) `set` with the same value twice doesn't double-emit (`MutableStateFlow.value =` semantics) — pins a property a downstream `distinctUntilChanged` consumer would otherwise have to enforce defensively; (8) `updates` exposes the same flow instance across reads (`assertSame`). Verified locally via `:libraries:storage:impl:testDebugUnitTest` — 8/8 pass.
**Reviewer notes:** None — pure black-box test against the public `Cache<T>` contract this class implements. Test (7) (`set` with the same value twice) is the property that's load-bearing for downstream collectors but isn't actually enforced in `InMemoryCache` itself — it's `MutableStateFlow.value =` semantics. Pinning it here means a future refactor that swaps the backing flow shape (e.g. `SharedFlow` with `replay = 1`) gets a loud failure rather than silently producing double-emits.
**Source:** worker-hydrated this cycle (not human-curated). Lane A — verifiable gap (sibling `SignedOutLocalDataCleanerTest` exists, this class had nothing), high signal because the supplier-recompute contract is one of those things a "let's memoize this for perf" refactor would silently break.

## refactor(shop): route ProductIcon corner radius through Radii token

**Problem:** AGENTS.md §DS rule 5 flags hand-tuned `RoundedCornerShape(N.dp)` outside the DS token system as drift. [`ProductIcon`](../../features/shop/impl/src/commonMain/kotlin/com/cards/features/shop/impl/ShopComponents.kt) took a `cornerRadius: Dp = 20.dp` default and called `.clip(RoundedCornerShape(cornerRadius))` — the 20.dp literal exactly matches `Radii.R800` (`DimensionResource.D800 = 20.dp`), and no caller in the shop module overrides the default, so the parameter was just hiding a `RoundedCornerShape(20.dp)` behind a Dp.
**Approach:** Changed the parameter to `radius: Radius = Radii.R800` and swapped the call site to the `Modifier.clip(radius)` overload from `:libraries:ui` (the same shape `HeroProductIcon` at [`ShopScreen.kt:413`](../../features/shop/impl/src/commonMain/kotlin/com/cards/features/shop/impl/ShopScreen.kt) already uses with `Radii.R850.shape`). Pixel-identical rendering (`Radii.R800.shape` is `RoundedCornerShape(20.dp)`); the change is mechanical — the type now affirms "this is a DS radius" instead of a raw Dp. Dropped the now-unused `RoundedCornerShape` and `androidx.compose.ui.draw.clip` imports. Verified `:features:shop:impl:compileDebugKotlinAndroid` + `:features:shop:impl:compileKotlinIosSimulatorArm64` both build clean.

The wider `Color.White.copy(alpha = X)` + raw-hex audit in §A still has open candidates (the `:libraries:ui/components/poker/CardBackStyle.kt` palette literals — 14 card-back styles, each with 1–3 raw `Color(0xFF…)` per style — and the `AchievementMedallion` / `PlayingCard` shimmer overlays); not picking those up in this commit because both are larger and at least one (the shimmer overlays) is a "needs new DS token" call that's worth a human triage pass.
**Reviewer notes:** None. The parameter rename (`cornerRadius` → `radius`) is a source-level break for any external caller passing it by name, but no such caller exists in the repo (grep `ProductIcon(.*cornerRadius` → zero hits). If a future caller wants a different radius they'll pass `radius = Radii.RX` — that's the desired shape.
**Deferred:** `CardBackStyle.kt` raw-hex promotion (likely a new `CardBackPalette` sibling object under `:libraries:ui/system/color/` rather than expanding `PokerPalette` — the file's 14 styles would dominate `PokerPalette`'s docstring scope) and shimmer-overlay `Color.White.copy(alpha)` sites (need a "decorative shimmer" token family to land first — design call). Left in the §A DS-sweep bullet for the next pass.
**Source:** worker-hydrated this cycle (not human-curated). Lane D — mechanical token swap from the explicit P2 DS-promotion sweep in §A.

## test(networking): pin AppStateImpl offline + initial-value contract

**Problem:** [`AppStateImpl`](../../libraries/networking/impl/src/commonMain/kotlin/com/cards/libraries/networking/impl/AppStateImpl.kt) is the source `OfflineBanner` and any future "blocked-by-overlay" UI subscribe to, and its `initialValue = false` (optimistically online) is a *deliberate* call — the inverse would cause cold launches to flash an offline banner before the platform observer's first emission lands. No test existed: `libraries/networking/impl` had no `commonTest` directory at all, so a refactor that flips the initial value, drops the `!online` inversion, or wires up `isBlockActive` to something would all ship silently.
**Approach:** Added [`AppStateImplTest`](../../libraries/networking/impl/src/commonTest/kotlin/com/cards/libraries/networking/impl/AppStateImplTest.kt) (6 cases) against the `AppState` contract this class implements, with a small in-file `FakeConnectivityObserver` (backed by `MutableSharedFlow` so the test can stage emissions). Pins: (1) initial `isOffline.value` is `false` before the observer emits — the cold-launch optimism call; (2) observer-emits-`true` (online) leaves `isOffline` at `false` (Turbine `expectNoEvents()` — `stateIn` deduplicates same-value emissions); (3) observer-emits-`false` (offline) flips `isOffline` to `true`; (4) repeated transitions follow the observer (`!online` inversion pinned); (5) `isBlockActive` is always `false` regardless of observer state (V1 unwired-field contract); (6) eager subscription — `SharingStarted.Eagerly` means an emission before the first `collect` is honoured by the time `value` is read. Bootstrap: added `commonTest.dependencies { implementation(projects.libraries.core); implementation(projects.libraries.flowroutines); implementation(projects.libraries.flowroutines.testing) }` to `libraries/networking/impl/build.gradle.kts`. Verified locally via `:libraries:networking:impl:testDebugUnitTest` — 6/6 pass.
**Reviewer notes:** Test (2) (online emission → no value change) relies on `stateIn` deduplicating same-value emissions. If a future refactor swaps `stateIn` for a `SharedFlow` or `shareIn` shape, that test will start failing — that's the intended signal (the dedup is what makes `isOffline.value` safe to read in compose-recomposition paths without spurious recompositions).
**Source:** worker-hydrated this cycle (not human-curated). Lane A — verifiable gap (no `commonTest` directory existed on this module), high signal because the initial-value call is one of those "I bet the inverse is more conservative" refactor traps that produces a "we always flash an offline banner on launch" bug post-merge.

## test(config): pin ConfigJsonConverter primitive ordering + round-trips

**Problem:** [`ConfigJsonConverter`](../../libraries/config/impl/src/commonMain/kotlin/com/cards/libraries/config/impl/serialization/ConfigJsonConverter.kt) is the JSON ↔ `Any?` adapter under every QA-menu override write/read (via [`ConfigOverrideRepositoryImpl`](../../libraries/config/impl/src/commonMain/kotlin/com/cards/libraries/config/impl/data/ConfigOverrideRepositoryImpl.kt)), the fallback-config-bundle load path (`FallbackConfigMap`), and the remote config decode in `OfflineFirstAppConfigRepository`. The repo-level test exercises round-trips but only with `String`/`Boolean` payloads; the converter's `JsonElement.toAny()` primitive-classification ordering (`isString → booleanOrNull → longOrNull → doubleOrNull → content`) and the `mapNotNull { … toAny()?.let { … } }` silent-null-drop are both subtle invariants no test pinned. A reordering that put `booleanOrNull` ahead of `isString` would silently misclassify a stored `"true"` string as a Boolean — a real corruption mode for any QA override the user saved across launches.
**Approach:** Added [`ConfigJsonConverterTest`](../../libraries/config/impl/src/commonTest/kotlin/com/cards/libraries/config/impl/serialization/ConfigJsonConverterTest.kt) (17 cases) using the production `Json { ignoreUnknownKeys = true }` instance the DI graph hands it. Pins: (1–2) `decodeOverrides` round-trips a `true` boolean and an integer-as-`Long` (precedence of `longOrNull` over `doubleOrNull`); (3) decimals decode as `Double`; (4–5) `isString` precedence — `"42"` and `"true"` stay strings, never reinterpreted as Long/Boolean; (6) the `mapNotNull` silent-null-drop is documented behavior (`{"path":"missing","value":null}` round-trips as nothing); (7) empty arrays decode to empty sets; (8–9) malformed JSON + wrong shape both return `Catching.failure` rather than throwing (load-bearing for the repo's "fall back to empty" path); (10) Boolean/Long/String round-trip through `encodeOverrides` → `decodeOverrides` preserves type; (11) empty list encodes as `[]`; (12) unknown value type (a `data class`) falls back to `toString`; (13–14) `decodeToMap` handles nested objects + JSON arrays; (15) top-level non-object fails (it's `require(element is JsonObject)`); (16) `encodeMap`/`decodeToMap` round-trips a nested-shape map; (17) `JsonElement` pass-through preserves type (`JsonObject` round-trips as `Map`, `JsonNull` as `null`). Verified locally via `:libraries:config:impl:testDebugUnitTest` — 17/17 pass.
**Reviewer notes:** Test (6) documents the silent-null-drop behavior, not the desired behavior. If we ever want `null`-valued overrides to mean "explicitly clear this key" (rather than being silently filtered), that test will be the loud signal to revisit the `mapNotNull`. Test (12) (`toString` fallback for unsupported types) also documents *current* behavior — relying on it would be fragile, but the converter has to do *something* with arbitrary `Any?` and `toString()` is the least-surprising default.
**Source:** worker-hydrated this cycle (not human-curated). Lane A — verifiable gap (`:libraries:config:impl` had tests on `MapOverrides`, `ConfigOverrideRepositoryImpl`, `OfflineFirstAppConfigRepository` but none on the converter the first two route through); high signal because the primitive-ordering invariant is exactly the kind of single-line reorder that produces silent data corruption.

## test(identity): pin AvatarPackCache session + EMPTY semantics

**Problem:** [`AvatarPackCache`](../../libraries/identity/impl/src/commonMain/kotlin/com/cards/libraries/identity/impl/profile/AvatarPackCache.kt) is the disk-persisted half of the avatar-pack session-aware cache called out in `AGENTS.md` (alongside `ProductsRepositoryImpl` as a reference implementation). Three contracts ride on it: (1) `read()` collapses the `CachedAvatarPack.EMPTY` shape (empty packs + null sessionId) back to `null` so `ProfileRepositoryImpl.fetchAvatarPack` can tell "never fetched" apart from "fetched and found nothing"; (2) the on-disk `PackRecord` shape mirrors `AvatarPack` but stays decoupled — a future field addition on the public type shouldn't break snapshots written by old builds; (3) `clear()` resets to `EMPTY` so a `read()` after sign-out looks like a fresh install. Coverage existed for `ProfileRepositoryImpl` (which uses this cache) but not for the cache itself, so a regression in the EMPTY-collapse or the `PackRecord` ↔ `AvatarPack` round-trip would surface as a downstream profile-repo bug rather than a loud cache-level failure.
**Approach:** Added [`AvatarPackCacheTest`](../../libraries/identity/impl/src/commonTest/kotlin/com/cards/libraries/identity/impl/profile/AvatarPackCacheTest.kt) (7 cases) using the same in-file `InMemoryCacheFactory` shape `ProfileRepositoryImplTest` already uses (factory → `FakeCache` backed by `MutableStateFlow`; the persistent-cache `serializer.read(null)` round-trip returns the type's default). Pins: (1) fresh cache `read()` returns `null`; (2) `write` → `read` round-trips packs / palette / sessionId / timestamp; (3) a `write` with an empty `packs` list but a non-null sessionId still surfaces from `read()` — this is the "successfully fetched, server returned no catalog" signal that mustn't be confused with "never fetched"; (4) `PackRecord.from` + `toDomain` preserves every public `AvatarPack` field including the nullable `unlockProductId` corner; (5) `write` replaces a prior snapshot wholesale (not merge — packs+palette+session all swap); (6) `clear()` resets to `EMPTY` and the next `read()` is `null` (the sign-out-wipe contract); (7) `toSuccess()` on the cached row faithfully mirrors the palette field across the round trip. Verified locally via `:libraries:identity:impl:testDebugUnitTest` — 7/7 pass.
**Reviewer notes:** Test (3) (empty-catalog-but-fetched stays non-null) documents what feels like a sharp edge — there's no observable consumer today that cares about distinguishing "empty + fetched" from "empty + never fetched" because the repo overlays a hardcoded fallback pack anyway. But the cache's `read()` shape explicitly preserves the distinction (the `lastFetchSessionId == null` guard, not just `packs.isEmpty()`), so pinning it here keeps a future "let me simplify this to just `packs.isEmpty() ? null : record`" refactor from silently dropping the distinction the moment a real consumer wants it.
**Source:** worker-hydrated this cycle (not human-curated). Lane A — verifiable gap (the class had no direct test; `ProfileRepositoryImplTest` exercises the cache transitively but only through the repo's resolve/update paths, none of which probe the EMPTY-collapse rule).


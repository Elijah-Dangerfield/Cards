## docs(todo): drop 3 stale ENG @Preview items already satisfied in-tree

**Problem:** The todo-maintainer filed three `[P2]` ENG items (proposed 2026-07-01) asking for `@Preview` coverage on the two lobby screens, the two public-matchmaking screens, and `ProfileScreen` — all claiming the screens had none.
**Approach:** Verified each named screen already carries matching `@Preview`s through `PreviewContent { ... }`: `PrivateJoinScreen` (default + RoomNotFound), `PrivateCreateScreen` (1), `PublicSearchingScreen` (10 covering searching/chooser/joined/offer/bot-fallback/error states), `PublicFindScreen` (default + insufficient), and `ProfileScreen` (populated + fresh-user + stocked-shelf). All predate this cycle (last touched by #82/#83/#84, SHOP-5, PROG-7). The acceptance criteria are already met, so shipping code would be inventing work — removed all three items instead. Removing them emptied the ENG section, so I dropped the section header and the now-orphaned trailing rule per worker-prompt rule 5a, leaving a "no open items" line.
**Reviewer notes:** The maintainer almost certainly grepped for a bare `@Preview` and missed the fully-qualified `@org.jetbrains.compose.ui.tooling.preview.Preview` these files use — worth teaching the maintainer's scan to match the qualified form so it stops re-filing satisfied preview items. No code changed; docs-only, nothing to build.

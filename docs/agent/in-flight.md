## feat(cards): tag every EarnableCosmetics entry with a CosmeticTier

**Problem:** Catalog gating todo called for tagging every `EarnableCosmetics.kt` pairing with `EARN_ONLY / EARN_OR_BUY / BUY_ONLY` so shop / inventory surfaces can render the right badge.
**Approach:** Added `CosmeticTier` enum (only `EARN_ONLY` + `EARN_OR_BUY` — `BUY_ONLY` has no `EarnableCosmetics` entry by definition) and a `tier` field on `CosmeticReward`. Tagged all 18 V1 pairings `EARN_ONLY` because every matching `productId` ships `unlock_only = TRUE` server-side (V17 / V20 / V27–V34 migrations). The 18-id pinning test fails if a row's server tier flips to `unlock_only = FALSE` without a matching client-side tier flip — kept as the alignment guarantee. The audit half of the original todo (decide keep / raise-the-bar / retire across the 18) checked out as keep-all: no single-showdown RNG entries remain post the 2026-05-29 retire, and the rest sit on volume / skill / personality-mastery axes per the spec direction.
**Reviewer notes:** I sliced the todo: tagging shipped, the badge-rendering follow-up stays in `docs/todo.md` with a clear acceptance ("Earned" pin in inventory, achievement-badge alongside the EARN_OR_BUY earned instance). Re-slice if the reviewer wants the badge wired in the same PR rather than next cycle. The `EARN_OR_BUY` tier has zero entries today — it's load-bearing once the heat-map widget / W/L-odds display seed entries land.
**Deferred:** Badge rendering on shop product cards + inventory item rows (rewritten in `docs/todo.md`); first EARN_OR_BUY seed entries (gated on the heat-map and W/L-odds widget items, no movement on those).

## docs(agent): strings-enforcement recommendation as a hydrator proposal

**Problem:** `docs/todo.md` §C carried the worker-investigate-and-recommend strings-enforcement bullet. The bullet's own contract was "Worker investigates + recommends; the recommendation lands as a hydrator proposal for human review before implementation."
**Approach:** Surveyed the four options listed (Detekt custom rule, pre-commit grep hook, pre-push CI check, Gradle plugin). Picked the Gradle `verifyStrings` task wired into `check` — same regex precision as the grep options but gates locally *and* in CI (no new tooling), and no `--no-verify` escape. Detekt rejected as overkill for one rule today (brings a plugin + ruleset jar + version drift); pre-commit hook rejected because workers can't use `--no-verify` so the hook never gates them; pre-push CI alone rejected because the local feedback gap forces fix-up pushes. Full writeup with table + rejected-alternatives + suggested item shape lives in `docs/agent/todo-proposals.md`. The `docs/todo.md` bullet is removed; a placeholder line tells future workers where the proposal went.
**Reviewer notes:** I didn't ship the actual Gradle task — that's the gated-on-triage half. The proposal is structured so the human can either accept it directly into `docs/todo.md` as a worker-ready follow-up, edit the regex shape, or pivot to Detekt with the rejected-alternatives reasoning visible. One thing the proposal flags but I'd repeat here: the regex approach has false-positive risk and the proposed escape hatch is `// strings-enforce: skip` per-line — that's a judgment call the reviewer may want to course-correct (a separate allowlist file is the alternative; both are fine).
**Deferred:** The actual `verifyStrings` task implementation (see proposal); baseline-allowlist file for current real violations like `PurchaseConfirmSheet.kt:189` and `AppGuardLayer.kt:237` (separate `[P2]` cleanup item once the task lands).

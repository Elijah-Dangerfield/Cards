# In-flight (current cycle)

This file is ephemeral — one block per worker commit this cycle. The reviewer reads it when writing the PR description, then deletes the file as part of opening the PR.

## feat(catalog): pair SHOW_FULL_HOUSE with title_full_boat

**Problem:** §A "Catalog gating — More earnable cosmetic pairings" called out `SHOW_FULL_HOUSE` and `SHOW_FOUR_OF_KIND` as the remaining unpaired EPIC showdowns. After V42 every rarity-EPIC-or-above achievement *except* those two had a cosmetic complement, breaking the showdown-ladder symmetry that V41/V42 established for the two LEGENDARY mysteries.
**Approach:** seeded `title_full_boat` via V44 (server SQL migration following the V35/V41/V42 unlock-only-title pattern — `unlock_only=TRUE`, `is_equippable=TRUE`, sort 790 just below `title_suited_run`); wired client-side via `cosmeticRewardFor(SHOW_FULL_HOUSE)`, `titleForProductId("title_full_boat")`, and `ClientGrantableAchievements.Default.clientGrantable`. Picked "Full Boat" over "Housemaster" / "Boat Captain" / literal "Full House" — "boat" is universal poker slang for a full house and the two-poker-word shape mirrors V42's "Suited Run" without repeating the literal hand name. Updated `EarnableCosmeticsTest` (added the new mapping, moved `SHOW_FOUR_OF_KIND` into the no-reward null-check spot vacated by `SHOW_FULL_HOUSE`); added `GrantsRoutesTest.defaultPolicy_grantsFullBoatTitle_forShowFullHouse` mirroring the V41/V42 test shape.
**Reviewer notes:** the name itself is the directional call worth a second eye — "Full Boat" reads natural to poker players but might look unfamiliar to a brand-new user the first time it lands under their seat. Easy revert (one DB row + four when-branch entries + two test pins) if the reviewer wants a different word.
**Deferred:** the matching `title_quartet` for `SHOW_FOUR_OF_KIND` ships in the next commit this cycle, so they roll together as one PR but each gets its own logical commit.

## feat(catalog): pair SHOW_FOUR_OF_KIND with title_quartet

**Problem:** completes the EPIC-showdown pairing pair started in the prior commit — `SHOW_FOUR_OF_KIND` was the last rarity-EPIC-or-above showdown achievement still without a cosmetic complement.
**Approach:** seeded `title_quartet` via V45 (same unlock-only-title pattern as V41/V42/V44, sort 800 just below `title_full_boat`); wired client side identically — `cosmeticRewardFor(SHOW_FOUR_OF_KIND)`, `titleForProductId("title_quartet")`, and `ClientGrantableAchievements.Default.clientGrantable`. Picked "Quartet" — single elegant word capturing "four of one kind" via the musical metaphor (a quartet is exactly four), mirroring V41's single-word "Royalty" shape. Considered "Quad King" (two words, "King" disconnected from four-of-a-kind), "Quads" (literal achievement name, same redundancy V41/V42/V44 avoided), "Four-Star" (military-rank metaphor that overloads with review/quality meanings), "Quad Squad" (rhyme reads juvenile under a player's name). Added `showFourOfKind_mapsToQuartetTitle` to `EarnableCosmeticsTest`, moved `SHOW_FLUSH` into the null-check spot vacated when both EPIC showdowns gained mappings (keeps the test pinning a RARE-tier showdown that still earns no cosmetic). Added `defaultPolicy_grantsQuartetTitle_forShowFourOfKind` mirroring the V44 grants-route test.
**Reviewer notes:** same directional call as the V44 commit — the word "Quartet" is the part worth a second eye. Single point of revert (one DB row + four when-branch entries + two test pins). After this commit the showdown rarity ladder (Flush=RARE → Full House=EPIC → Quads=EPIC → Straight Flush=LEGENDARY → Royal Flush=LEGENDARY) is fully paired above the RARE tier, and the §A todo entry is rewritten to reflect that completion.

## refactor(profile): lift DeleteAccountState.error to sealed DeleteAccountError

**Problem:** the §A strings sweep had `DeleteAccountState.error: String?` still raw-stringy in the VM — four user-facing copy strings inlined into VM branches, with the screen rendering them verbatim and the test asserting via substring matches. The strings-sweep todo lists the sealed-error pattern's worked examples (Lobby + SignIn + SignUp + ClaimAccount + Onboarding + EditProfileDisplayName) — DeleteAccount sat at the same shape but hadn't been migrated.
**Approach:** lifted `error: String?` to `error: DeleteAccountError?` — sealed interface with four `data object` variants (NotConfigured / AnonymousNotAllowed / NetworkError / Unknown) that map 1:1 to the four non-success `DeleteAccountOutcome` branches the VM was already pattern-matching. Added `profile_delete_error_{not_configured,anonymous_not_allowed,network,unknown}` resource keys with the prior copy verbatim. Screen resolves via a private `@Composable DeleteAccountError.message(): String` extension at the bottom of `DeleteAccountScreen.kt`, mirroring the same shape `ClaimAccountScreen` and `EditProfileScreen` use. `DeleteAccountViewModelTest`'s four substring matchers (`.contains("anonymous")` etc.) lifted to typed equality. Dropped now-unused `assertTrue` + `assertNotNull` imports. `DeleteAccountScreenPreview_Error` switched from a hardcoded preview string to `DeleteAccountError.NetworkError`.
**Reviewer notes:** mechanical lift — exact same shape as the five sealed-error VMs already shipped this sweep. No behavior change; the copy strings are byte-identical between the old VM branches and the new resource entries. The `EditProfileState.error: String?` field flagged as "dead" in the §A todo still hasn't been touched — that's a separate is-the-banner-wanted question out of scope for the strings sweep, leaving it alone.
**Deferred:** `FeedbackViewModel.errorMessage: String?` and `BugReportViewModel.errorMessage: String?` are the two remaining VM-side raw-error fields in `:features:profile:impl`. Both are simpler shapes than DeleteAccount (one-branch errors fed straight from a single `Catching {}` block) so the lift is more mechanical than judgment-heavy. Left for a future cycle — they're a clean follow-up pick for the next worker.


## feat(server): allowlist client-grantable achievements + 403 server-witnessed

**Problem:** `POST /v1/me/grants/achievement/{id}` resolved through `AchievementRewards.mapping` and 204'd anything not in it. Accidentally safe today (only two ids map, none are server-witnessed yet), but the first future server-witnessed achievement added to that map would let any authed client self-grant it.
**Approach:** replaced the single `AchievementRewards` object with a `ClientGrantableAchievements` policy that resolves to `Grant(productId) | ServerWitnessed | Unknown`, and split the route into 200 / 403 / 204 accordingly. The policy is a class with a `Default` companion (production wiring stays a one-liner), and `grantsRoutes` takes it as an injectable param so the 403 branch is testable without polluting prod data. `serverWitnessed` is empty today by design — it's the slot future PvP / league achievements drop into. Also deleted the dead `PlayPokerEvent.ShowAchievementUnlock` (no collector) and the never-fired `HapticKind.AchievementUnlock` / `SoundKind.AchievementChime` enum entries it would have triggered — unlock callouts live in BustDialog / ShowdownDialog by product call.
**Reviewer notes:** `ClientGrantableAchievements` is a class (not object) so tests can build one with `serverWitnessed` entries; `Default` is the prod singleton. The 403 message ("granted server-side; clients can't self-grant it") is reachable but never fired today — first user to see it is whoever ships a server-witnessed achievement and bumps an old client into hitting the route. The dropped enum entries were genuinely unreferenced (grep clean across the repo).

## feat(server): seed starter inventory atomically in profile creation

**Problem:** every `GET /v1/me` re-fired `recordEarnedGrant(felt_default)` so new users had at least one row in My Items — cheap but conceptually wrong (the earned-grant pathway is for achievement rewards, not "you signed up"), and the only starter row was the felt; no default card back, no default emoji-blast pack.
**Approach:** introduced `StarterInventory.productIds` (`felt_default`, `cardback_default`, `emoji_pack_starter`) and seed all three inside `PostgresProfileRepository.findOrCreate` in the same outer transaction as the profile insert, using `insertIgnore` for idempotency. V18 migration adds the two new catalog rows (both `unlock_only = TRUE`) and backfills every existing profile with all three starter rows (`ON CONFLICT DO NOTHING`, so the historical felt_default rows keep their original `purchased_at`). Dropped the `/v1/me` grant block + the now-unused `clock` parameter from `meRoutes`. Client `EmojiPackCatalog` learned the new starter pack's emoji set (👋 👍 🎉 😀) so owning the row actually surfaces emojis in the in-game tray rather than being inventory-decoration. `EmptyInventory` test stub in `MeRoutesTest` now throws on `recordEarnedGrant` so a regression that re-introduces the grant fails loudly.
**Reviewer notes:** picked the starter emoji set (👋 👍 🎉 😀) as a friendly neutral default — none of the existing packs use them, so no overlap. Card-back surface needed no client change (the renderer already falls back to `CardBackStyle.Default` blue for unknown ids, and `cardback_default` matches that fallback). `PostgresProfileRepository` now reaches across to `InventoryTable` directly inside its transaction — this is the path the todo explicitly carved out (decoupling via injection is on the backlog under "ProfileRepository → InventoryRepository import"); for now atomicity wins over cohesion. Test coverage: two new integration tests in `PostgresProfileRepositoryTest` lock in seeding and idempotency.
**Deferred:** seat-bottom emoji-pack-owned indicator at the table tray (only matters once the pack ID list changes again); nothing yet — reviewer please triage.

## fix(catalog): realign title_pot_magnet copy with BB-relative POT_5000

**Problem:** V17 seeded `title_pot_magnet` with the description "Unlocked by sitting at a 5,000-chip pot. …" against the V1 absolute-chip POT_5000 criterion. The previous commit in this cycle re-anchored POT_5000 to ≥ 25× BB, so that copy is now user-facing wrong — MyItemsScreen renders this description under the title row when the title lands in inventory.
**Approach:** V19 migration `UPDATE products SET description_by_locale = '{"en":"Unlocked by sitting at a pot 25× the big blind. …"}' WHERE id = 'title_pot_magnet'`. Only the EN side changes; V17 didn't seed Spanish, and the catalog matcher falls back to EN.
**Reviewer notes:** None. Sibling `title_short_stack_hero` description is unchanged (COMEBACK_FROM_5BB has always been BB-relative).

## test: expand preview coverage on AchievementsScreen + BugReportScreen

**Problem:** todo §A "Previews on every user-facing composable" — both screens only had a single mid-state preview. Achievements lacked the empty / full-collection states that new and end-game users see; BugReport lacked the no-context (settings-entry), submitting, and error states.
**Approach:** added `AchievementsScreenPreview_Empty` + `AchievementsScreenPreview_AllEarned` alongside the renamed `AchievementsScreenPreview_SomeEarned`; added `BugReportScreenPreview_Empty`, `..._Submitting`, `..._Error` alongside the renamed `BugReportScreenPreview_WithCapturedContext`. Followed the `<ScreenName>Preview_<StateDescription>` naming convention from AGENTS.md.
**Reviewer notes:** None.

## feat(achievements): re-anchor DONT_CALL_IT_COMEBACK + POT_5000 to BB multiples

**Problem:** the V1.x bullet in todo.md flagged two tier-blind achievements broken by `StakeTier` shipping. `DONT_CALL_IT_COMEBACK` armed at ≤100 chips / fired at ≥1000 chips — meaningful on Casual but a near-bust read on High (0.5 BB) and easier-than-intended on Practice. `POT_5000` triggered on absolute 5K pots — trivial on Challenging (blinds 100/200 cross 5K by the turn) and a real EPIC-tier accomplishment on Casual. The audit doc shipped last cycle; this is the follow-up that lands the code.
**Approach:** re-anchored both criteria to BB multiples per the spec's Summary §1 + §2, tier-agnostic. `updateDontCallItComebackCounter` now arms when ending stack is in (0, 10 BB] and fires when armed + ending stack ≥ 100 BB (the universal "full stack" since buy-ins are 100× BB across all tiers). For `POT_5000`, added a new `MAX_POT_BB_RATIO` high-water counter that runs alongside the absolute `MAX_POT_SEEN` (POT_500 / POT_1000 keep reading the absolute — they're tolerable per the audit) and pointed `POT_5000`'s criterion at it with target `25`. Description text on both achievements refreshed to read in BB terms. Existing users keep unlocks (the earned ledger is independent of criterion math). Added `AchievementRepositoryImplTest` covering arm/fire across Casual + High tiers, the no-arm-no-fire path, the bust-doesn't-arm guard, the 25× BB threshold both sides, and the high-water-mark ratchet — tests pre-mark all other achievements as earned so the `MAX_ACHIEVEMENTS_PER_HAND` cap can't crowd the target out.
**Reviewer notes:** added a parallel counter rather than re-purposing `MAX_POT_SEEN` because POT_500 / POT_1000 still read the absolute value cleanly per the audit; one counter per concept beats overloading the same key with two semantics. The MP-keyed sibling ids (Summary §3) are gated on Phase 4.2 and stay parked in the spec + a one-line todo bullet. Updated `docs/achievements-spec.md` status header + the two affected rows to reflect what landed.

**Problem:** the todo's achievements-audit bullet explicitly required the audit doc to land *before* any registry code changes — "Once the doc reads true end-to-end, then land the registry changes." No spec existed.
**Approach:** wrote `docs/achievements-spec.md` with one row per registry entry: current mode, proposed mode, split-id-or-no, threshold rationale, V1/V1.x ship target. Three concrete V1.x changes fall out: re-anchor `DONT_CALL_IT_COMEBACK` to BB multiples (absolute 100/1000 thresholds break at high tiers), re-anchor `POT_5000` to ≥25× BB (trivial on Challenging today), and add `FIRST_BUST_DEALT_MP` / `BUST_DEALT_5_MP` siblings once Phase 4.2 lands. Updated the todo bullet to reference the spec as the source of truth for the follow-up registry changes.
**Reviewer notes:** intentionally an audit, not a creative pass — no new achievement concepts proposed (those live elsewhere). The MP-only `*_MP` ids are recommendations not commitments: they depend on Phase 4.2 (server-authoritative gameplay) landing for a trustworthy grant pathway. The two re-anchor proposals are also out-of-scope here — they're the *next* engineering bullet, not this commit.

## feat(catalog): seed cardback_comeback_kid for DONT_CALL_IT_COMEBACK

**Problem:** todo §A "Seed an opening pool of unlock-only catalog content" landed two unlock-only titles (V17) but no earned card backs, so the unlock-only catalog still flexed only one cosmetic shape. `AchievementId.DONT_CALL_IT_COMEBACK` (the EPIC "drop to ≤10 BB then build back to 100 BB" milestone) had no reward at all.
**Approach:** V20 migration inserts `cardback_comeback_kid` as `unlock_only = TRUE` with `cost_chips = 0` and `is_equippable = TRUE`, slotted at sort order 540 (directly under the shoppable card backs 500–530). Added `CardBackStyle.ComebackKid` with a phoenix-tone palette (deep red `0xFF6B1E0E` → bright orange `0xFFE85D1B` → warm gold `0xFFE8BC4D`, border `0xFFFFC766`) and wired `cardback_comeback_kid` → `CardBackStyle.ComebackKid` in `cardBackForProductId`. Extended `ClientGrantableAchievements.Default` with `"DONT_CALL_IT_COMEBACK" to "cardback_comeback_kid"` so unlocks land via `POST /v1/me/grants/achievement/{id}`. Added a `defaultPolicy_grantsComebackKidCardBack_forDontCallItComeback` route test that mirrors the existing POT_5000 happy-path test. Also surfaced the new card back in `PreviewCosmeticPreview_CardBacks` so the DS preview row stays accurate.
**Reviewer notes:** chose a *card back* (not the felt the original todo bullet floated) so the unlock-only catalog flexes a second cosmetic slot in addition to the two existing titles — felts already have one shoppable variant per palette family, while card backs were title-shape-only before this. No backfill for users who already earned DONT_CALL_IT_COMEBACK pre-V20; matches the V17 precedent and is documented inline in the migration header (closing the gap needs server-side achievement tracking, which is Phase 4.2). `AchievementGrantApiImpl` handles the new 200 path without changes — the route returns OwnedItemDto, same shape as the title grants.

## test: expand preview coverage on FeedbackScreen + StatsScreen

**Problem:** todo §A "Previews on every user-facing composable" — both screens had only two `@Preview` states. FeedbackScreen lacked the `isSubmitting`, `errorMessage`, and char-limit-reached states (each renders different chrome — disabled button, red helper text, red counter). StatsScreen lacked the initial loading state (`isLoading = true` shows a spinner instead of the hero) and the achievements-row populated state.
**Approach:** Renamed `FeedbackScreenPreview` / `FeedbackScreenPreviewDisabled` to the `<ScreenName>Preview_<StateDescription>` convention from AGENTS.md (`_Empty`, `_Filled`) and added `_Submitting`, `_Error`, `_CharLimitReached`. Added `StatsScreenPreview_Loading` and `StatsScreenPreview_WithAchievements` alongside the existing `_Empty` + `_Populated`; the new "with achievements" preview pulls the first three registry entries via `AllAchievements.take(3)` so the row renders against real registry data rather than a hand-rolled stub.
**Reviewer notes:** None.

## test: expand preview coverage on OnboardingScreen

**Problem:** todo §A "Previews on every user-facing composable" — OnboardingScreen had one preview per step (4 total) but no coverage of the off-happy-path states: OAuth in flight, OAuth error on Welcome, saving / save-error on PickIdentity. Those are exactly the states a state-specific layout bug shows up in (disabled providers, red error text under the name field) and the previews catch.
**Approach:** added `OnboardingScreenPreview_Welcome_OAuthInFlight`, `OnboardingScreenPreview_Welcome_AuthError`, `OnboardingScreenPreview_PickIdentity_Saving`, and `OnboardingScreenPreview_PickIdentity_SaveError` alongside the existing happy-path step previews. Followed the `<ScreenName>Preview_<StateDescription>` convention from AGENTS.md.
**Reviewer notes:** None.

## test: expand preview coverage on LobbyScreen

**Problem:** todo §A "Previews on every user-facing composable" — LobbyScreen had 6 previews (idle / idle-with-code / 4× in-room variants) but no coverage of the busy states (`creating`, `joining`, `leaving`) or the idle `error` banner. Those flip button labels, disable inputs, and surface the danger-tone error text; previews catch state-specific layout regressions there.
**Approach:** added `LobbyScreenPreview_Idle_Creating`, `_Idle_Joining`, `_Idle_Error`, and `_InRoom_Leaving` alongside the existing happy-path previews. Followed the `<ScreenName>Preview_<StateDescription>` convention from AGENTS.md.
**Reviewer notes:** None.

## test: expand preview coverage on DeleteAccountScreen

**Problem:** todo §A "Previews on every user-facing composable" — DeleteAccountScreen had 3 previews (empty / confirmed / error) but no coverage of the in-flight submit state. `isSubmitting = true` disables the back arrow, the confirmation field, the cancel text button, and flips the danger CTA label to "Deleting…" — a distinct chrome state worth pinning.
**Approach:** added `DeleteAccountScreenPreview_Submitting` alongside the existing previews. Followed the `<ScreenName>Preview_<StateDescription>` convention from AGENTS.md.
**Reviewer notes:** None.

## test: add populated MyItemsScreen preview with mixed inventory

**Problem:** todo §A "Previews on every user-facing composable" — MyItemsScreen had `MyItemsScreenPreview_Empty` for the empty path but the populated state was only ever exercised via two row-only previews. The in-file comment justified that as "ownedItems is a derived join — we'd need to fake both inventory + catalog." Now that `ProductCatalog`, `InventoryItem`, and `MyItemsState.equippedIds` are all stable enough to assemble cheaply, the populated screen visual can be pinned end-to-end.
**Approach:** added `MyItemsScreenPreview_Populated` plus a private `previewPopulatedState()` factory that builds a four-row `MyItemsState` — an equipped purchased felt, an equipped earned card back (Earned tag + chrome), a purchased non-equippable emote pack (Unlocked badge), and an earned title (Earned tag, equip toggle). Kept the existing row-only previews so reviewers still have isolated row visuals when they want them.
**Reviewer notes:** the populated factory uses real product ids from the seeded catalog (`felt_midnight_blue`, `cardback_comeback_kid`, `emotes_drama`, `title_pot_magnet`) so `CosmeticPreview` renders the same visuals it would in production — no synthetic ids that would silently fall back to the default tile.

## refactor(qa-menu): route enum-chip shape through Radii.Round + add preview

**Problem:** the QA menu's allowed-value chip was the last DS-first hold-out in `QaMenuScreen` — it hand-rolled `RoundedCornerShape(50)` for the pill shape instead of the existing `Radii.Round` token. Also, the screen had only two previews (default config, with + without user id), neither of which exercised the chip-selected accent state or the "current ≠ default" branch of the row footer.
**Approach:** replaced `RoundedCornerShape(50)` with `Radii.Round.shape` and dropped the now-unused import. Added `QaMenuScreenPreview_OverridesActive` that pre-selects `maintenanceMode = "banner"` (pins the chip-selected accent) and `minSupportedVersionCode = 99` (pins the "current: 99    default: 1" row).
**Reviewer notes:** None.

## refactor(ds): route remaining pill-shape callsites through Radii.Round

**Problem:** todo §A "DS components should own their visual defaults" — `RoundedCornerShape(50)` was still hand-rolled at 12 callsites across `BoardArea`, `WinOddsBadge`, `HandRankingsCheatSheet`, `ProfileScreen`, and `StatsScreen`. The QA menu sweep landed earlier this cycle removed the last one in that screen; this finishes the job everywhere else `Radii.Round` already would have fit.
**Approach:** mechanical swap to `Radii.Round.shape` per the DS-first rule. Same visual (both expressions resolve to `RoundedCornerShape(CornerSize(percent = 50))`); the codebase now has one source of truth for the pill radius. Dropped the now-unused `RoundedCornerShape` import from the three files that no longer have any callsites left; `BoardArea` and `HandRankingsCheatSheet` keep theirs for the remaining `RoundedCornerShape(N.dp)` callsites that don't have a matching token.
**Reviewer notes:** the `N.dp` callsites in `HandRankingsCheatSheet` (20.dp, 28.dp) and `BoardArea` (16.dp) map cleanly to `Radii.R800` / `Radii.R1000` / `Radii.R700` — left untouched to keep this sweep narrow to pills, but they're a natural next pass.
**Deferred:** the dp-valued `RoundedCornerShape(...)` callsites — left in `docs/todo.md` § "DS components should own their visual defaults"; reviewer can pick up in a follow-up cycle.

## refactor(ds): route pill-shape callsites in shop + libraries/ui through Radii.Round

**Problem:** todo §A "DS components should own their visual defaults" — the prior pill sweep in this branch missed two pockets: (a) four DS primitives in `:libraries:ui` (`RankBadge`, `ChipPill`, `LeadingPill`, `LastActionPill`) still hand-rolled `RoundedCornerShape(50)` even though they live alongside `Radii` itself, and (b) the shop chrome used `RoundedCornerShape(999.dp)` (BadgePill, OverhangBadge, CountdownBadge, LockedFooter, InsufficientChipsFooter, OwnedFooter, ChipCostFooter) which clamps to the same pill visual but reads as a magic number at the callsite.
**Approach:** mechanical swap to `Radii.Round.shape` across all 11 callsites — 4 in `:libraries:ui`, 2 in `ShopComponents`, 1 in `CountdownBadge`, 4 in `ShopScreen`. Same visual (`percent = 50` and `999.dp` both clamp to half the shorter dimension on the small pill chrome these surfaces use). Dropped the now-unused `RoundedCornerShape` import from `RankBadge`, `ChipPill`, `LeadingPill`, `LastActionPill`, and `CountdownBadge`; kept it in `ShopScreen` (HeroProductIcon's 22.dp callsites stay — no matching token) and `ShopComponents` (the parametrised `ProductIcon(cornerRadius: Dp)` callsite still routes through `RoundedCornerShape`).
**Reviewer notes:** the remaining N.dp callsites (`PlayerActionSheet` 28/32/36.dp, `PlayerArea` 16/20.dp, `BoardArea` 16.dp, `HandRankingsCheatSheet` 20/28.dp, plus the 22.dp HeroProductIcon, the 4.dp BustedStamp, the 6.dp BarChart) split into two camps: those with matching `Radii.R*` tokens (16→R700 / 20→R800 / 28→R1000), and those without (4 / 6 / 22 / 32 / 36 / parametrised). The matching-token group is the next sweep — picked up in the follow-up commit this cycle. The non-matching values are narrow enough (1–2 callsites each) that token expansion isn't justified yet.

## refactor(ds): route N.dp room callsites through matching Radii tokens

**Problem:** todo §A "DS components should own their visual defaults" — the prior pill sweep noted that `BoardArea` (16.dp), `PlayerArea` (16.dp + 20.dp), `HandRankingsCheatSheet` (20.dp + 28.dp), and `PlayerActionSheet` (28.dp) hand-rolled corner radii that map cleanly to `Radii.R700` / `R800` / `R1000` — exactly the case the DS-first rule is targeting.
**Approach:** mechanical swap to `Radii.R700.shape` / `R800.shape` / `R1000.shape` (Dimension D700 = 16.dp, D800 = 20.dp, D1000 = 28.dp — identical visual). Touched callsites: `BoardArea.BoardWell` (2 of 16.dp), `PlayerArea` (2 of 20.dp, 2 of 16.dp), `HandRankingsCheatSheet.SectionHeader / .RankRow / .RankingCard` (1 of 28.dp + 2 of 20.dp), `PlayerActionSheet.ActionTile` (1 of 28.dp). Added the `Radii` import to `PlayerArea` and `PlayerActionSheet`. Dropped the now-unused `RoundedCornerShape` import from `BoardArea`, `PlayerArea`, and `HandRankingsCheatSheet`; kept it in `PlayerActionSheet` (the 32.dp / 36.dp slider-thumb / track callsites stay — no matching token).
**Reviewer notes:** `PlayerActionSheet`'s 32.dp (slider track) and 36.dp (slider thumb) sit between `Radii.R1000` (28.dp) and the never-defined R1050/R1150 slots. Could extend `Radii` with `R1050 = D1050.dp (32.dp)` and `R1150 = D1150.dp (36.dp)` — the Dimension tokens already exist — but those are the only callsites for either value, so I left it. Same call for `BustedStamp` (4.dp = D100, only 2 callsites), `BarChart` (6.dp = D200, 1 callsite), `ShopScreen.HeroProductIcon` (22.dp = D850, 2 callsites), and `TypographyResource` (4.dp, 1 callsite).
**Deferred:** extending `Radii` with `R100 / R200 / R850 / R1050 / R1150` to cover the remaining N.dp callsites — left in `docs/todo.md` § "DS components should own their visual defaults"; cost/benefit is low enough at current callsite counts that the reviewer should sign off before lifting them.

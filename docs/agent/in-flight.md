## refactor(room): migrate PlayerProfileSheet + playing-style + difficulty-tier to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — `PlayerProfileSheet.kt` (tap-an-opponent profile sheet) carried inline copy for its section titles + Mute emoji row ("Playing style" heading / "At this table" / "Difficulty" / "Settings" section titles / "Mute emoji" headline / muted-vs-unmuted supporting copy). Two of its helpers also held user-facing copy with no resource backing: `BotPlayingStyle` (6 archetype label + description rows: Maniac / Loose aggressive / Tight aggressive / Tight passive / Loose passive / Balanced) and `BotDifficultyTier` (Casual / Standard / Challenging description rows; label is the wire-key from `SoloBotsPokerSessionFactory.difficultyName`). ~22 strings between the three files, all on the surface a curious user pops up tap-tap during a hand.
**Approach:** Added the `room_player_profile_*` family under the existing `{surface}_{role}_{specifier}` naming, with sub-families `_style_*` for the six archetype label/description pairs and `_difficulty_*` for the three tier descriptions. `BotPlayingStyle.label` + `.description` lifted from `String` to `StringResource` so `playingStyleFor(personality)` stays a non-Composable mapping function and the resolution happens at the `PlayingStyleBlock`'s `Text(text = stringResource(style.label), ...)` callsite — same resource-key-as-data pattern as the prior cycle's `BetPreset.labelResource` + this cycle's `RankingEntry.name`/`.tagline` lifts. `BotDifficultyTier.label` deliberately *stays* as `String` (Casual / Standard / Challenging are wire-keys from `SoloBotsPokerSessionFactory.difficultyName` — same call as the home cycle's deferral of those three labels at `OptionPillRow`'s callsite), only `.description` lifts to `StringResource`. **Rejected alternative:** lifting both `.label` + `.description` would dragnet the wire-key seam and conflict with the home-cycle deferral of the same three strings; splitting them lets the user-visible description migrate today while the wire-key stays untouched until a typed `BotDifficulty` enum lands. **Rejected alternative:** lifting `RadarAxis.label` (Tight / Aggro / Bluff / Patient on the playing-style radar chart) to `StringResource` — would require either changing `RadarAxis` in `:libraries:ui` (cross-module surface, RadarChart's own previews use plain strings too) or making `radarAxesFor` `@Composable` (breaks the existing non-Composable unit-test contract that asserts `axes.map { it.label } == listOf("Tight", "Aggro", "Bluff", "Patient")`). Four ultra-short typography labels stay inline; they're spoke labels on a chart, not body copy. **Test updates:** `BotPlayingStyleTest` swapped its `label == "Tight passive"` assertions to `label == Res.string.room_player_profile_style_tight_passive_label` (and dropped the `description.isNotBlank()` smoke check — meaningless against a resource id), plus added a "≥4 distinct description keys across the 5-bot Roster" check to pin the archetype-to-description mapping. `BotDifficultyTierTest` dropped its `description.isNotBlank()` check (same reason) but kept the "all three resolve" + "descriptions are distinct" pins (distinct by `StringResource` identity now). `commonTest` source set gained `projects.libraries.resources` so the test references compile.
**Reviewer notes:** `:features:room:impl:compileDebugKotlinAndroid` + `:features:room:impl:testDebugUnitTest` + `:apps:compose:assembleDebug` all green. All four `PlayerProfileSheetPreview_*` variants (BotUnmuted_TightPassive / BotMuted_Maniac / Bot_LooseAggressive / NoPersonality) render through the migrated Composables. Module-level deprecation warnings on tutorial / Preview-import paths are pre-existing.
**Deferred:** (1) `SeatTenure.kt` — `formatHandsAtTable(count)` + `formatPlayingSince(epochMs)` + 12 month names + the "Hands you've shared with this table." / "Hands they've shared with this table." / "When your account joined." supporting copy. Non-Composable `String`-returning formatters with a heavy `SeatTenureTest` contract (`assertEquals("1 hand at this table", formatHandsAtTable(1))` shape). Migrating means either restructuring tests to Compose UI test runtime or moving the API to `suspend getString(...)`. Own slice. (2) `TableUiState.badgeFor()` — same shape ("Lvl $level" / "Bot · $label" / "Bot"), non-Composable on the hot path with `TableUiStateTest` coverage. Defer alongside `SeatTenure`. (3) The bigger room surfaces — `PlayPokerScreen` chrome, `OpponentsRow`, `PlayerArea`, `BoardArea`, `TableActionBar`, `EmojiTray`, the tutorial family — each its own future slice.

## refactor(room): migrate HandRankingsCheatSheet copy to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — `HandRankingsCheatSheet.kt` is the largest remaining bounded slice in `:features:room`: the tap-the-?-icon reference modal that explains both how to act (Check / Call / Raise / Fold / All in title + description rows) and the full ten-entry hand-ranking ladder, plus the per-hand context card (HAND #N pill + six BettingRound labels + six per-street explainers). Roughly 35 inline strings between the static rankings catalog, the action rows, the street mapping, and the hero copy. Cousin to the migrated `HandLabelExplainer` + `LastActionExplainer` mapping pair but with a static-data ranking catalog as the structural twist.
**Approach:** Added the `room_cheat_sheet_*` family under the existing `{surface}_{role}_{specifier}` naming. The `RankingEntry` private data class lifts its `name: String` + `tagline: String` to `name: StringResource` + `tagline: StringResource` so the file-level static `rankings` catalog stays non-Composable; `RankingCard` resolves at the `Text` callsite via `stringResource(entry.name)` + `stringResource(entry.tagline)`. Same pattern as the prior cycle's `BetPreset.labelResource` lift — resource-key-as-data, resolution-at-callsite — so the catalog doesn't have to become Composable just to feed names through. The street mapping (`streetLabelForCheatSheet` + `streetExplainer`) collapsed into two non-Composable mapping functions returning `StringResource` (`streetLabelResourceFor` + `streetExplainerResourceFor`) — same shape as `LastActionExplainer.explainerResourceFor` so the per-BettingRound dispatch table stays grep-able from one place. Action symbols (✓ = ↑ ✕ ★) stay inline at the `ActionRow` callsite — they're typography characters rendered in a single 36dp accent-tinted circle, not user-translatable copy; same call as the BlindRoles 🎲 / cheat-sheet-glyph deferrals in earlier slices. The HAND #N pill interpolates the int via `%1$d` rather than `%1$s` + `.toString()` — Int formatting through `%d` is safe across CMP Native targets (the prior `%1$s` pattern only matters for `Long` values that need explicit `.toString()` because the resource API doesn't auto-box). Hero subtitle "Strongest on top." migrated verbatim as `room_cheat_sheet_rankings_subtitle`; the action / ranking headings each get a `_heading` suffix.
**Reviewer notes:** `:features:room:impl:compileDebugKotlinAndroid` + `:features:room:impl:testDebugUnitTest` both green. All three `HandRankingsCheatSheetPreview_*` variants (MidHand / NoHandInfo / River) render through the migrated Composable so they auto-pick-up the new copy. The `rankings` `val` is still file-level static — moving the names out to resources doesn't change that since `StringResource` is a stable id, the lookup happens lazily at the `Text` site. Module-level deprecation warnings on tutorial / Preview-import paths are pre-existing (not introduced by this commit).
**Deferred:** (1) `PlayerProfileSheet` is the next bounded slice planned for this same cycle as a follow-up commit — its mapping functions (`playingStyleFor` + `difficultyTierFor`) need the same shape as the room cheat-sheet's `streetLabelResourceFor`, plus the inline "Mute emoji" / "At this table" / "Playing style" / "Difficulty" / "Settings" section copy. (2) `PlayPokerScreen` chrome, `OpponentsRow`, `PlayerArea`, `BoardArea`, `TableActionBar`, `EmojiTray`, the tutorial family — each its own future slice; the bigger ones may split further.

## refactor(room): migrate hand-end + achievement-celebration copy to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — the hand-end result surfaces and the bot-mode achievement celebration carry the bulk of the high-frequency copy users see at table: `HandResultDialogs.kt` (`ShowdownDialog` outcome headline + win amount + pot label + board label + next-hand CTA, `BustDialog` title + body + deal-me-in CTA, the inline `AchievementUnlockedCallout` for MP unlocks, the `XpEarnedBubble`, the `ShowdownRow` per-seat tile) and `AchievementCelebrationSheet.kt` (single vs multi unlock title + subtitle, continue CTA, the rewardSummary builder, the cosmetic-attribution line). ~25 inline strings between the two files, all firing at the moment the user is most emotionally engaged with the table.
**Approach:** Added the `room_showdown_*`, `room_bust_*`, `room_achievement_*`, `room_xp_bubble_*`, `room_celebration_*` families under the existing `{surface}_{role}_{specifier}` naming. The shared "Achievement unlocked" copy lifts into a single `room_achievement_callout_heading` key reused by both the MP inline callout and the single-unlock celebration sheet title — same English, same semantic role, splitting them would only invite drift. The `rewardSummary` `buildString` interpolation collapses into two resource keys: `room_achievement_reward_xp` (XP-only path) and `room_achievement_reward_xp_plus_chips` (`+%1$d XP · +%2$s chips` for the chip-bonus path) — splitting at the conditional rather than concatenating " · +X chips" onto a base string keeps each whole English construction in the catalog, so a future voice-pass can tune the chip-bonus phrasing without cracking open the builder. `AchievementCelebrationSheet`'s now-unused `rewardSummary(xp, chips)` helper got dropped — the two-branch resolution lives at the callsite. The XP-bubble glyph "✦" routed through `room_xp_bubble_glyph` even though it's a typography choice rather than English copy — the bubble's `+X XP earned` construction is text-rendered, so the glyph belongs in the catalog for consistency. The 🎁 cosmetic icon stays inline at both callsites (identity glyph, not user-translatable copy). The "—" no-hand placeholder in `ShowdownRow` migrated as `room_showdown_row_no_hand` even though it's a symbol; same call as the stepper glyphs in the prior PlayerActionSheet commit. Money amounts (`humanWinAmount`, `totalPot`) route through `%1$s` + `.toString()` at the callsite for cross-platform consistency — same pattern as the prior `room_stack_explainer_title` slice; `formatThousands(winAmount)` continues to feed the `room_showdown_row_win_amount` string in the per-seat row.
**Reviewer notes:** `:features:room:impl:compileDebugKotlinAndroid` + `:features:room:impl:testDebugUnitTest` + `:apps:compose:assembleDebug` all green. All five `ShowdownDialog` / `BustDialog` / `AchievementCelebrationSheet` previews render through the migrated composables. The `AchievementUnlockedCallout` (private to HandResultDialogs) is now lighter — the `buildString` interpolation collapsed into a two-branch `if` calling either `room_achievement_reward_xp` or `room_achievement_reward_xp_plus_chips`. Two cycles ago this surface was still the most-engaged-with copy seam on the table; now it's resource-backed end-to-end.
**Deferred:** (1) `HandRankingsCheatSheet` is the next bounded explainer-family slice (full hand-rankings reference); held back here because the cheat-sheet's hand-by-hand entries pattern-match `:libraries:gameplay`'s `HandRank` enum, and a typed enum-to-resource mapping may want to live in the engine module rather than `:features:room:impl`. (2) `PlayerProfileSheet` (tap-an-opponent profile) — own slice; carries playing-style + difficulty labels that may want the same `BotSpeed`-style enum-side `labelResource()` extension. (3) The bigger room surfaces: `PlayPokerScreen` chrome (loading / error / reconnect banner copy), `OpponentsRow`, `PlayerArea`, `BoardArea`, `TableActionBar`, `EmojiTray`, the tutorial family. Each its own slice.

## refactor(room): migrate PlayerActionSheet copy to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — `PlayerActionSheet.kt` is the most-used UI surface on the bot table: every hand a player acts goes through Fold / All in / preset bet pills (Min / ½ Pot / ¾ Pot / Pot) / a stepper row (– / +) / a confirm pill that toggles between "Bet" and "Raise to" depending on whether the street has an open bet. 11 inline strings across the action grid, the bet-preset catalog, the stepper buttons, and the confirm pill — bigger than the prior explainer slices but still tightly bounded to a single composable.
**Approach:** Added the `room_action_*` family under the existing `{surface}_{role}_{specifier}` naming. The `BetPreset` data class lifted its `label: String` to `labelResource: StringResource` so the non-Composable `BetPresets.from(legal)` factory can stay non-Composable — the consuming `BetPresetGrid` resolves the resource at the `Text` callsite. Same pattern as the prior cycle's profile-BotSpeed enum split (resource-key-as-data, resolution-at-callsite) so the preset catalog stays a plain function rather than a Composable. **Rejected alternative:** parameterizing `BetPreset` as `(@Composable () -> String)` — would require every preset-catalog caller to live inside a Composable scope and tangles state-management out of the `BetPresets` object. The confirm pill's two verbs ("Bet" vs "Raise to") split into `room_action_confirm_bet` + `room_action_confirm_raise_to` (each interpolating the bet amount as `%1$s`) rather than a single `room_action_confirm` with a separate verb token — splitting at the verb keeps each English construction whole in the catalog, so a future voice-pass can tune "Bet 60" → "Raise to 60" independently (in some languages the verb conjugation depends on the next word). The bet amount lifts via `%1$s` + `raiseTotal.toString()` for the same cross-platform reason as the prior commit's `room_stack_explainer_title`. The "–" / "+" stepper glyphs migrated even though they're glyphs not English — they're rendered as `Text`s here, not icons, so they pattern-match user-visible strings rather than identity emoji glyphs (the literal characters are arguably a typography choice rather than copy, but routing them through the catalog gives a future voice-pass the option without lifting the catalog later).
**Reviewer notes:** `:features:room:impl:compileDebugKotlinAndroid` + `:features:room:impl:testDebugUnitTest` are green. All three `PlayerActionSheetPreview_*` variants (FacingABet / OpenBet / CannotRaise) render through the migrated composable. The `formatCompactChips`'d max-hint and all-in amounts stay raw at the callsite — they're already format-helpers, the resource just receives them as `%1$s` formatted text. The raise-confirm pill renders `raiseTotal.toString()` un-formatted (matches the pre-migration `"$verb $raiseTotal"` interpolation); a future visual-pass that wants thousands-separation there is a separate, opinionated change.
**Deferred:** (1) The remaining big room surfaces — `PlayPokerScreen`, `HandResultDialogs`, `PlayerProfileSheet`, `OpponentsRow`, `PlayerArea`, `BoardArea`, `AchievementCelebrationSheet`, `HandRankingsCheatSheet`, `EmojiTray`, `TableActionBar`, the tutorial family. Each its own future slice.

## refactor(room): migrate Last-action + Hand-label explainers to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — `LastActionExplainer.kt` carried 13 inline strings (six `PlayerAction`-keyed headlines × six bodies plus the cheat-sheet hint footer); `HandLabelExplainer.kt` carried 13 (12 hand-category bodies that branch on prefix-match against the engine's hand label plus the same cheat-sheet hint footer). Both are tap-on-an-affordance explainer dialogs that close out the bounded explainer family in `:features:room:impl` started in the prior commit. Bigger than the other explainers because each carries an inline `when` mapping a sealed type / prefix to copy.
**Approach:** Added the `room_last_action_*` and `room_hand_label_*` families under the existing `{surface}_{role}_{specifier}` naming. The two mapping `when` blocks each split into a paired structure: a `headline(seatName, action)` `@Composable` that resolves the headline through `stringResource(...)` with the `formatThousands`'d amount as a `%2$s` arg, and a non-Composable `explainerResourceFor(action)` that maps the sealed `PlayerAction` subclass to a `StringResource` key — the `Text` callsite then does `stringResource(explainerResourceFor(action))`. Same shape for HandLabelExplainer: a non-Composable `explainerResourceFor(label)` returns the `StringResource` for the matching hand category, and the caller resolves at the `Text`. **Rejected alternative:** resolving the explainer body string fully inside the mapping `fun` (i.e. make the mapping `fun` `@Composable` returning `String`). Splitting the resource key from the resolution call lets the mapping `fun` stay non-Composable + easily unit-testable on the resource-key returned, without dragging in a `@Composable` annotation that prevents calling the mapping from anywhere other than a Composable scope. The `headline` mapping has to be `@Composable` regardless because it interpolates `formatThousands` results at the resource boundary; that asymmetry is fine — the headline IS the user-visible copy, the body is just a key lookup. Two `cheat_sheet_hint` strings under separate `room_last_action_cheat_sheet_hint` + `room_hand_label_cheat_sheet_hint` keys even though both are sibling-identical English today — same call as the lobby cycle's split of `lobby_idle_code_field_label` from `lobby_in_room_code_label` so a future voice-pass can tune one footer without dragging the other. Hand-label prefix match also gained `ignoreCase = true` (the original used `label.lowercase()` then `startsWith` against lowercase literals, which is functionally identical but the inline-lowercase variant ports better through prefix-comparing engine variations).
**Reviewer notes:** `:features:room:impl:compileDebugKotlinAndroid` + `:features:room:impl:testDebugUnitTest` are green. `LastActionExplainer`'s three previews (`_Raise` / `_Fold` / `_AllIn`) and `HandLabelExplainer`'s one preview render through the migrated Composables. The room module is now ~6 explainer dialogs deep on resources — `PlayPokerScreen` is the largest unmigrated screen left, plus the family of action sheets / cheat sheets / opponent/player tiles. Each is its own future slice.
**Deferred:** (1) The larger room surfaces — `PlayPokerScreen`, `HandResultDialogs`, `PlayerActionSheet`, `PlayerProfileSheet`, `OpponentsRow`, `PlayerArea`, `BoardArea`, `AchievementCelebrationSheet`, `HandRankingsCheatSheet`, `EmojiTray`, `TableActionBar`, the tutorial family. Each is its own slice; the bigger ones (e.g. `PlayPokerScreen`) may want to split into a screen-chrome slice + an action-bar slice. (2) The hand-label prefix-match list lives in `:libraries:gameplay` (engine emits strings like `"Pair · Aces"`); a typed `HandCategory` enum could replace the prefix-match dance entirely, but that's a separate refactor from a strings sweep.

## refactor(room): migrate targeted explainer dialogs to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — four small `:features:room:impl` explainer dialogs still carried inline copy: `PotExplainer` (2 strings — title + body), `StackExplainer` (3 — title with stack-count arg + body + practice-chips note), `BetPillExplainer` (2 — title with seat-name + amount args + body), `BlindRolesExplainer` (10 — title + three label/title/description triples for the D / SB / BB chips). All are tap-on-an-affordance explainer modals that fire from a single touchpoint on the table — bounded, low-risk targets for the strings sweep.
**Approach:** Added the `room_pot_explainer_*`, `room_stack_explainer_*`, `room_bet_pill_explainer_*`, `room_blind_roles_*` families under the existing `{surface}_{role}_{specifier}` naming. The two numeric args (`stack: Long` on StackExplainer; `amount: Long` on BetPillExplainer) lifted into `%1$s` placeholders with `.toString()` at the callsite rather than `%1$d` with `stack` raw — Compose Multiplatform resources are stricter than Android's `String.format` on numeric formatting across JVM / Native targets, and `%s` + caller-side `.toString()` is the established cross-platform pattern (matches `home_active_room_subtitle`'s `%1$s` for the room code arg). Trade-off: callers have to remember to format before passing, but the resource never has to know whether the arg is `Int` / `Long` / `String`. BlindRolesExplainer's three role chips lifted their `label` (D / SB / BB), `title` (Dealer / Small blind / Big blind), and `description` triples into separate keys per role — keeps the per-role narrative aligned in one place and makes a future voice-pass surgical (the small-blind / big-blind descriptions are the strings most likely to need clarification). Emoji glyphs (🎲 on BlindRoles) stay inline at the callsite — identity glyphs, not user-translatable copy, same call as every prior cycle.
**Reviewer notes:** `:features:room:impl:compileDebugKotlinAndroid` + `:features:room:impl:testDebugUnitTest` are green. Previews on all four screens render through the migrated Composables. The `room_stack_explainer_title` "You have %1$s chips" preview pins the un-thousands-separated stack format from the original (the helper `formatThousands` was deliberately NOT introduced here — preserving the original visual exactly; if a future visual-pass wants thousands separators that's a separate, opinionated call). Module-level deprecation warnings on tutorial files' `Preview` import paths are pre-existing.
**Deferred:** (1) The mapping explainers (`LastActionExplainer`, `HandLabelExplainer`) — own follow-up slice, planned for the next commit in this cycle. They both carry an inline `when` block mapping a sealed enum / hand-label prefix to a one-line explainer; the mapping shape ports cleanly to the resource catalog but warrants its own commit. (2) The bigger room surfaces (`PlayPokerScreen`, `HandResultDialogs`, `PlayerActionSheet`, `PlayerProfileSheet`, `OpponentsRow`, `PlayerArea`, `BoardArea`, `AchievementCelebrationSheet`, `HandRankingsCheatSheet`, `EmojiTray`, `TableActionBar`) — each is its own slice, scoped out for a future cycle.

## refactor(room): migrate Leave + SwipeFold confirm dialogs to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — `:features:room:impl` is the largest remaining unmigrated user-facing surface. `LeaveBotsConfirmDialog.kt` carried 4 inline strings (title + body + Stay / Leave CTAs); `SwipeFoldConfirmDialog.kt` carried 5 (title + body + "Don't show this again" + Cancel / Fold CTAs). Both are bounded confirm dialogs — a natural opening slice for the room module since they fire as guards around hand-end / leave intents and pattern-match the form/destructive surfaces the prior cycle migrated under `:features:profile`.
**Approach:** Bootstrap the room module on `:libraries:resources` by adding `implementation(projects.libraries.resources)` to `features/room/impl/build.gradle.kts`, then added the `room_leave_bots_*` + `room_swipe_fold_*` families under the existing `{surface}_{role}_{specifier}` naming. Both dialogs use Dialog's emoji top-accessory (🚪 / 🃏) — emoji glyphs stay inline at the callsite, same call as every prior cycle's emoji handling (identity glyphs, not user-translatable copy). Kept the two confirm-button keys distinct (`room_leave_bots_leave_button` vs `room_swipe_fold_confirm_button`) so a future voice-pass can tune the leave action and the fold action separately — same call as the lobby cycle's split of `lobby_idle_code_field_label` from `lobby_in_room_code_label`.
**Reviewer notes:** `:features:room:impl:compileDebugKotlinAndroid` + `:features:room:impl:testDebugUnitTest` are green. Previews on both screens render through the migrated Composables. Module-level deprecation warnings on `org.jetbrains.compose.ui.tooling.preview.Preview` import paths in tutorial / other files are pre-existing — not introduced by this commit.
**Deferred:** (1) The simple explainer dialogs in `:features:room:impl` (`PotExplainer`, `StackExplainer`, `BetPillExplainer`, `BlindRolesExplainer`) — own follow-up slice, pulled into the next commit in this cycle. (2) The mapping explainer dialogs (`LastActionExplainer`, `HandLabelExplainer`) — own follow-up slice, planned for the third commit. (3) The bigger room surfaces (`PlayPokerScreen`, `HandResultDialogs`, `PlayerActionSheet`, `PlayerProfileSheet`, `OpponentsRow`, `PlayerArea`, `BoardArea`, `AchievementCelebrationSheet`, `HandRankingsCheatSheet`, `EmojiTray`, `TableActionBar`) — each is its own slice, scoped out for a future cycle.

## refactor(profile): migrate EditProfileScreen copy to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — `EditProfileScreen.kt` (the largest unmigrated `:features:profile` surface, 745 lines incl. previews) carried ~12 inline user-facing strings across the name/avatar form: hero title + subtitle, Display name field label + range helper ("%d–%d characters"), Avatar color / Avatar section headers, Save / Saving… CTAs, Loading… + load-error states for the pack picker, the "🔒 %s" locked-pack prefix, and the "Get in shop" CTA on locked packs. Closes out the entire user-facing `:features:profile:impl` Composable surface (only VM-side error strings remain, deferred separately).
**Approach:** Added the `profile_edit_*` family under existing `{surface}_{role}_{specifier}` naming. The range-helper string lifts the original `"${MIN}–${MAX} characters"` interpolation into a parameterized `profile_edit_display_name_helper_range` with `%1$d` / `%2$d` args — keeps the constants in sync between resource and the validation logic. Pack name `name: String` comes from server data (`AvatarPack.name` resolved from the catalog endpoint), stays inline at the callsite — translatable copy lives in the server-side catalog row, not in the client resource catalog. The locked-pack header `"🔒 $name"` lifts to `profile_edit_pack_locked_prefix` with `%1$s` so a future voice-pass can change the lock prefix (or drop it entirely) without touching the screen.
**Reviewer notes:** Pre-existing `EditProfileScreen` previews (anonymous-state / signed-up / locked-pack / loading / error) render through the migrated Composable and pick up new copy automatically. `:features:profile:impl:compileDebugKotlinAndroid` + `:testDebugUnitTest` + `:apps:compose:assembleDebug` all clean. The deprecated androidx Preview warnings in this file are pre-existing — AGENTS.md §previews flags migrating those to the multiplatform `org.jetbrains.compose.ui.tooling.preview.Preview`. Held back from doing that swap in this commit — it's a separate, cosmetic concern.
**Deferred:** (1) `state.displayNameError` (`EditProfileState`-side raw `String?`) — same VM-error-string deferral as Lobby / Auth / Claim / Delete VMs. (2) `state.error` (raw `String?` for general submit errors) — same deferral. (3) The androidx-vs-jetbrains Preview-import warning is now repo-wide on every unmigrated profile screen — separate cleanup pass not bundled here. (4) `:features:room` Composable copy is the remaining clean Composable-only target the next strings-sweep cycle can pick up.

## refactor(profile): migrate Claim + Delete account screens to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — `ClaimAccountScreen.kt` carried 8 inline strings (back-button a11y + hero title + subtitle + three provider button labels + coming-soon notice + conflict-switch button with interpolated provider name); `DeleteAccountScreen.kt` carried 13 (back a11y + title + subtitle + "What gets deleted" section title + four bullet items + confirm-phrase input label that interpolates `DeleteAccountState.REQUIRED_PHRASE` + Delete / Deleting… / Cancel CTAs). Both are bounded form/destructive surfaces alongside the Feedback + Bug Report pair the prior commit picked up.
**Approach:** Added the `profile_claim_*` and `profile_delete_*` families under the existing `{surface}_{role}_{specifier}` naming. Two enum-driven decisions noted: (1) `OAuthProvider.label` stays inline at the `profile_claim_conflict_switch_button` `%1$s` placeholder — same call as the prior cycle's BotSpeed.label / TurnFeedback.label deferral; the enum property is wire-key-adjacent and lifting it would dragnet `:libraries:identity` callsites we don't need to touch in a strings sweep. (2) `DeleteAccountState.REQUIRED_PHRASE` (currently `"delete"`) gets interpolated into `profile_delete_confirm_input_label`'s `%1$s` instead of hard-coding the phrase into the resource — keeps the prompt and the VM-side validator in lockstep if the phrase ever changes, even though both will live in English-only land for V1. Emoji glyphs (🔐 / ⚠️) stay inline. Two separate `profile_claim_back_a11y` + `profile_delete_back_a11y` keys ship even though both are "Back" today — same call as prior cycles where shared English copy might diverge under a voice-pass. Module already had `implementation(projects.libraries.resources)` from this cycle's earlier commits — no `build.gradle.kts` churn.
**Reviewer notes:** Existing profile tests still pass; `:features:profile:impl:compileDebugKotlinAndroid` + `:testDebugUnitTest` clean. Previews on both screens render through the migrated Composables.
**Deferred:** (1) `ClaimAccountViewModel` error strings (`"${provider.label} sign-in isn't available yet."`) — same shared ErrorReason vs `suspend resolve` deferral as `LobbyViewModel.error` / `OnboardingViewModel` / `SignInViewModel` / `SignUpViewModel`. All four want one decision. (2) `EditProfileScreen.kt` (745 lines, biggest unmigrated `:features:profile` surface) — own follow-up slice; flagged "sizable" in the todo. (3) `state.error` on both `ClaimAccountState` and `DeleteAccountState` stays raw `String?` per the VM-error deferral.

## refactor(ui): hoist tutorial sparkle + coin gold hues into `PokerPalette`

**Source:** worker-hydrated this cycle (not human-curated).
**Problem:** Two tutorial files carried seven `Color(0xFF…)` literals for the same handful of poker-domain colors: `TutorialReadyScreen.kt` had `0xFFE5B946` three times across the gold radial glow and the four sparkle strokes (one of those strokes' colors is set once and re-used across four `drawLine` calls), and `TutorialNarrationStep.kt`'s `Coin` Composable had a 2-stop radial gradient (`0xFFFFD66B` + `0xFFD9A933`), a `0xFFB68721` border, and a `0xFF3D2A0A` `$` glyph. AGENTS.md DS rules §1/§4: raw `Color(0xFF…)` outside `:libraries:ui/system/color/` is the anti-pattern; semantic poker-object hues belong on `PokerPalette` next to the existing `ChipGold` / `ChipGoldOutline` / `CardBackBlue` / `BlindRed` / `ProgressionCyan` / `ProgressionGreen` family. Lane D mechanical swap with a cheap revert path if direction is wrong.
**Approach:** Added five new `PokerPalette` swatches with sibling docblocks to the existing entries: `SparkleGold` for the tutorial celebration glow (intentionally brighter than `ChipGold` since "ready" celebration shouldn't read as a routine chip stack), and `CoinGradientStart` / `CoinGradientEnd` / `CoinOutline` / `CoinGlyph` for the coin disc. Bundled the coin set under one `Coin*` prefix rather than spelling out each color's role in a long name — pattern matches the `Chip*` prefix for `ChipGold` + `ChipGoldOutline`. Routed all seven callsites through the new tokens. **Rejected alternative:** picking one canonical "tutorial gold" and reusing `ChipGold` everywhere. The two ambers differ by intent — chip gold is the dealer-button + bet-pill workhorse; sparkle gold is the celebratory hit — and merging them would either dim the celebration or brighten every chip pile. Keeping them distinct in the palette preserves both reads.
**Reviewer notes:** Pixel-identical: every new token is the exact same `Color(0xFF…)` value that was inline. Tutorial previews are the visual safety net. `:apps:compose:assembleDebug` clean; no test churn since the engine already covered these helpers via the per-tutorial-screen preview pins.

## refactor(profile): migrate Feedback + Bug Report form copy to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — `FeedbackScreen.kt` and `BugReportScreen.kt` both carried inline copy for shared form vocabulary (Message label + "Describe what happened…" placeholder + Email label + "optional" placeholder + char-counter format + Send / Sending… buttons) plus per-screen titles + hero copy; BugReport additionally carries the "Captured details" SectionCard + "Error code" / "Report id" SummaryRow labels for the auto-attached crash context.
**Approach:** Added separate `profile_feedback_*` and `profile_bug_*` resource families even though seven of the strings (Message / Describe what happened… / Email (so we can follow up) / optional / %d/%d / Send / Sending…) are identical English today. Rationale loud: the two forms target different user moods — feedback is "you have time, tell us your idea" while bug-report is "something broke, give us evidence quickly" — and a future voice-pass may well diverge their tone on the same field. Merging the keys later if a copy-style review rules them identical is cheap (single XML edit + callsite swap); splitting them later if they diverge is more friction. Mirrors the prior cycle's `home_friends_see_all` / `home_recents_see_all` / `home_recent_achievements_see_all` split for the same reason. State-side error strings (`state.errorMessage`) stay raw `String?` — VM-side error-strings deferral is the same one open across LobbyViewModel / Auth VMs / ClaimAccountViewModel.
**Reviewer notes:** Pure migration; `:features:profile:impl:compileDebugKotlinAndroid` + `:features:profile:impl:testDebugUnitTest` are green. Previews on both screens (Empty / Filled / Submitting / Error / CharLimitReached for FeedbackScreen; Empty / WithCapturedContext / Submitting / Error for BugReportScreen) render through the migrated Composables so they pick up new copy automatically.
**Deferred:** `state.contextMessage` on `BugReportState` is still rendered as a raw `String?` — it's set by the VM from crash plumbing and not a translatable phrase (it's a stack trace tail / error description), so leaving inline. ViewModel error strings are the shared VM-error deferral.

## refactor(profile): migrate Notifications + My items screens to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — `NotificationsScreen.kt` had 3 inline Composable strings (top-bar title + empty title + empty body); `MyItemsScreen.kt` had ~10 (top-bar title, two subtitle variants for empty vs populated, two badge labels Earned + Unlocked, "Only you see this" personal-cosmetic tag, Equip / Unequip CTAs, empty title + body). Both surfaces fall in the same `:features:profile:impl` slice that ProfileScreen.kt opened in the prior commit — natural follow-on rather than a separate cycle.
**Approach:** Added the `profile_notifications_*` family (title + empty title + empty subtitle) and the `profile_my_items_*` family (title + two subtitle variants under `_empty` / `_populated` so plural-vs-singular voice can branch later + badge_earned/badge_unlocked + personal_cosmetic_tag + button_equip/button_unequip + empty_title/empty_body). `MyItemsScreen` has two callsites of "Earned" — the `OwnershipBadge` for unequippable items and the `EarnedTag` next to equippable items' subtitle — both land under the single `profile_my_items_badge_earned` key because they convey the same fact, and splitting would only invite drift. Same for the singular vs populated subtitle: the count-aware string uses a `%1$d` placeholder per the established `home_friends_see_all_with_*` plural-branching pattern. Emoji glyphs (🔕 empty state, 🎁 empty state) stay inline — identity glyphs, not translatable copy.
**Reviewer notes:** Pure copy migration; `:features:profile:impl:compileDebugKotlinAndroid` + `:features:profile:impl:testDebugUnitTest` are green. Previews on both screens (Empty / Populated / SingleNoEmojiNoLink / WithMessages) render through the migrated Composables so they automatically pick up the new copy.
**Deferred:** The preview-only `previewInbox()` / `previewPopulatedState()` factories still hold sample copy ("5,000 chips on us", "Maintenance Sunday", "Midnight Blue Felt", etc.) — explicitly out of scope per the strings-sweep todo's "Out of scope: preview-only / test-only strings."

## refactor(profile): migrate ProfileScreen copy to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — `:features:profile:impl/ProfileScreen.kt` carried ~40 inline user-facing strings: five section headers (Account / Gameplay / Support / About / Debug), every list-row headline + supporting line (Notifications + unread variant / My items / Rank + Unranked sentinel / Stats / How to play / Bot speed + 3 picker labels / Your turn feedback + 3 picker labels / Send feedback / Report a bug / Privacy policy / Terms of service / QA menu), Sign out / Delete account buttons (incl. progress label), SignOutConfirmDialog (4 strings × anonymous vs claimed branches × confirm / cancel), profile header (level summary "Level N · X XP" + "X XP to level N+1" + avatar-edit a11y), ClaimAccountCard (title + body + Get started CTA), and the "v%s" app-version footer. Same per-callsite duplication the §A P0 keeps shipping slices against; profile is the natural next surface now that home + lobby + onboarding are done.
**Approach:** Added the `profile_section_*`, `profile_account_*`, `profile_gameplay_*`, `profile_support_*`, `profile_about_*`, `profile_sign_out_*`, `profile_debug_*`, `profile_avatar_*`, `profile_level_summary_*`, `profile_claim_card_*`, `profile_bot_speed_*`, `profile_turn_feedback_*` families under the established `{surface}_{role}_{specifier}` naming. Two enum-driven decisions: BotSpeed / TurnFeedback expose their picker labels via new private `labelResource()` extension functions on each enum (resolved with `stringResource(it.labelResource())` at the callsite), leaving the enum's existing `.label: String` property in `:libraries:cards` untouched — that property is wire-key-adjacent (the enum is `@Serializable` + persisted in `AppData`) and lifting it would dragnet test fixtures + storage round-trip we don't need to touch in a strings sweep. Trade-off: a third Bot Speed value would have to add a row to the `when` and to the resource catalog — easy enough to keep aligned, and the alternative (route every UI rendering of every settings enum through a separate `LocalizedEnumLabel` helper) is over-engineering for two enums. `DropdownAccessory`'s `label: (T) -> String` parameter was lifted to `@Composable (T) -> String` so the lambda can hit `stringResource(...)` directly — the lambda already runs inside `BasicDropdownMenuItem.text`'s `@Composable` content slot. The two `Sign out` button labels (top-level button + dialog primary) land under separate keys (`profile_sign_out_button` vs `_dialog_confirm_button`) so a future voice-pass can tune one without dragging the other — same call as the lobby cycle's split of `lobby_idle_code_field_label` from `lobby_in_room_code_label`. The 👋 emoji on the sign-out dialog top accessory stays inline (identity glyph, not user-translatable copy, same call as every prior cycle's emoji handling). `:features:profile:impl/build.gradle.kts` gained `implementation(projects.libraries.resources)`. Module already had `compose.components.resources` for the existing edit-profile picker.
**Reviewer notes:** Existing profile tests still pass; `:apps:compose:assembleDebug` clean. `@Preview`s in `ProfileScreen.kt` (Anonymous / Claimed / SigningOut / WithUnreadNotifications / DebugBuild) render through the same screen Composable so they pick up migrated copy automatically. The deprecated androidx Preview warnings the compiler now surfaces on `EditProfileScreen` / `FeedbackScreen` / `MyItemsScreen` / `NotificationsScreen` are pre-existing — not introduced by this commit; the rest of the profile screens use the deprecated `androidx.compose.ui.tooling.preview.Preview` import that AGENTS.md §previews flags for migration. ProfileScreen.kt itself uses the multiplatform preview import already (fully qualified `org.jetbrains.compose.ui.tooling.preview.Preview` at line 558+).
**Deferred:** (1) Rest of `:features:profile` — NotificationsScreen / MyItemsScreen / FeedbackScreen / BugReportScreen / EditProfileScreen / ClaimAccountScreen / DeleteAccountScreen — each is its own follow-up slice; pulled the easy ones into this same cycle as subsequent commits. (2) `ClaimAccountViewModel` error strings (`"${provider.label} sign-in isn't available yet."`) — same `ErrorReason` enum vs `suspend resolve` deferral as `LobbyViewModel.error` / Auth VM error strings, all four need one shared resolution. (3) The `BotSpeed.label` / `TurnFeedback.label` properties in `:libraries:cards/AppCache.kt` — used as defaults / serialized values, leaving alone per the wire-key deferral pattern.

## test(server): pin GameSessionRegistry → GameEventWriter wiring

**Source:** worker-hydrated this cycle (not human-curated).
**Problem:** The B0 persistence commit threaded `GameEventWriter` from `InMemoryGameSessionRegistry` into every `GameSession` it creates. The session-level tests pin the writer contract end of that flow, but there was no regression-pinning test at the registry boundary itself — a future refactor could silently drop the `eventWriter = eventWriter` arg from `GameSession(...)` inside the registry's `startHand`, and the only signal in production would be empty `game_events` rows. Mechanical Lane A gap.
**Approach:** Added a `registry_routesEventWriter_intoEverySessionItCreates` test against a `RecordingGameEventWriter` that asserts (a) every session minted by the registry gets a unique UUID, (b) events from `startHand` land in the writer tagged with the matching session's id, and (c) two sessions for two different room codes don't cross-tag each other's event rows. Also lifted three test-helper writers (`RecordingGameEventWriter`, `ExplodingGameEventWriter`, `FlippableGameEventWriter`) out of `GameSessionTest.kt`'s private scope into a shared internal `TestGameEventWriters.kt` since this cycle's two test files both want them — the alternative of duplicating the class was a clear footgun if their contracts ever diverged.
**Reviewer notes:** Pure test addition + helper extraction; no production code touched. `:apps:server:test` is green on the full game-package run.

## refactor(home): migrate Home rump (banners + dialogs) to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — four `:features:home:impl` Composables still carried inline copy: `ActiveRoomBanner` (~9 strings inc. room-code-interpolated subtitle + forfeit-confirm dialog), `TutorialBanner` (~4 strings inc. dismiss-icon a11y), `WelcomeDialog` (~5 strings — greeting, gift / chip-use / daily-grant lines, primary CTA), `BotTableSetupDialog` (~12 strings — title + four seat-count-specific subtitle variants + Difficulty/Seats labels + two seat-summary variants + balance disclaimer + Start CTA). Same per-callsite duplication the §A P0 keeps shipping slices against.
**Approach:** Added the `home_active_room_*`, `home_tutorial_banner_*`, `home_welcome_dialog_*`, `home_bot_setup_*` families under the established `{surface}_{role}_{specifier}` naming convention. The Casual / Standard / Challenging difficulty option labels stayed inline at the `OptionPillRow` callsite — those strings double as wire keys consumed by `PlayPokerFeatureEntryPoint`'s downstream routing, so resolving them to localized labels needs a typed `BotDifficulty` enum (out of scope for a strings sweep). `subtitleFor(seatCount, difficulty)` and the inline `${seatCount - 1} bots, …` expression both got lifted to `@Composable` helpers so they can hit `stringResource(..., args)` directly — splitting the four subtitle variants and the two seat-summary variants into separate resource keys keeps each English construction in the catalog instead of `if (n == 2) "heads-up" else "$n seats"` in code, and gives the future Spanish row room to decline `seat → asiento` per branch. "Forfeit" appears twice (banner button + dialog primary) and lands under separate keys (`home_active_room_forfeit_button` + `_forfeit_dialog_primary`) — same call as the lobby cycle's split of `lobby_idle_code_field_label` from `lobby_in_room_code_label` so a future voice-pass can tune one without the other. Emoji glyphs (⏳ / 🔑 / 🤖) stay literal at the callsite — same identity-glyphs-aren't-translatable line as the home shelves + onboarding cycles.
**Reviewer notes:** Module already had `implementation(projects.libraries.resources)` from a prior cycle — no `build.gradle.kts` churn. Resource imports use `cards.libraries.resources.generated.resources.*` (the auto-generated module package), not `com.dangerfield.cards.libraries.resources.*` — caught and fixed in the same cycle, noted here in case the reviewer wants to script the path elsewhere. Existing home tests + `:apps:compose:assembleDebug` clean. The two `home_welcome_dialog_chip_use_line` + `_gift_line` keys ship under sibling-not-merged names so a future copy-tweak that wants to change one line without the other doesn't have to split the key first.
**Deferred:** `:features:profile` + `:features:room` + the V1 `:libraries:ui` snackbar/dialog/banner copy + the VM-side `LobbyState.error` / `SignInState.error` / etc. raw-`String` deferrals are all still in the todo strings-sweep entry. Also deferred: typing the `BotDifficulty` wire key so the Casual/Standard/Challenging labels can move to resources — would touch `PlayPokerFeatureEntryPoint` + a couple of downstream consumers, bigger than a strings sweep.

## feat(server): persist GameSession events to Postgres before broadcast

**Problem:** Todo §B0's first bullet — every emitted `GameEvent` needs to land in the durable `game_events` log before the in-memory `MutableSharedFlow<GameEvent>` broadcast, transactionally with the state mutation, so server restart retains hand history and the in-memory `StateFlow<GameState>` becomes a derived view. V31 provisioned the table last cycle; this cycle wires the producer end of the pipeline.
**Approach:** Added `GameEventWriter` interface (with `NoOp` for tests) + `PostgresGameEventWriter` impl that writes the v1 `GameEventEnvelope` JSONB via a single transaction per intent (events from one engine call are all-or-nothing). `GameSession` got a UUID `id` + writer dependency; both `startHand` (and its private `requestNextHand`-driven `startHandLocked` sibling, lifted to `suspend`) and `applyIntent` now call `eventWriter.append(id, result.events)` *before* mutating `_state.value` or emitting to `_events` — the write-before-broadcast invariant the event-sourced architecture leans on. Writer failure: caught and returned as `IntentResult.Rejected("persistence failed: …")` with state untouched and no broadcast, so a DB blip looks to the client like a transient action-rejection rather than partial-state corruption. Also added a `jsonb(...)` Exposed column type in `:apps:server` because text→jsonb has no implicit Postgres cast — the writer wraps payloads in `PGobject(type="jsonb")` and reads pull `value` back out as `String`. **Rejected alternative:** wiring per-event (one transaction each) would risk partial-event log state if event N+1 fails mid-batch; batching all events from a single engine call is the cleaner invariant.
**Reviewer notes:** The crash-mid-hand replay equivalence test from the original B0 acceptance criterion is deferred — the replay function (event log → live `GameState`) doesn't exist yet and likely lands alongside B1's snapshot path. Rewrote the todo entry to slice that off as remaining work. The PK uniqueness + per-intent atomicity Postgres invariants are pinned by `(session_id, seq)` in the migration; an integration-test pin via `assertFailsWith<ExposedSQLException>` would not catch the throw cleanly across `newSuspendedTransaction`'s coroutine boundary (the exception leaks out to `runTest` even with a try/catch on the suspending caller) so the tests focus on the round-trip + isolation paths. `InMemoryGameSessionRegistry` constructor gained one required `GameEventWriter` arg — three test sites updated to pass `GameEventWriter.NoOp`. Direct test pre-existing failure: none on the suite I ran; full `:apps:server:test` is green including the Postgres-backed `PostgresGameEventWriterTest`.
**Deferred:** Replay-equivalence test (now its own B0 bullet in `docs/todo.md`). B0 sub-items B1 (snapshot compaction), B2 (WS reconnect upgrade), B3 (persisted room membership), B4 (event-sourced gameplay items) — all untouched and still in the todo. The `id: UUID` on `GameSession` is currently process-local (regenerated on each restart) — once B1 + B3 land, the registry will need to either persist a `room_code → session_uuid` mapping or promote sessions to a `sessions` table that the registry hydrates from at boot.

## refactor(ui): swap tutorial pill `RoundedCornerShape(999.dp)` for `Radii.Round.shape`

**Source:** worker-hydrated this cycle (not human-curated).
**Problem:** Four tutorial-screen callsites used `RoundedCornerShape(999.dp)` — the "infinite radius = pill" trick — while `Radii.Round = CornerSize(percent = 50)` already exists in `:libraries:ui` for exactly this. AGENTS.md DS rule §5 says corner radii come from `Radii` tokens.
**Approach:** Mechanical swap. `TutorialPokerScreen` lines 380/382/423 and `TutorialNarrationStep.kt:452` → `Radii.Round.shape`. `TutorialPokerScreen` keeps the `RoundedCornerShape` import for its remaining `RoundedCornerShape(2.dp)` callsite at line 314; `TutorialNarrationStep` drops the import. Visual: pill shape is unchanged (50% of height ≈ 999.dp on heights < 2000dp).
**Reviewer notes:** None. Tutorial previews are the visual safety net.

## refactor(ui): hoist LevelPill progression hues into `PokerPalette`

**Source:** worker-hydrated this cycle (not human-curated).
**Problem:** `LevelPill.kt` hardcoded `Color(0xFF4FC3F7)` (twice — gradient start + outer ring) and `Color(0xFF66BB6A)` (gradient end). The block comment at lines 211-215 explicitly flagged the next step: "a `PokerPalette` entry for 'progression cyan' would let both lift off the literal." AGENTS.md DS rule §1/§4 — raw `Color(0xFF…)` outside `:libraries:ui/system/color/` is the anti-pattern. `LevelProgressGradient.kt` shared the same two literals.
**Approach:** Added `PokerPalette.ProgressionCyan` + `PokerPalette.ProgressionGreen` next to existing brand swatches (chip-gold, card-back-blue), routed `LevelPill`'s gradient + `RING_HUE` and `LevelProgressGradient` through them, dropped the apologetic block comment. Two callsites lifted off the literal in one sweep.
**Reviewer notes:** Pixel-identical — both tokens are the exact same `Color(0xFF…)` values that were inline. Preview pins on `LevelPill` are the visual safety net.

## refactor(lobby): migrate LobbyScreen copy to `:libraries:resources`

**Problem:** `LobbyScreen.kt` carried ~20 inline user-facing strings (top-bar titles, idle hero, create + join CTAs, in-room code surface, member list, start/leave CTAs, connection-status banner, seat label) — the same per-callsite duplication the docs/todo §A strings sweep names as the V1 quality bar. The shop module is already migrated; lobby was the next clean Composable-only surface.
**Approach:** Added the lobby string family (`lobby_topbar_*`, `lobby_idle_*`, `lobby_in_room_*`, `lobby_connection_*`) under existing naming convention `{surface}_{role}_{specifier}` and routed every Composable callsite through `stringResource(...)`. `LobbyScreen.kt` no longer carries any inline copy; `ConnectionStatusRow` resolves its label off the sealed `ConnectionStatus`. Added `implementation(projects.libraries.resources)` to `features/lobby/impl/build.gradle.kts`. Two "Room code" instances were intentionally split into `lobby_idle_code_field_label` + `lobby_in_room_code_label` so a future voice-pass can tune one without dragging the other.
**Reviewer notes:** Existing lobby tests still pass; `:apps:compose:assembleDebug` clean. No `@Preview` was touched — they render through the same screen Composable so they get the migrated copy automatically.
**Deferred:** `LobbyViewModel.kt`'s error-state strings (currently raw `String` written into `LobbyState.error`) need either a typed `ErrorReason` enum + screen-side `stringResource(...)`, or a `suspend getString(...)` resolution in the VM before the `String` lands in state. Both shapes are valid; picking one is a small judgement call I'm leaving for the reviewer / human to direct. Noted in the updated `docs/todo.md` strings-sweep entry under "Remaining work."

## refactor(home): route ComingSoon sheet copy through `:libraries:resources`

**Problem:** `HomeFeatureEntryPoint.kt` carried four inline "coming soon" blurbs (Quick Match / Friends / Friend requests / Recently played with) — each a multi-sentence body the user reads on a sheet. Same per-callsite duplication the strings-sweep §A P0 names, and one of the few remaining unconverted user-facing surfaces in `:features:home`.
**Approach:** Added the `home_coming_soon_*` family (`quick_match` / `friends` / `friend_requests` / `recently_played` × `title` + `body`) under the existing `{surface}_{role}_{specifier}` naming convention. Pre-resolved all eight strings in the `screen<HomeRoute> { }` Composable scope and passed the resolved values into each `ComingSoonContent(...)` construction at the callsite — the `ComingSoonContent` data class stays raw-`String`-shaped because it's an internal payload, not a Composable. Emoji glyphs (⚯ / 🤝 / 🃏) stay as literals at the callsite — they're identity glyphs, not user-translatable copy, same call as the V25-V30 pack icons. Added `implementation(projects.libraries.resources)` to `features/home/impl/build.gradle.kts`.
**Reviewer notes:** Existing home tests pass; `:apps:compose:assembleDebug` clean. The two `🤝` reuses (Friends sheet + Friend requests sheet) are intentional — both belong to the friend-system family. The 🤖 / 🤝 / ⚯ glyphs could move into the catalog later if previewing them across locales becomes useful, but no consumer cares about translating an emoji glyph today.
**Deferred:** The rest of `:features:home` (HomeScreen, FriendsStrip, RecentlyPlayedWithStrip, ActiveRoomBanner, TutorialBanner, WelcomeDialog, BotTableSetupDialog, SectionHeader) still has inline copy — each is a separate slice the next cycle can pick up. Updated `docs/todo.md` strings-sweep entry to reflect the new "rest of `:features:home`" rump.

## feat(catalog): seed Disciplined emote pack as fifth unlock-only pairing

**Problem:** Four unlock-only emote packs shipped (Eliminator / Baller / Iron Stack / Convincer); the todo §A "More emote / blast-pack unlocks" P2 names three candidates for the next pairing. `AchievementId.GOOD_FOLD_25` ("Disciplined", RARE, EITHER, mystery) currently grants 200 XP only — leaving it without a cosmetic complement to the just-shipped Convincer pack, even though the todo explicitly named it as the bluff-energy counterweight.
**Approach:** Picked the `GOOD_FOLD_25 → emotes_disciplined` pairing — todo's third candidate. Rationale loud in the V30 migration header: discipline narrative (hindsight-correct folds) maps to a patient/observant emote set, complements the Convincer pack's bluff energy, and the new emoji set (🧘 🦉 👁️ 🪞) doesn't overlap any prior pack. Rejected the other two candidates: `BEAT_*_10` per-bot packs need five new packs and five new client emoji sets to ship together to avoid an inconsistent half-rollout (bigger judgement call than this cycle warrants); `SHOW_*` rare-hand packs collide with the existing fierce / royal purchasable packs on suit/fierce emoji semantics. Pack icon emoji is 🦉 (unique — owl reads as watchful patience) instead of the achievement's own 🧘 (which collides with `NO_BUST_50`'s icon); 🧘 still ships *in* the emoji set so the unlock callout pattern-matches. Wired the four touch points the V29 commit established: migration (V30), client `EmojiPackCatalog.PackEmojis`, server `ClientGrantableAchievements.Default.clientGrantable`, client `cosmeticRewardFor`. Tests added in all three test surfaces (`EmojiPackCatalogTest`, `EarnableCosmeticsTest`, `GrantsRoutesTest`) following the prior pack's test shape.
**Reviewer notes:** Pre-existing `PostgresProfileRepositoryTest` failure in `:apps:server:test` is environmental (Docker / Testcontainers couldn't start `postgres:16-alpine` on this run) — unrelated to this change; the targeted `GrantsRoutesTest` rerun is green. Pack title / subtitle / description copy is en + es per the V29 pattern; if Spanish doesn't survive a future voice-pass it's a single-row UPDATE, no migration rewind. The `🧘` reuse with the achievement icon is intentional (mirrors the family) but listed here in case the reviewer wants a unique-only set — easy swap, no callsite churn.

## refactor(onboarding): migrate OnboardingScreen copy to `:libraries:resources`

**Problem:** `OnboardingScreen.kt` carried ~25 inline user-facing strings across Welcome / PickIdentity / HowItWorks steps — the same per-callsite duplication the strings-sweep §A P0 names as the V1 quality bar. Shop and Lobby are already migrated; onboarding's main screen was the next contained Composable-only surface.
**Approach:** Added the `onboarding_welcome_*`, `onboarding_identity_*`, `onboarding_how_*` families under the existing `{surface}_{role}_{specifier}` naming convention and routed every Composable callsite through `stringResource(...)`. Helpers that take a raw `String` (`SectionLabel`, `Icons.Pencil`-style content-desc, `AvatarCircle.name` placeholder) consume `stringResource(...)` at the callsite — kept the helpers raw-string-shaped so they remain testable from previews. Added `implementation(projects.libraries.resources)` to `features/onboarding/impl/build.gradle.kts`.
**Reviewer notes:** Existing onboarding tests still pass; `:apps:compose:assembleDebug` clean. `@Preview`s render through the same screen so they pick up migrated copy automatically. Only Composable-side copy moved — `AuthScreens.kt` and the VM-side error strings (raw `authError` / `saveError` typed as `String`) stayed inline; same architectural call as the Lobby cycle's deferral and now noted that way in the todo.
**Deferred:** (1) `AuthScreens.kt` (sign-in / sign-up / verify-email surfaces) — own follow-up commit, ~30 more strings. (2) `OnboardingViewModel` / `SignInViewModel` / `SignUpViewModel` error-state strings — same `ErrorReason` enum vs. `suspend resolve` judgement call as the deferred `LobbyViewModel.error`; one decision should land both. Both noted under "Remaining work" in the updated `docs/todo.md` strings-sweep entry. Reviewer please triage whether the two VM-error deferrals get bundled into one shared resolution this cycle or stay parked.

## test(achievements): pin AchievementMode filter across bots / MP hands

**Source:** worker-hydrated this cycle (not human-curated).
**Problem:** `AchievementRepositoryImpl.modeAllows()` already filters `BOTS` / `MULTIPLAYER` achievements off the wrong-mode hands, and `ClientGrantableAchievements.Default.serverWitnessed` blocks self-grants on the server, but neither half had a regression-pinning test. A future refactor could silently drop the client-side filter and let a player self-grant `FIRST_BUST_DEALT_MP` from a bot table — the server 403 is the only remaining guardrail.
**Approach:** Added three tests against the existing fake DAO + counter-preload scaffolding: MP achievement criterion-met in a BOTS hand → not earned; same achievement in an MP hand → earned; BOTS achievement criterion-met in an MP hand → not earned. The `summary(...)` factory grew an optional `mode = XpMode.BOTS` parameter — every existing call defaults the same way, no other tests change. Picked `FIRST_BUST_DEALT_MP` + `BEAT_JANE_10` as the representative pair because both criteria are single-counter custom keys with a target of 1/10 (cheap to preload) and they ship as the canonical mode-gated entries in the V1 registry.
**Reviewer notes:** None. Tests run under `testDebugUnitTest` clean. The shipped behavior was already correct — these tests just pin it so a future "drop modeAllows since the server enforces it anyway" wouldn't slip through review.

## feat(catalog): seed Grinder emote pack as sixth unlock-only pairing

**Problem:** Five unlock-only emote packs shipped (Eliminator / Baller / Iron Stack / Convincer / Disciplined); the todo §A "More emote / blast-pack unlocks" P2 names the candidate list. V30's commit footer rejected `BEAT_*_10` (would need five packs in one ship) and `SHOW_*` (emoji collisions with `emotes_fierce` / `emotes_royal`). The single largest XP-only achievement left was `AchievementId.HANDS_1000` ("Grinder", EPIC, EITHER mode, icon 🏆) — every other EPIC tier in the registry has a cosmetic complement; HANDS_1000 was the outlier.
**Approach:** Picked the `HANDS_1000 → emotes_grinder` pairing — direction loud in the V32 migration header: 1,000 hands is a real time investment, the "grinder" archetype (steady, slow, chipping away) deserves a blast set the player can use to signal "still here, still working." Set is ☕ ⛏️ 🛠️ ⌛ — none overlap any of the nine prior pack sets or any achievement icon (rejected 🐢 because it's `BEAT_STEVE_10`'s icon — would muddy the meaning; rejected 📊 because it's `DOUBLE_UP`'s icon). Pack icon is ⛏️ (pickaxe — the most evocative grinder glyph; deliberately different from the achievement's own 🏆 so the unlock callout reads as a new cosmetic, not a duplicate medallion). Wired the four touch points the V25–V30 commits established: migration (V32), client `EmojiPackCatalog.PackEmojis`, server `ClientGrantableAchievements.Default.clientGrantable`, client `cosmeticRewardFor`. Tests added in all three test surfaces (`EmojiPackCatalogTest`, `EarnableCosmeticsTest`, `GrantsRoutesTest`) following the prior pack's test shape. HANDS_1000 stays in `clientGrantable` (not `serverWitnessed`) because the counter is on-device today; mode is `EITHER` so a bot-table or MP-table 1000th hand both unlock the pack — the achievement is "you played a lot," not "you played a lot of bot hands."
**Reviewer notes:** Pre-existing `PostgresProfileRepositoryTest` failure path (Testcontainers / Docker) wasn't exercised on this run — targeted `GrantsRoutesTest`, `EmojiPackCatalogTest`, `EarnableCosmeticsTest` reruns are green. Pack title / subtitle / description copy is en + es per the V29-V30 pattern; if Spanish doesn't survive a future voice-pass it's a single-row UPDATE, no migration rewind. Sort order 390 sits directly after V30 (380) so the unlock-only emote packs are visually grouped in catalog order.
**Deferred:** Next pack candidates updated in the todo entry (`CHALLENGING_10_WINS` / `DOUBLE_UP` / `REACH_LEVEL_25` — each with its trade-off noted) so the next worker / human can pick from a more concrete shortlist.

## refactor(home): migrate Home shelves + CTA cards to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — the Home tab's main composables still carried ~22 inline strings: HomeScreen ("Take a seat" + the three HomeCtaCard title / subtitle / trailing triples), FriendsStrip ("Friends" + empty state + four see-all-label branches), RecentlyPlayedWithStrip (section title + "See all" + empty-state + two suggest pills + "Sent" / "Add friend"), RecentAchievementsStrip (section title + "See all"). The ComingSoonSheet entries migrated last cycle so this slice closes the Home shelf-rendering surface.
**Approach:** Added the `home_section_*` / `home_cta_*` / `home_friends_*` / `home_recents_*` / `home_recent_achievements_*` families under `{surface}_{role}_{specifier}`. Lifted `seeAllLabel(onlineCount, pendingRequests)` in `FriendsStrip.kt` from a plain `fun` to a `@Composable` so the four branches resolve via `stringResource(Res.string.…, ...)` directly — splitting the four cases into separate resource keys (no online / one-request / many-requests / online-count) keeps the singular-vs-plural English copy in the catalog instead of `if (n == 1) … else …` in code, and lets the future Spanish row decline `request → solicitud` per branch. Emoji glyphs in HomeCtaCard (🤖 / 👯‍♂️ / ⏳) stay inline — same call as prior cycles (identity glyphs, not user-translatable copy).
**Reviewer notes:** Existing home tests still pass; `:apps:compose:assembleDebug` + `:features:home:impl:testDebugUnitTest` clean. Module already had `implementation(projects.libraries.resources)`, no `build.gradle.kts` churn. `SectionHeader.kt` is unchanged — it's a generic primitive whose copy comes from callers, so the migration was on the call sites, not the primitive. Two `See all` keys ship under separate names (`home_friends_see_all` + `home_recents_see_all` + `home_recent_achievements_see_all`) so a future voice-pass can tune one without dragging the other two — same call as the lobby cycle's split of `lobby_idle_code_field_label` from `lobby_in_room_code_label`. Trade-off: three keys for one English word today; cheap to merge if a future style-guide rules they should always be identical.
**Deferred:** Rump of `:features:home` (HomeHeader, ActiveRoomBanner, TutorialBanner, WelcomeDialog, BotTableSetupDialog, ComingSoonSheet, HomeCtaCard's own labels) — each can land as its own slice in a future cycle. Notes updated on the strings-sweep todo entry. Per-default `defaultOnlineFriends()` / `defaultRecentOpponents()` fake names ("Jordan", "Priya", "Marcus", "Patrice", "Jules", "Omar") are preview-only / V1-placeholder sample data — explicitly out of scope per the strings-sweep todo's "Out of scope: preview-only / test-only strings."

## refactor(onboarding): migrate AuthScreens copy to `:libraries:resources`

**Problem:** Strings-sweep §A P0 — `AuthScreens.kt` carried ~30 inline user-facing strings across SignIn / SignUp / VerifyEmail (titles + subtitles + OAuth row + submit / link CTAs + claim-progress dialog four-line set + five `VerifyEmailState.Banner` variants + shared field labels). The prior cycle deferred this surface as its own follow-up; the now-migrated `OnboardingScreen.kt` is the template.
**Approach:** Added the `auth_*` family (`auth_back_button` / `auth_field_email_label` / `auth_field_password_label` shared, plus `auth_sign_in_*` / `auth_sign_up_*` / `auth_verify_email_*` per-screen) under the existing `{surface}_{role}_{specifier}` naming. The five sealed `Banner` variants fold into a small `bannerResource(Banner) → (StringResource, ColorResource)` Composable helper — keeps the sealed-when mapping in one place so a future Banner variant can't drift on copy. `PasswordField`'s `label: String = "Password"` default lifted to `label: String = stringResource(Res.string.auth_field_password_label)` — Compose allows `@Composable` calls in default-parameter expressions of a `@Composable fun`. The `📧` glyph in `VerifyEmailScreen` stays as a literal at the callsite — same call as the Home cycle's emoji-glyphs-aren't-translatable line. Module already had `implementation(projects.libraries.resources)` from the prior cycle, no `build.gradle.kts` churn.
**Reviewer notes:** Existing onboarding tests still pass; `:apps:compose:assembleDebug` clean. Previews render through the migrated screen Composables so they pick up the new copy automatically. Suppressed `DEPRECATION` on the `bannerResource` return type because `ColorResource` is the type the `Text(color = ...)` overload still takes — same situation every screen lives in until the DS migrates off `ColorResource` wholesale.
**Deferred:** (1) `state.error` on `SignInState` / `SignUpState` is still a raw `String` written by the VM — same `ErrorReason` enum vs. `suspend resolve` judgement call deferred two cycles ago across `LobbyViewModel.error` / `OnboardingViewModel` / `SignInViewModel` / `SignUpViewModel`. One decision should land all four; left for the reviewer / human to triage. (2) Now that AuthScreens is migrated, the rump of `:features:home` (HomeScreen, FriendsStrip, RecentlyPlayedWithStrip, ActiveRoomBanner, TutorialBanner, WelcomeDialog, BotTableSetupDialog, SectionHeader) is the next clean Composable-only slice — pulled into this same cycle as the next commit.

## feat(server): provision game_events table + transport-level seq mirror

**Problem:** Todo §B0 first bullet — the event-sourced multiplayer migration path starts with a durable surface (`game_events(session_id UUID, seq BIGINT, occurred_at, event_type, event_jsonb)`) plus a top-level `seq` field on the `GameEventOccurred` WS frame so the persisted row and the transport message share an explicit cursor. Last cycle landed the kotlinx `GameEventEnvelope(v, type, payload)` carrier; this cycle provisions the JSONB destination.
**Approach:** Added `V31__game_events.sql` matching the spec verbatim (composite PK `(session_id, seq)` + secondary index on `session_id` alone for "give me this session's recent tail" replay), plus an Exposed projection `GameEventsTable` in `Tables.kt` (JSONB surfaced as text — same trick as `products.title_by_locale`). `GameEventOccurred` gains `seq: Long` on both server `RoomSocketEventDto` and client `:libraries:rooms:impl` mirror — the broadcast in `RoomSocketRoutes.kt` reads it off the existing `GameEvent.sequence`. Held back: no producer wired (next B0 bullet), no FK to a parent `sessions` table (the parent shape lands with B0 bullet 2 / B1). `event_type` mirrors the kotlinx `@SerialName` discriminator so SQL filtering ("show me every HandComplete on session X") works without unmarshalling JSONB.
**Reviewer notes:** Wire-compat: `GameEventOccurred` JSON gains a top-level `"seq"` field — pre-launch, no clients in the wild. `RoomSocketRoutesTest` continues to pass (it doesn't pin frame shape on `game_event` variants). `DatabaseSchemaTest` was updated with the new selectAll-on-empty smoke check; the Testcontainers-backed run is green on this machine. Picked `UUID` for `session_id` over the existing 6-char room `code` string per the spec — the next bullet has to either promote `GameSession` to a UUID-stamped row when it first writes, or maintain a `code → session_uuid` map. Either resolution is contained to the producer; flagged so reviewer sees the seam.
**Deferred:** B0 bullet 2 (writing through the per-session mutex). Untouched: `event_jsonb` write path, `GameEventEnvelope` round-trip through the JSONB column, and the bootstrap-from-Postgres read path (B1).

## feat(gameplay): add v1 GameEvent envelope for future persistence

**Problem:** Once `game_events.event_jsonb` rows start landing (B0 first bullet), every row is forever — a future v2 reader must fork on a stable version discriminator without rewriting history. AGENTS.md / docs/decisions.md (2026-05-27 MP architecture revisit) call this out as the first prereq before any event-log persistence ships.
**Approach:** Added `GameEventEnvelope(v, type, payload)` + `toEnvelope()` / `toGameEvent()` helpers next to `GameEvent` in `:libraries:gameplay`, with `GameEventEnvelopeJson` (`classDiscriminator = "type"`, `encodeDefaults = true`). The envelope lifts kotlinx's polymorphic discriminator out of the payload into the outer `type` field — payload stays clean, version-aware reader dispatches on the envelope. Also baked stable `@SerialName` values on every `GameEvent` and `PlayerAction` subclass so the on-disk discriminator survives any future class rename. The WS wire format keeps using the bare kotlinx polymorphic JSON (discriminator value changes from the FQ class name to the new short `@SerialName`, no other shape change). Rejected: building a parallel envelope-only discriminator registry — keeping it in sync with kotlinx is a footgun, and lifting the existing kotlinx discriminator costs nothing.
**Reviewer notes:** Wire compat: the WS discriminator value changes from `"com.dangerfield.cards.libraries.gameplay.GameEvent.HandStarted"` → `"HandStarted"`. Pre-launch, no clients in the wild. New `GameEventEnvelopeTest` pins the v1 layout + round-trip for every variant; existing `RoomSocketRoutesTest` continues to pass (it deserialises by type, doesn't pin the discriminator string).

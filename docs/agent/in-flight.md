## feat(cards): tag every EarnableCosmetics entry with a CosmeticTier

**Problem:** Catalog gating todo called for tagging every `EarnableCosmetics.kt` pairing with `EARN_ONLY / EARN_OR_BUY / BUY_ONLY` so shop / inventory surfaces can render the right badge.
**Approach:** Added `CosmeticTier` enum (only `EARN_ONLY` + `EARN_OR_BUY` — `BUY_ONLY` has no `EarnableCosmetics` entry by definition) and a `tier` field on `CosmeticReward`. Tagged all 18 V1 pairings `EARN_ONLY` because every matching `productId` ships `unlock_only = TRUE` server-side (V17 / V20 / V27–V34 migrations). The 18-id pinning test fails if a row's server tier flips to `unlock_only = FALSE` without a matching client-side tier flip — kept as the alignment guarantee. The audit half of the original todo (decide keep / raise-the-bar / retire across the 18) checked out as keep-all: no single-showdown RNG entries remain post the 2026-05-29 retire, and the rest sit on volume / skill / personality-mastery axes per the spec direction.
**Reviewer notes:** I sliced the todo: tagging shipped, the badge-rendering follow-up stays in `docs/todo.md` with a clear acceptance ("Earned" pin in inventory, achievement-badge alongside the EARN_OR_BUY earned instance). Re-slice if the reviewer wants the badge wired in the same PR rather than next cycle. The `EARN_OR_BUY` tier has zero entries today — it's load-bearing once the heat-map widget / W/L-odds display seed entries land.
**Deferred:** Badge rendering on shop product cards + inventory item rows (rewritten in `docs/todo.md`); first EARN_OR_BUY seed entries (gated on the heat-map and W/L-odds widget items, no movement on those).

## docs(agent): strings-enforcement recommendation as a hydrator proposal

**Problem:** `docs/todo.md` §C carried the worker-investigate-and-recommend strings-enforcement bullet. The bullet's own contract was "Worker investigates + recommends; the recommendation lands as a hydrator proposal for human review before implementation."
**Approach:** Surveyed the four options listed (Detekt custom rule, pre-commit grep hook, pre-push CI check, Gradle plugin). Picked the Gradle `verifyStrings` task wired into `check` — same regex precision as the grep options but gates locally *and* in CI (no new tooling), and no `--no-verify` escape. Detekt rejected as overkill for one rule today (brings a plugin + ruleset jar + version drift); pre-commit hook rejected because workers can't use `--no-verify` so the hook never gates them; pre-push CI alone rejected because the local feedback gap forces fix-up pushes. Full writeup with table + rejected-alternatives + suggested item shape lives in `docs/agent/todo-proposals.md`. The `docs/todo.md` bullet is removed; a placeholder line tells future workers where the proposal went.
**Reviewer notes:** I didn't ship the actual Gradle task — that's the gated-on-triage half. The proposal is structured so the human can either accept it directly into `docs/todo.md` as a worker-ready follow-up, edit the regex shape, or pivot to Detekt with the rejected-alternatives reasoning visible. One thing the proposal flags but I'd repeat here: the regex approach has false-positive risk and the proposed escape hatch is `// strings-enforce: skip` per-line — that's a judgment call the reviewer may want to course-correct (a separate allowlist file is the alternative; both are fine).
**Deferred:** The actual `verifyStrings` task implementation (see proposal); baseline-allowlist file for current real violations like `PurchaseConfirmSheet.kt:189` and `AppGuardLayer.kt:237` (separate `[P2]` cleanup item once the task lands).

## feat(server): emit anon-orphan-count gauge via OTel metrics

**Problem:** `docs/todo.md` §C asked for a scheduled OTel gauge measuring how many orphan anonymous Supabase users have accumulated, so we can tell whether the [device-keyed backup sweep](docs/decisions.md) needs to come out of park. No metric existed; the server's OTel SDK was traces+logs only, no MeterProvider.
**Approach:** Wired a `SdkMeterProvider` into `buildOpenTelemetrySdk` (OTLP/HTTP exporter when `OTEL_EXPORTER_OTLP_ENDPOINT` is set, `LoggingMetricExporter` stdout fallback otherwise, 60s `PeriodicMetricReader`). Added a new `ServerMetrics` top-level object that mirrors `serverTracer`'s shape — no DI, registered from `installOpenTelemetry`. Sweep records the candidate count + the TTL it used on every run via `ServerMetrics.recordAnonOrphanCandidates(...)`; gauge callback skips emission while the sentinel -1 is set so a freshly booted server doesn't post a fake "0 orphans" before the first sweep.

**Key direction call:** I named the metric `cards.anon_orphans.over_ttl` and attribute the configured TTL (currently 30 days, env `ORPHAN_ANON_TTL_DAYS`), instead of the todo's literal `cards.anon_orphans.over_90d`. Rationale: the sweep already paginates the full anon-user pool with the TTL cutoff to find candidates for deletion; recording that exact list count is one Supabase listing per sweep. A literal 90d metric would require a second full paginated listing every sweep run (the Admin REST API doesn't expose count-only queries), doubling Supabase API cost for a metric that answers the same "are orphans piling up?" question. The TTL attribute keeps the metric meaningful if the env knob ever moves. The alternative (a second listing) is rejected as wasteful given V1 traffic; ship the TTL-attributed gauge now, add a separate 90d-fixed metric later if the operational story needs the dead-letter signal distinct from the sweep-incoming rate.

**Reviewer notes:** The `ServerMetrics` object pattern matches the existing `Tracing.kt` top-level shape (no DI), which I think is right at this scale — promote to DI when the metric set grows past a handful. Test layout: I merged the metrics assertions into `TelemetryTest` rather than standing up a parallel class, because `GlobalOpenTelemetry` is set-once per classloader and competing test classes on the global slot would race each other. The sweep-side test uses internal debug accessors (`ServerMetrics.debugLastAnonOrphanCount` / `…TtlDays`) rather than wiring a real meter — avoids the same global-slot conflict and keeps the sweep test scope on the sweep.

One thing worth a second look: the gauge is `ObservableLongGauge` with a callback that re-emits the cached value on every export interval. That means between daily sweep runs (24h) the line stays flat at the last-recorded value, which is the right behavior for "current orphan-pool size" semantics — but if the reviewer wants the metric to read as "events" instead (only emit when sweep runs), swap to synchronous `LongGauge.set` and the line will be sparse rather than continuous. Continuous is what I'd want for dashboarding; flagging in case the operational pattern disagrees.
**Deferred:** A literal `over_90d` dead-letter metric (rejected per Approach above; can land later if the TTL-based gauge ever shows a value that's hard to interpret). The `[P1]` "expand OTel span coverage on broadcast + per-recipient ws-send" item — context propagation through the publisher Flow chain is the meaty half; left untouched since this commit was already wide enough.

## feat(profile): catalog-axis tier badge on inventory rows

**Problem:** `docs/todo.md` §A asked for the catalog-axis tier badge to render on inventory rows now that every `EarnableCosmetics` entry carries a `CosmeticTier`. The state model didn't surface the tier and the row's `EarnedTag` was tier-agnostic.
**Approach:** Added `tierForProductId(productId)` to `EarnableCosmetics.kt` — a lazy productId → tier inverse of `cosmeticRewardFor(...)` so render paths don't pay a per-row enum scan. Threaded the tier through `OwnedItem` (new nullable field) and the `MyItemsState.ownedItems` computed property. `EarnedTag` now takes the tier and renders two variants: plain "Earned" for `EARN_ONLY` (current V1 behavior — every shipped entry) and a 🏆-prefixed "Earned" for `EARN_OR_BUY` (forward-compat for the heat-map / W/L-odds seeds called out in `docs/todo.md`). Added the EARN_OR_BUY preview alongside the existing earned preview so the trophy variant is visible in Studio's preview pane.

**Key direction call:** The trophy-emoji prefix as the `EARN_OR_BUY` distinguisher is a defensible visual choice but not the only one — alternatives include a separate badge color, an icon-only chip, or routing through a sibling primitive. I picked the emoji prefix because it (a) keeps the existing `EarnedTag` shape so the row rhythm doesn't shift between tiers, (b) matches the `PersonalCosmeticTag` "Only you" treatment idiomatically, and (c) needs zero new strings — the prefix is typographic, the label stays "Earned". Reviewer can swap to a richer treatment when EARN_OR_BUY seed entries actually land and the visual distinction becomes load-bearing rather than dormant.

**Reviewer notes:** V1 behavior is unchanged at every callsite because the V1 catalog is wholly `EARN_ONLY` — the EARN_OR_BUY branch only fires when a future seed entry tags an item that way. Tests pin the threading (`MyItemsViewModelTest.ownedItems_threadsCosmeticTier_fromEarnableMap`) and the lookup contract (`EarnableCosmeticsTest.tierForProductId_*`), but there's no preview / golden-style regression for the visual badge itself — the chip-rendering primitive item in the todo is asking for that pattern more broadly, so I held off on adding a one-off here. Reviewer please call if the EARN_OR_BUY visual needs a preview pin even pre-seed.
**Deferred:** Shop-side rendering (`features/shop/impl/.../ShopScreen.kt`'s `ChipOfferCard`). The `docs/todo.md` bullet has been rewritten to scope the remaining work to just the shop product card — the inventory half is fully shipped.

## refactor(shop): migrate ad-hoc cost pills to ChipCoinAmount

**Problem:** `docs/todo.md` §A "Consolidate chip / coin / stack rendering" called out 10+ feature files supposedly rendering their own chip styles. Audit (this cycle) found that the DS adoption is actually high — `ChipCoinAmount` is used in `OpponentsRow`, `PurchaseConfirmSheet`, `PlayerArea` already. The genuine ad-hoc renderings were two pills in `ShopScreen.kt`: `InsufficientChipsFooter` (line 752) and `ChipCostFooter` (line 814), both manually composing `Row { ChipCoin(16dp); Spacer(6dp); Text(formatChips(...)) }` instead of using the existing `ChipCoinAmount` primitive.
**Approach:** Migrated both callsites to `ChipCoinAmount(amount = cost, coinSize = 16.dp, typography = Body.B500, color = ..., formatter = ::formatChips)`. The outer `Row` shrinks to a `Box` because `ChipCoinAmount` is itself a `Row`. Visual output is byte-identical — same gold coin, same gap, same typography, same number formatting. No new primitive added; the existing one already covered the shape.

**Key direction call:** Rather than add a new `ChipBadge`-style "cost pill" primitive as the original todo wording implied, I treated this as a "DS adoption" pass — the existing `ChipCoinAmount` IS the consolidated shape and the work is migrating ad-hoc callsites onto it. The remaining direct `ChipCoin(...)` callsites in feature code are: (a) `WelcomeDialog`'s custom 0→balance tween (intentional, not a pill shape), (b) three Dialog explainers' title slots (`StackExplainer`, `PotExplainer`, `BetPillExplainer`), and (c) `OnboardingScreen`'s info card. The three dialog-title callsites repeat the same `ChipCoin + heading text` shape and could promote into a `ChipCoinDialogTitle` primitive — I rewrote the `docs/todo.md` item to scope to exactly that (P2, deferrable). Rejected promoting it in this commit because the right shape probably belongs as a `coinIcon` slot on the `Dialog` primitive itself (sibling to `dialogEmoji("🎉")` per AGENTS.md), not a feature-side helper that ships another bubble framing.

**Reviewer notes:** No tests added — these are pure render-equivalent migrations with no behavioral change. The existing `ChipCoinAmount` preview in `ChipCoin.kt` already covers the visual shape; the existing `ChipOfferCard` previews in `ShopScreen.kt` cover the surrounding-pill assembly. If the reviewer wants a regression pin for the specific cost-pill rendering they can ask.

The todo was overstating the gap a little — "10+ feature files render their own chip/coin styles" turned out to be mostly nervousness about the breadth, not actual ad-hoc usage. Most of the named files already use `ChipCoinAmount`. The rewritten todo entry reflects the real remaining surface (3 dialog-title callsites) rather than re-litigating the audit.
**Deferred:** `ChipCoinDialogTitle` primitive for the three dialog explainers (rewritten as a P2 in `docs/todo.md`); the wider DS-promotion sweep for non-chip patterns (kept as the existing P2 sweep item below it).

## feat(shop): "Also earnable" affordance on EARN_OR_BUY product cards

**Problem:** `docs/todo.md` §A "Catalog gating" remaining slice — after the inventory side landed the trophy-prefixed `EarnedTag` for `EARN_OR_BUY` rows earlier this cycle, shop product cards still rendered no signal that a tile sold for chips could *also* be reached through an achievement. Forward-compat scaffolding called out in the todo: read `tierForProductId(product.id)` in `ChipOfferCard()` and surface an "Also earnable" hint on `EARN_OR_BUY` tiles.
**Approach:** Threaded `tier: CosmeticTier?` into `ChipOfferCard` (set by the grid call to `tierForProductId(offer.id)`) and added a `🏆 Also earnable` hint line below the existing "Only you see this" hint when `tier == EARN_OR_BUY`. Uses the same `AppTheme.colors.accentEarned` color the inventory pin uses, and the trophy glyph prefix matches the inventory's typographic affordance — so a buyer seeing the shop tile reads "this is also reachable through an achievement" with the same visual idiom they'll later see on the earned instance in My Items. New `shop_also_earnable_hint` string added to `:libraries:resources`; preview `ChipOfferCardPreview_EarnOrBuyTier` renders the affordance on a synthesized `tier = EARN_OR_BUY` card so it's visible in Studio (V1 catalog has zero real `EARN_OR_BUY` entries today).

**Key direction call:** I threaded `tier` as a parameter to `ChipOfferCard` rather than calling `tierForProductId(offer.id)` inside the card. Two reasons: (a) it makes the preview shape testable without needing a real `EARN_OR_BUY` productId in the global earnable map (V1 has none), and (b) it keeps the dependency on the global map at the grid-row seam, so a future caller wanting to override the tier (a custom merchandising surface, a "what if" preview) has a clean handle. Rejected option: call `tierForProductId(offer.id)` directly inside `ChipOfferCard` — cleaner at first glance but locks the card to the global lookup with no override.
**Reviewer notes:** I render the affordance in every card state (Available / Insufficient / Locked / Owned) — there's an argument for hiding it on Owned (the user already has it, so "you could earn this" is now backward-looking advice). I left it on because: (a) V1 has zero `EARN_OR_BUY` entries so the Owned-state question is academic today, (b) hiding-on-Owned would mean a user who bought-then-earned would see the affordance disappear from their own copy in the shop, which feels inconsistent. Reviewer course-correct welcome if the product intent is "Owned hides the hint" — it's a one-line change.

No new unit test added: the `tierForProductId` lookup contract is already pinned by `EarnableCosmeticsTest.tierForProductId_*` (covers known + unknown + mirror-every-achievement); the visual coverage is the new `ChipOfferCardPreview_EarnOrBuyTier`. The classifier in `ShopState.classify(...)` doesn't compute the tier, so there's no VM-side seam that benefits from a separate threading test the way `MyItemsViewModel.ownedItems_threadsCosmeticTier_fromEarnableMap` did for the inventory side.
**Deferred:** Seed `EARN_OR_BUY` catalog entries (heat-map widget + W/L-odds display) — still gated on those features landing per the todo; this commit ships the rendering wiring so the seeds light up immediately when they arrive.

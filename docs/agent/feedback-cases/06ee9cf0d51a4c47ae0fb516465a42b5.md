# Feedback case 06ee9cf0d51a4c47ae0fb516465a42b5

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-2T (feedback twin); carrier crash https://elijah-dangerfield.sentry.io/issues/CARDS-2Q
- **Reported:** 2026-06-24T12:42:xx · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** todo: "[P0] MP client crash — Duplicate cards in hand on showdown"

## Bug description
> i was in the middle of a multiplayer game and the app crashed. That should NEVER happen. Worst case we should catch errors and show an unrecoverable error state. Using the Catching{} code helps. Maybe this was just thrown because its debug build tho so idk

## IDs
- user: ed8b752e-1077-4db7-8376-cdcc6eb51d82 (LuckyKing27)
- session: a8b2fcb3-c3b8-4c57-954e-59b71ace6dfe (feedback); crash event tagged session 6d07773d (shared device install 82b59ecd)
- install: 82b59ecd-b901-438b-a33b-0be45478f64d
- MP context:
  - room: S3XG9M
  - opponents (Loki room_code query): 8729ee18-6b11-4126-8589-81147011b867 (QuickQueen74, install 9d442b33, sessions fb519a07 / 214bc377)

## Reporter client log
Buffer not pulled for the twin; the carrier crash CARDS-2Q is the load-bearing artifact (see below).

## Client state at submit
No client-state.json on the feedback twin.

## Server activity
- Crash CARDS-2Q (event a37b5865141f4a6a9240c7f6c1d4cfa4) at 12:40:32, route PlayMultiplayerRoute, room S3XG9M, level=fatal, mechanism=nsexception, handled=no:
  `kotlin.IllegalArgumentException: Duplicate cards in hand: [8♠, 2♥, 8♣, Q♠, 8♠, A♣, 6♥]` — note the duplicate 8♠ in a 7-card (board+hole) set handed to the hand evaluator.
- Loki room S3XG9M: Hand 1 finished 12:40:27 (session=a4250067…); the crash fired ~5s later at showdown render. No server-side exception — the throw is client-side (Tempo/Loki clean for the session), so the duplicate-card set was assembled on-device, almost certainly a snapshot-merge / hole-card-vs-board overlap when applying a StateSnapshot.

## Working theory
The iOS client built a 7-card evaluation set containing a duplicate card (8♠×2) and the engine's `require(no duplicates)` threw uncaught on `PlayMultiplayerRoute`, killing the app at showdown. Root cause is client-side state assembly (board + hole merge or a snapshot applied on top of stale local cards), not the server — backend telemetry for the session is clean and the hand finished normally server-side. Two work items: (1) fix the duplicate-card assembly so the evaluator never sees a dup, and (2) per the reporter, wrap the MP render/apply path in `Catching {}` and surface an unrecoverable-error state instead of crashing. P0 — fatal crash mid-game on a core MP surface. Debug build, but an uncaught engine `require` would crash release too.

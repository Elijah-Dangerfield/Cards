# Feedback case d6022eaee9794557844307fedfdd03ca

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-8H (feedback twin: CARDS-8J)
- **Reported:** 2026-07-04T13:28:28Z · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** todo: "GAME-17 — Make an early bot fold legible: seat fold cue + last move on the player card"

## Bug description
> I'm playing against bots but it looks like one folded immediately? can't really tell I just see the two gray cards and when I click on the bot to see the player card it doesn't have a last move. Just kinda confusing as a user

No contact email.

## IDs
- user: e5b30ff3-132a-4f2b-9a7e-d721cadd1cd4 (BraveJack20)
- session: 7996d88a-9d37-4077-a787-b9380d54f369
- install: 48cdbcd7-b3a3-46a0-a7ff-1abf6fddc8c0 (known owner-tester install, iPhone18,3 simulator, iOS 26.1)
- No MP context — no room_code tag; game in question is solo `LocalBotsSession` (PlayBotsRoute). Earlier in the session the tester created/left MP rooms VMB4J8/X7BZR3/TY6YX3 without playing.

## Reporter client log
From the carrier's breadcrumb trail (ring buffer; timestamps are all stamped at submit time, so ordering only):

```
[DelegatingRouter] Enqueuing navigation: navigate to PlayBotsRoute
[LocalBotsSession] startNextHand: handNumber 0 -> 1
[GameEngine] startHand hand=1 button=0 sb=seat1/5 bb=seat2/10 stacks=[s0=1000, s1=995, s2=990, s3=1000]
[PlayPokerViewModel] hole-card projection hand=1 dealt=2 faceUp=2
[klog] Sending event PlaySound (x2)
[LocalBotsSession] runUntilHumansTurnOrComplete enter: hand=1 street=Preflop acting=3
[LocalBotsSession] applyIntentAndEmit: Fold(seatIndex=3, clientNonce=null) hand=1 street=Preflop actingBefore=3
[GameEngine] applyIntent hand=1 street=Preflop seq=9 seat=3 action=Fold stack=1000->1000 contributedHand=0->0 currentBet=10
[LocalBotsSession] applyIntentAndEmit: applied. street=Preflop actingAfter=0 events=1
[PlayPokerViewModel] VM received Submit Call(seatIndex=0, clientNonce=null)
... hand proceeds normally (calls/checks through Flop and Turn), then:
[DelegatingRouter] Enqueuing navigation: navigate to FeedbackRoute
```

The seat-3 bot (UTG, first to act) folded as the very first intent of hand 1, applied inside `runUntilHumansTurnOrComplete` immediately at hand start — while the client was still presenting the deal. No error, no crash.

## Client state at submit
Absent — non-MP report (`client-state.json` is MP-only).

## Server activity
- Tempo: one trace `9aa5218e3e9c08c9f6efe659d3266032` for the session — room-create/socket spans only, all sub-second. Gameplay is local; nothing gameplay-related server-side.
- Loki: 19 lines, all healthy 2xx (room create/leave for VMB4J8/X7BZR3/TY6YX3, wallet/inventory/equipment sync, achievement grants POT_500 + GOOD_FOLD_FIRST). One `401 Unauthorized DELETE /v1/rooms/VMB4J8/me` immediately retried to 204 — token-refresh race, unrelated. Backend not implicated.

## Working theory
The bot legitimately folded — seat 3 was UTG and `LocalBotsSession.runUntilHumansTurnOrComplete` applies the first bot intents synchronously at hand start, so the fold lands instantly (little/no think-delay pacing for actions that fire before the human's first turn, and possibly while the deal animation is still playing). The UI then shows the folded bot as just two gray cards with no fold announcement, and the tap-an-opponent player-card sheet has no "last move" populated for the fold, so the user can't reconstruct what happened. Two-part fix under one todo: (1) give the fold a legible seat-level cue (fold state/animation + pacing so the first bot action doesn't beat the deal), and (2) surface the seat's last action — including Fold — on the opponent player-card sheet. Relates to shipped GAME-9 (turn-ring stuck vs bots) but is a distinct legibility gap.

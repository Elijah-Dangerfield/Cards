# Feedback case 4587d9b2aab446d080ab9b7453aaab27

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-9N (twin CARDS-9P)
- **Reported:** 2026-07-11T20:28:52Z · FeedbackRoute · beta-ios-release · cards@1.0+740 (5ce8e0b3, main)
- **Disposition:** todo: "[P1] ROOM-16 Sole member of a matchmaking room isn't host — add-bots 403s, user reads it as offline"

## Bug description
> I couldn't add bits to my game. maybe the room is dead. I previously was searching for a room, but I canceled the search. I killed and reopened the app and it said that I was found in a room so I rejoined it. now my phone says off-line some features available not sure why because I am online

("bits" = bots — the trail is add-bots calls.) Contact: 2h7rmvs42g@privaterelay.appleid.com. Note: this is the **owner's device** (install cb87c0e4) under the brand-new Apple hide-my-email account created minutes earlier during the CARDS-9D stranded-identity mess.

## IDs
- user: 6f0a900c-b6b5-429c-a00b-c3ec6bc80f19 (Floppy-Jolt-5187)
- session: c105b164-9d2e-4820-a860-b0ec434ba368 (prior session: 7375aa1a, where matchmaking placed him)
- install: cb87c0e4-ca5b-4552-81f8-343b8312cd3f
- MP context:
  - room: Y4ZHD3 (prod; no room_code tag on the carrier — report filed from FeedbackRoute after leaving the lobby flow)
  - opponents: none — server says he was the only member ("Room Y4ZHD3 closed: last member 6f0a900c… left")

## Reporter client log
```
20:27:59 DelegatingRouter: navigate to LobbyRoute
20:28:00 room.joined
20:28:00 RoomSocket: Room socket connected (code=Y4ZHD3)
20:28:01 RoomSocket: recv snapshot members=1
20:28:03–08 NetworkCall ×7: POST /v1/rooms/Y4ZHD3/bots → 403 {"code":"not_host","message":"Only the host can add bots."}
20:28:11 DelegatingRouter: navigate to FeedbackRoute
```
No `isOffline` edge, no `net.backend_unreachable`, no socket drop anywhere in the buffer — the connection was healthy the entire time.

## Client state at submit
No client-state.json attachment (report filed from FeedbackRoute, outside the active MP play screen).

## Server activity
- Loki (prod): matchmaking placed him at 20:27:31 (socket connect in session 7375aa1a), app was killed (~16s socket), rejoin POST /join 200 at 20:27:59, socket reconnected 20:28:00, seven 403s on POST /bots 20:28:03–08, DELETE /me at 20:29:16 → "Room Y4ZHD3 closed: last member 6f0a900c-b6b5-429c-a00b-c3ec6bc80f19 left" (`InMemoryRoomService.leave`, line 336).
- Tempo: trace 7b9215621b857f4e1b9c9a57b3aff0a7 spans match the HTTP lines above; nothing slow or erroring beyond the 403s.

## Working theory
Matchmaking placed the user in room Y4ZHD3; he killed the app, relaunched, and rejoined via the active-rooms prompt. The rejoin snapshot shows him as the room's **only member**, yet the server still refuses `POST /bots` with `not_host` — host was never assigned to (or reassigned to) the sole remaining human. Either the matchmaking-created room's host is a departed/ghost user, or hostship was dropped on his disconnect and never restored on rejoin. Two client aggravators: the add-bots button gives no visible failure feedback (he tapped 7× in 5s), and the "offline — some features available" banner he saw has **no corroborating connectivity event** — most plausibly the client surfaces repeated request failures (or the stale room state) as offline, which is a misdiagnosis worth checking while fixing this. Fix: host follows the sole/first human member on (re)join in `InMemoryRoomService`, plus an explanatory error on add-bots failure.

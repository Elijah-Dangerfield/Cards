# Feedback case 5767dcdb543447aab90f419805d7a33b

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-7A
- **Reported:** 2026-07-01T14:08:41Z · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** fixed directly (owner asked to fix now, no todo) — refines the player-area half of case 758f5be6

## Bug description
> I just sent some feedback about the player card area clipping some of the information. It looks like the information that clip is the amount that the user bet. but that information should get rendered the same way that the opponent Does. Which is a badged type of thing. takes up less space. maybe we need to have that be a shared component

## IDs
- user: 9d4b0e89-0a9f-4f0e-bd7e-bd96707cfcc7 (SteadyQueen70)
- session: 4fdb0541-b407-4ef2-a49b-f06e8895e853
- install: 61e95306-2d91-4cad-b82b-a7f795d2d9eb
- iPhone14,2 · iOS 26.5 · solo (no room_code)

## Server activity
None expected — solo session, pure client UI.

## Working theory + fix
Confirms the clipped info is the human's bet amount and asks for it to render "the same way the opponent does … a badged type … shared component." The shared component already existed — `SeatActionChip` in `OpponentSeat.kt` — but the human tile wasn't using it; it stacked a `ChipPill`/`LastActionPill` in the column, which overflowed the locked row height. Fix: overlay `SeatActionChip(seat.lastAction)` on the human avatar's bottom-center (cut out of the tile surface), remove the stacked pill block, and bump the post-avatar spacer to D300 (8dp, matching opponents) so the straddling badge clears the name. `features/room/impl/.../ui/PlayerArea.kt`.

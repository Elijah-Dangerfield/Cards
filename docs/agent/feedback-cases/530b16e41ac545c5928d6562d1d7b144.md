# Feedback case 530b16e41ac545c5928d6562d1d7b144

- **Sentry issue:** carrier [CARDS-5H](https://elijah-dangerfield.sentry.io/issues/CARDS-5H) · feedback twin [CARDS-5J](https://elijah-dangerfield.sentry.io/issues/CARDS-5J)
- **Reported:** 2026-06-27T22:11:06Z · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** todo: "GAME-8 game-speed setting suppresses deal/reveal animations in real (bots-for-chips) MP games"

## Bug description
> there's no animations all the sudden even tho I'm playing a real game against Bots. the bot speed should only apply to practice I think. like the cards aren't flipping over and stuff

## IDs
- user: f3775c93-169b-4858-8e52-9ac167b31dbe (SteadyEight23)
- session: 607adee8-b418-4f17-b41f-6a799b53e266
- install: 61e95306-2d91-4cad-b82b-a7f795d2d9eb
- MP context:
  - room: BY6HUV (public match, 4 seats)
  - reporter seat: 0 · hand: 5
  - opponents (Sentry tag `opponent_user_ids`): 32f8f3f3-96a6-44e6-bcd4-9a30c51bf530
  - other room members seen in log: 39376022-bb85-420e-be88-e23e2687f4ca, c884cb7c-e49e-4a54-9c76-a9696f4658fe (bots / fillers)

## Reporter client log
Reporter is in a real-chip public room (BY6HUV) against bots. The hand log shows
each hand advancing through `recv game_state … StateSnapshot` transitions with no
`StreetAdvanced`-driven deal/reveal pacing the user could see — hands fly straight
to Complete (e.g. hand 4: River bet → `game_state hand=4 street=Complete pot=60`
→ hand 5 Preflop immediately). The user perceives "no animations / cards aren't
flipping." The same build added GAME-6's unified Game speed setting
(`GameSpeed.animationScale` + `botThinkScale`) — the reporter is hypothesising the
bot/animation speed scaling is bleeding into a non-practice game.

## Client state at submit
client-state.json present on the carrier event but the Sentry attachment API
returned a binary-decode error on download; salient state recovered from the log
instead (room BY6HUV, seat 0, hand 5 Preflop/Flop at submit time).

## Server activity
- Not separately queried — this is a client-side animation/timing complaint, not a
  server-state discrepancy. The socket frames in the log are well-formed and
  sequential (no gaps/dupes), so the missing animation is a client rendering/pacing
  issue, not dropped server events.

## Working theory
GAME-6 unified "Game speed" (Normal/Fast/Instant) scales both bot think time and
play-screen deal/reveal animations via `LocalTableTempo`. The setting is global, so
a player who set Fast/Instant for solo practice now gets the same suppressed
animations in a *real* bots-for-chips MP game — cards don't visibly flip, deals are
instant. The reporter's instinct (speed should be practice-only, or at least not
strip reveals in a real game) is the product call to make. Fix: either scope the
speed/animation scaling so real-chip games keep a minimum reveal animation, or make
the setting practice-only. Confidence medium — the exact tempo plumbing
(`TableTempo.kt`, `PlayPokerViewModel` tempo wiring) needs a worker to confirm
whether MP reads the same `LocalTableTempo` as solo.

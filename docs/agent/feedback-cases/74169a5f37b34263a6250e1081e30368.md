# Feedback case 74169a5f37b34263a6250e1081e30368

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-37 (carrier; no feedback-twin object — comment was empty)
- **Reported:** 2026-06-24T19:15:10Z · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** todo: "MP-8 — room socket reconnect storm after sole-human-left"

## Bug description
> (empty comment — user submitted feedback with no text, then bailed. Investigated from breadcrumbs + session-log.)

## IDs
- user: 8729ee18-6b11-4126-8589-81147011b867 (QuickQueen74)
- session: 864a61b7-c785-4fe3-991d-f854104d576f
- install: 9d442b33-a6f0-4af3-9742-33bef8a2ba21
- MP context:
  - room: NP2DDJ (private, 2-player)
  - opponent: ed8b752e-1077-4db7-8376-cdcc6eb51d82 (left mid-hand-2)

## Reporter client log
Joined NP2DDJ ~19:10:33, played hands 1–2. 19:10:53 `recv member_left user=ed8b752e` + `Sending event HostPromoted` + `Sending event OpponentsLeft` — opponent leaves, reporter promoted to host, now sole human. 19:12:16 `POST /v1/rooms/NP2DDJ/bots` → **409 room_not_joinable "You can only add bots while the room is in the lobby (status: Playing)"**. Then from 19:14:27, NSPOSIXError 57 "Socket is not connected" kicks off a **relentless reconnect storm**: hundreds of `Room socket connected (code=NP2DDJ)` immediately followed by `Room socket reconnecting (code=NP2DDJ, attempt=1, backoff=…)` — connect→drop→reconnect with `attempt=1` never advancing, for ~30s straight. User mashes "go back" (multiple `Enqueuing navigation: go back`), finally escapes to Home 19:14:58, opens Feedback 19:15:01, submits empty at 19:15:10.

## Client state at submit
No client-state.json. State from log: sole human in a Playing room after opponent left; socket wedged in a tight reconnect loop.

## Server activity
Not re-queried for NP2DDJ in this pass (room since closed). The 409 came from the bots endpoint guard; the room status never dropped back to Lobby after the opponent left mid-hand, which is the same residual captured in the existing backlog item "MP lobby shows $0 buy-in + 409 on POST /bots after sole-human-left rebound" (same room NP2DDJ, owner repro).

## Working theory
Two coupled bugs, both rooted in the sole-human-left rebound not resetting room status to Lobby: (1) the auto-bot-add fires against a still-`Playing` room → 409 (already in backlog); (2) **new** — the room socket enters an unbounded connect/reconnect loop (`attempt=1` never increments, no backoff ceiling, no give-up) once the peer is gone and the server-side socket is half-open (NSPOSIXError 57). The reconnect loop is the worse symptom: it pegs the client and the only escape is hammering Back. The reconnect backoff/attempt counter and a terminal "give up → show reconnect-failed UI" state are the fix. Distinct enough from the buy-in/409 backlog item to warrant its own todo.

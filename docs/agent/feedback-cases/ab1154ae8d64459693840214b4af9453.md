# Feedback case ab1154ae8d64459693840214b4af9453

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-AM (carrier) · https://elijah-dangerfield.sentry.io/issues/CARDS-AN (twin)
- **Reported:** 2026-07-18T19:02:31Z · FeedbackRoute · dev-android-release (emulator, **prod** backend per OkHttp hosts) · cards@0.1.0+1 @ a4abb1f379e7 (develop)
- **Disposition:** todo: MP-37

## Bug description
> I landed in a bad game state. The other 2 real players left and I reminded with just the bot and the bot seemingly bust but no hands turned over. I couldn't make a move and neither could the bot but the game wasn't reported as bots only. like it didn't transition into a practice game

(Owner dogfood. Missed by the 07-21 nightly — not in the ledger despite being 3 days old then.)

## IDs
- user: 3188c2ca-b35a-47c4-a390-ce9f0b0bec2c (Sentry user d0d7ce6d93b54667a0c963d1923f5225)
- session: 1ffeab70-b360-4e38-b4f5-a9e009a8a0e1
- install: 2144ea2a-fc8a-4547-8ca9-dff018e6bc62
- MP context (recovered from Loki; no room_code tag on the report — filed after leaving):
  - room: 9NTSXV (joined by code via `POST /v1/rooms/9NTSXV/join`, so a lobby game, not matchmade)
  - opponents: 2 humans + 1 bot per the report; opponent sessions not enumerated (room predates the report window; the wedge is fully server-side)

## Reporter client log
Attachment not pulled — server evidence is conclusive (below).

## Client state at submit
Not pulled (report filed from FeedbackRoute after leaving the table; no live MP session at submit).

## Server activity
- Loki (`room_code=9NTSXV`, trace 343d6a978a6f537d9a21c6edd3e6159b):
  - 18:58:54 reporter joins, socket connects.
  - 19:00:49 **WARN `Socket for room=9NTSXV died reading` — `java.lang.IllegalArgumentException: Need at least 2 active seats with chips to start a hand`** at `GameEngine.startHand` ← `GameSession.startHandLocked` (GameSession.kt:735) ← `dealFundedHand` (RoomSocketRoutes.kt:797).
  - 19:00:50 reporter's socket reconnects; 19:01:27 reporter leaves, `Room 9NTSXV closed: last member left`.
- Timeline matches the report exactly: humans left, bot busted → next-hand deal fired with only one funded seat → unhandled exception killed the socket coroutine → table wedged (no hand, no moves, no practice-mode transition).

## Working theory
`dealFundedHand` calls `GameSession.startHand` without guarding the "fewer than 2 funded active seats" precondition that `GameEngine.startHand` enforces, reachable when leavers + a busted bot drain the table between hands. The exception propagates out of the socket read loop (killing it) instead of resolving the table state, so the last human is stranded on a dead table with no acting seat. Fix needs both halves: catch/act on the <2-funded-seats case at the deal boundary (end the match with settlement, or offer rebuy/bot refill per the practice-mode rules), and decide the product behavior for "bot busted, only human remains" — the reporter expected a bots-only/practice transition. A regression test can reproduce with the existing server harness: 3 humans + 1 bot, two humans leave mid-session, bot busts, next hand boundary.

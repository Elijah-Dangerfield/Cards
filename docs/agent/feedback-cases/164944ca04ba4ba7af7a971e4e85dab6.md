# Feedback case 164944ca04ba4ba7af7a971e4e85dab6

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-2W
- **Reported:** 2026-06-24T~12:43 · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** no-action: friend graph is unbuilt and friends/social are descoped to V2 behind `social.enabled` (SOC-2); friend requests don't deliver yet by design

## Bug description
> another player sent me a friend request. I killed my app and cold started. I don't see friend requests anywhere.

## IDs
- user: 8729ee18-6b11-4126-8589-81147011b867 (QuickQueen74)
- session: 214bc377-8355-457c-90ff-ca2dbd318830
- install: 9d442b33-a6f0-4af3-9742-33bef8a2ba21

## Server activity
- No friend-request persistence/delivery exists server-side (the social graph is a no-op shell). Nothing to find in Tempo/Loki for a request that was never stored.

## Working theory
The friend feature is intentionally unbuilt for V1 — prior reports CARDS-1M/1N already resolved this same way ("friend requests inbox — friend graph is unbuilt/no-op, so requests don't deliver yet"). It is now formally descoped: SOC-2 gates every social surface behind `social.enabled` (default off), which will hide the friend-request affordances entirely so a user can't send a request that goes nowhere. Resolve as duplicate — no new todo; the fix is shipping SOC-2 (and the directive CARDS-2H reinforces it).

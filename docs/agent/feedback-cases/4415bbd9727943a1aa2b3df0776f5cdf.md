# Feedback case 4415bbd9727943a1aa2b3df0776f5cdf

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-AD (twin) · carrier CARDS-AC
- **Reported:** 2026-07-17T19:50:28Z · FeedbackRoute · beta-ios-release · cards@1.0+877 (35aa51a917ee)
- **Disposition:** no-action (known issue — owner call 2026-07-18: only 07-18 reports are live)

## Bug description
> create account failed :/   (reporter LuckyJack47, user 7de9b42a, no email)

## IDs
- user: 7de9b42a-a259-4f2e-ac6e-aa2f43cf522b (LuckyJack47) · session c53ff7f7-1a68-4e3c-befc-825ea175d7e5 · install c4a56e15

## Working theory / why no-action
Email signup hit two 30s timeouts on iOS: `POST /auth/v1/signup` and `GET /auth/v1/user`, both with `redirect_to=cards://login-callback` (tag AuthRepository, HttpRequestTimeoutException). Guest `signInAnonymously` succeeded later in the same session, so the Darwin TLS engine (ENG-30) is working — this is the email-confirmation signup path specifically hanging (likely Supabase waiting on the confirmation-email send). Build 877 already ships **ENG-30** (Darwin engine), **AUTH-20** (slow email signup now shows a *retryable* "took longer than expected, try again" error instead of a dead end), **AUTH-21**, and **AUTH-22**. The underlying latency persists but the UX is already softened, and per the owner's 2026-07-18 call only the 07-18 reports are in scope. Resolved no-action; the residual email-signup latency is captured here for reference if it recurs at volume (watch the "Onboarding & signup errors" table on the dc-funnel dashboard — HttpRequestTimeoutException / AuthRepository row).

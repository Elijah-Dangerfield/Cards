# Feedback case 98a6f5d5e21c42d88651290159c98696

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-B1 (twin) · carrier CARDS-AZ
- **Reported:** 2026-07-21T14:21:55Z · FeedbackRoute · local-ios-debug · cards@0.1.0+1 (develop c30ed5153ad4)
- **Disposition:** todo: "[P1] GAME-33 stale per-seat action label ('fold') persists into the next hand"

## Bug description
> "oooh the next hand started but my player still has the label of "fold" from my last action on the previous hand."

## IDs
- user: SlyTen69 — install dd4742d5-ae89-4366-b54f-360e326db10a
- session: 8155af48-d6e4-4104-8e0c-bac28c9c7219
- MP context:
  - room: 64MDUA
  - reporter seat: 0 · hand: 2
  - opponents (Sentry tag `opponent_user_ids`): 17b709b5-cb0c-4e60-8476-3c4deca27f50

## Reporter client log
Carrier `session-log.txt` not pulled: symptom is a self-contained client render bug (the server's game_state is authoritative and correct across the hand boundary), so the log adds nothing the report doesn't already pin.

## Client state at submit
`client-state.json` not pulled — not needed; the defect is that the client's per-seat action-label UI state isn't cleared on the new-hand snapshot, independent of the snapshot's contents.

## Server activity
No backend dig warranted: the server deals a fresh hand and streams a new `game_state`; the stale "fold" is a client-side presentation state that survives the hand reset. DEV server showed no warn/error in the window.

## Working theory
When a new hand's state snapshot arrives, the client resets pot/board/hole cards but does not clear the last per-seat action badge ("fold"/"check"/"call"/etc.), so the previous hand's action label lingers on the player's seat into the new hand — misleading every hand after the first. Fix: reset each seat's action-label UI to none when a new-hand `game_state` (new hand_number) is applied. Worker should reproduce test-first on the play-screen state reducer (per-seat lastAction should be null at the start of a new hand). Confidence high; pure client fix. P1 — visibly wrong every hand, though not data-affecting.

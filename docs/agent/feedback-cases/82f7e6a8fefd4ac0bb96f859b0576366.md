# Feedback case 82f7e6a8fefd4ac0bb96f859b0576366

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-B0 (twin) · carrier CARDS-AY
- **Reported:** 2026-07-21T14:20:23Z · FeedbackRoute · local-ios-debug · cards@0.1.0+1 (develop c30ed5153ad4)
- **Disposition:** todo: "[P1] MP-35 matchmaking pairs two humans but the game never starts"
- **Covers also:** CARDS-AW (twin) / CARDS-AT (carrier), event 27eeabe0 — same session, "one device stuck on searching while the other is in the waiting room"; and CARDS-AX (twin) / CARDS-AV (carrier), event 88ca7722 — "table found but the title still says finding you a table". All three are one root cause.

## Bug description
> (CARDS-B0) "ummmm both users searched for a table and found a table together and its just stuck at "dealing you in any moment now". Nothing is happening. Ideally the moment there's enough people to play the game begins. If someone else gets seated mid hand then they just spectate until the next hand and we tell them they will get in at the next hand. We need to revisit how our match making and server controlled games work."
> (CARDS-AW) "i made 2 emulators look for a game at the same time. the other device (not this one) immediately saw the room with this current device in it. That other device is in the waiting room. But this device is still on the searching screen."
> (CARDS-AX) "the table was found but the title at the top of this screen says "finding you a table" confusing"

## IDs
- user: QuickJack15 (B0/AW) — install 2318a130-af1d-4f5a-873a-aca44859e2f5 · SlyTen69 (AX) — install dd4742d5-ae89-4366-b54f-360e326db10a
- session: dce8ee48-ca73-41f4-8458-a3b132692448 (B0/AW) · 8155af48-d6e4-4104-8e0c-bac28c9c7219 (AX)
- MP context: no room_code — this is the pre-room matchmaking / Find-a-Table funnel. The two matchmaking parties are installs 2318a130 (session dce8ee48) and dd4742d5 (session 8155af48).

## Reporter client log
Carrier `session-log.txt` not pulled: this is a pre-room matchmaking report (no active GameState, so no client-state.json), and the DEV server stream was decisive on its own.

## Client state at submit
No `client-state.json` — reporter had no active MP GameState (still in the matchmaking/searching screen).

## Server activity
- Loki (`cards-server`, deployment_environment="dev", 14:11–14:22Z): both installs repeatedly hit `GET /v1/matchmaking/candidates` (200 OK, ~60–90ms) and session dce8ee48 fired one `POST /v1/matchmaking/find` (200 OK, 178ms). There is **no** room-created / seated / game-start / waiting-room log line for either session in the window, and **zero** warn/error server-side across all of DEV in 14:10–14:40Z.
- So the server accepted find + candidate polls but never transitioned the matched pair into a started game.

## Working theory
Matchmaking find-or-create + cross-tier rescue shipped (MP-34, 2026-07-18) and the server clearly answers `/find` and `/candidates`, but the matched pair never advances to a first hand: one client stays on the searching screen (AW), the other sits at "dealing you in any moment now" (B0), and the searching-screen title never leaves "finding you a table" (AX). The absence of any room-start / seat log for these sessions points at the waiting-room → auto-start transition (start-hand when the Nth human is seated) not firing for matchmaker-placed rooms, and/or the searcher not being routed into the created room. Needs a worker to trace the matchmaking join + `GameSession` auto-start path; confidence medium — the server logs prove "no start happened" but can't alone decide client-routing vs server-start as the trigger. P1: blocks the core Find-a-Table MP flow.

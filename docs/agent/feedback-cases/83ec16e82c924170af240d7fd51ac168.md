# Feedback case 83ec16e82c924170af240d7fd51ac168

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-4D
- **Reported:** 2026-06-26T21:00:28Z · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** no-action — fixed in PR #74 (MP-16); reporter was on a pre-#74 build. Re-test.

## Bug description
> i set the buy in to 5k but on the lobby its rendering as 0 wtf

(QuickJack56, no contact email)

## IDs
- user: 2db26568-b03b-4e39-b5b7-48dd56615db4 (QuickJack56)
- session: f2671a84-9483-414c-ae81-dfba6e9ce815
- install: 48cdbcd7-b3a3-46a0-a7ff-1abf6fddc8c0
- MP context (no room_code tag on the carrier; recovered from Loki):
  - room: AA7S5H (private, reporter was host/creator, seat 0)
  - opponent: 634704c2-1d9e-458d-b7a4-3904a295ad73 (SlyTen50, seat 1) — joined via PrivateJoinRoute

## Reporter client log
Create → lobby trail (session-log.txt):
```
navigate to PrivateCreateRoute
RoomSocket: Room socket connected (code=AA7S5H)
recv snapshot members=1
recv member_joined user=634704c2-…   (opponent joins)
navigate to FeedbackRoute            (filed the report here)
```
The buy-in value isn't echoed in the client create call, but the stake WAS applied: a few lines later the wallet drops 10000 → 5000 after the hand stakes resolve (`ChipsSync … balance now 5000`). So the 5k buy-in was real server-side; the "0" was a client-side lobby render of a placeholder/stale room snapshot.

## Client state at submit
Non-MP route at submit (FeedbackRoute); no client-state.json attachment.

## Server activity
- Loki (room AA7S5H): `POST /v1/rooms/AA7S5H/join 200`, both sockets `Socket connected`, `Hand 1 finished`, `Hand 2 finished (seat 634704c2 forfeited)`, `Room AA7S5H closed: last member 2db26568… left`. All 200/204/101 — no errors. Server never saw a $0 room; the buy-in was applied.
- Tempo: HTTP spans carry the session_id; no error spans.

## Working theory
This is exactly MP-16 — the lobby rendering a $0/placeholder `Room` snapshot instead of the real buy-in. The fix shipped in PR #74 (`feat: heads-up match-over flow + MP money hardening`, merged 2026-06-26 07:29, in develop): the `Room.preferRealOver(previous)` / `Room.isPlaceholder` (`buyIn <= 0`) invariant is now applied at both staging boundaries — `RoomRepositoryImpl.upsertActiveRoom` and `ReconnectingRoomSocket`'s `Snapshot` emission — so a placeholder snapshot can no longer regress a known-good room, and the create path seeds the real room first (decisions.md 2026-06-25, MP-16). The report build string `cardse@0.1.0+1` is unchanged from the pre-#74 playtest builds, and the report landed at 21:00, so the tester was almost certainly on a build cut before pulling #74. No new defect; ask QA to re-run the MP-16 lobby check (QA.md:397) on a current build. If a $0 buy-in still appears on a post-#74 build, reopen — it would mean the create-path seed (not just the socket rebound) is leaking a placeholder.

# Feedback case d30606fc0af84acf88b28e4b4ece3e4a

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-47
- **Reported:** 2026-06-25T14:54:16Z · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** todo: "ROOM-2 — in-room 'Leave room' button is unreliable; top back button works"

## Bug description
> on the lobby the "leave room" button works bad but the actual back button up top works well. I've filed other leave room bugs but maybe lets add that for context

## IDs
- user: 5eb3a5c8-71a7-441e-bdae-77969b62e753 (BoldJack37)
- session: 0066cd72-c78d-47ad-a9de-5e388f27b5be
- install: df7c4598-c9f9-44a2-bba5-919c5ce1053d
- not MP-tagged on this event (filed from the lobby)

## Reporter client log
Buffer covers the lobby surface around 14:54. The DELETE `/v1/rooms/{code}/me` server calls in this session all return 204, and the top back button is reported working — so the leave *request* path is fine; the in-lobby "Leave room" button specifically is the unreliable affordance.

## Server activity
- Loki: leave/DELETE calls in session 0066cd72 return 204; rooms close. No server fault — this is a client-side button wiring/affordance issue, not a backend leave failure.

## Working theory
The lobby's dedicated "Leave room" button does not reliably trigger the leave/navigation that the top back arrow does — same end action, two entry points, one flaky. Likely the button's onClick is gated (debounce, disabled-while-state, or wired to a different handler than the back affordance) or its hit area is off. Distinct from the prior leave-*reliability* (DELETE 409/404) work (MP-5, shipped) — the backend leave succeeds here; this is the in-lobby button affordance. Fix: point the "Leave room" button at the same leave action the back handler uses and verify it's enabled/tappable in all lobby states. Low-risk P2 UX nit; reporter explicitly notes it's a small add-on to the broader leave-room work.

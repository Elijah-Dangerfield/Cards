# Feedback case 4e58157fc813433a9b84edda1ff2ad5c

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-6Y
- **Reported:** 2026-07-01T01:19:28Z · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** todo: "GAME-11 In-app feedback dialog renders behind bottom sheets"

## Bug description
> The sun feedback dialogue should show up on top of everything including bottom sheets. I just realized it's hidden behind that. That could probably be a global setting I don't know

("sun feedback dialogue" = the shake-to / in-app feedback surface reachable from anywhere.)

## IDs
- user: 0269a06a-2b8f-41fc-aa68-6777198c9ddf (QuickQueen52)
- session: dbc9be6d-c09a-4e1f-b8d0-63d08c148875
- install: 61e95306-2d91-4cad-b82b-a7f795d2d9eb
- non-MP report (no room_code).

## Reporter client log
No `session-log.txt` attachment. Buffer absent.

## Client state at submit
Absent — non-MP report.

## Server activity
- None relevant — pure client compositing/z-order concern. Loki for the session shows only unrelated 200 OK sync/config traffic.

## Working theory
The global feedback surface is presented at a compositing layer below Compose `ModalBottomSheet`s (which draw in their own high window/overlay). So when the user opens feedback while a bottom sheet is up, the feedback surface is occluded. The reporter's own suggestion (a "global setting") is right: the feedback presenter should sit at the top-most app overlay so it covers bottom sheets and dialogs. Fix in the app-level presenter/host layering (see `libraries/ui/.../snackbar/SnackbarHost` and the app `Screen`/root overlay wiring, and the `FeedbackRoute` presentation). Low urgency (feedback still works if no sheet is open), P2.

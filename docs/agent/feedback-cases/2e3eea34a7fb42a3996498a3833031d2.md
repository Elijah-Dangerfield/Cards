# Feedback case 2e3eea34a7fb42a3996498a3833031d2

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-53
- **Reported:** 2026-06-27T15:58:03Z · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** todo: "GAME-7 solo hole cards don't render — player sees one face-down card"

## Bug description
> i'm playing against bots right now in a practice round, but my cards won't flip over. I only have one card and it's face down.
> (email: 2h7rmvs42g@privaterelay.appleid.com)

## IDs
- user: ddfd2b19-cb9a-4267-8302-ce50cb328337 (Watchful-Ox-8262 / 2h7rmvs42g@privaterelay.appleid.com)
- session: ac386144-f149-4957-875e-871a6e30fea0
- install: 61e95306-2d91-4cad-b82b-a7f795d2d9eb
- non-MP — solo PlayBots (LocalBotsSession), no room_code.

## Reporter client log
Brand-new user: signed in with Apple at 15:56:24, wallet just created (10000 chips). Navigated `PlayBotsRoute` at 15:57:40 — their *first* solo hand. Engine dealt and played hand 1 normally:
```
15:57:40 LocalBotsSession: startNextHand: handNumber 0 -> 1
15:57:40 GameEngine: startHand hand=1 button=0 sb=seat1/5 bb=seat2/10 stacks=[s0=1000,...]
15:57:43 PlayPokerViewModel: VM received Submit Call(seatIndex=0, clientNonce=null)   <- user DID act
15:57:43 GameEngine: applyIntent hand=1 street=Preflop seq=10 seat=0 action=Call(amount=10) ...
... bots fold, hand plays to Flop ...
15:57:53 DelegatingRouter: Enqueuing navigation: navigate to FeedbackRoute            <- user gives up, files report
```
The engine holds two hole cards for seat 0 (the user could Call), but the user reports seeing a single face-down card. No deal/flip render is logged (log is engine-level, not view-level).

## Client state at submit
Non-MP report — no client-state.json attached.

## Server activity
No backend telemetry expected: solo PlayBots runs entirely client-side in `LocalBotsSession` / `GameEngine`. Server traffic in the session is only auth/config/equipment sync, unrelated to gameplay. Nothing in Tempo/Loki bears on the render.

## Working theory
Pure client-side hole-card rendering bug on the solo play screen: the engine dealt seat 0 two hole cards (the user could act), but the table rendered a single face-down card instead of the two face-up cards. Likely a first-hand race in the hole-card reveal/flip — the `PlayingCard` / hole-card composable not receiving (or not flipping) the dealt cards on the very first hand of a fresh session. Not reproducible from telemetry alone; needs a device repro on a brand-new install's first PlayBots hand. Severe first-impression bug (new user can't see their hand) but not a deadlock — the user could still act, so it's a render defect, not a stuck state. Look at the local-bots hole-card deal → `TableUiState` projection → card flip animation for a first-hand/initial-emission gap.

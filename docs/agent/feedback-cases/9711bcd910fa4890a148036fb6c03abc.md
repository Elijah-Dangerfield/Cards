# Feedback case 9711bcd910fa4890a148036fb6c03abc

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-AS (twin) · carrier CARDS-AR
- **Reported:** 2026-07-21T14:16:54Z · FeedbackRoute · local-ios-debug · cards@0.1.0+1 (develop c30ed5153ad4)
- **Disposition:** todo: "[P2] AUTH-28 returning guest routed through new-user onboarding, then shown a stale level-up"

## Bug description
> "somehow i just continued as guest and then immediately saw the level 2 level up congrats screen. i think maybe I was already authed but somehow accidentally went to the landing page?"

## IDs
- user: QuickJack15 — install 2318a130-af1d-4f5a-873a-aca44859e2f5
- session: dce8ee48-ca73-41f4-8458-a3b132692448
- MP context: none (auth/onboarding, no room).

## Reporter client log
Client event trail (Loki `cards-client`, dev), session dce8ee48, 14:15:49–14:15:54Z:
- `onboarding.step_viewed` step=pick_identity (14:15:49)
- `onboarding.auth_selected` method=guest, **returning=false** (14:15:49)
- `onboarding.step_viewed` step=how_it_works (14:15:51)
- `onboarding.step_viewed` step=starter_grant (14:15:52)
- `onboarding.completed` **account_ready=true, duration_sec=336** (14:15:54)

Then the level-2 level-up congrats appeared, and the report was filed one minute later (14:16:54).

## Client state at submit
No client-state.json (non-MP report).

## Server activity
DEV server showed no warn/error for this session in the window; the anomaly is client-side routing/classification, not a backend failure.

## Working theory
The install already had a guest identity carrying **level-2** progression, yet onboarding classified this pass as a brand-new guest (`onboarding.auth_selected returning=false`) and ran the full pick-identity → starter-grant → completed flow — i.e. an existing/authed guest was dropped back to the landing/pick-identity screen and re-onboarded as new. Right after "continue as guest" re-entered the pre-existing account, a level-up celebration fired for the already-earned level 2 (a stale reveal). Root cause is the new-vs-returning classification on the guest path (the `returning` signal that feeds onboarding routing — ties to the AUTH-22 `AuthOutcomeClassifier` work): a returning guest should skip new-user onboarding and must not replay a level-up for progression it already had. Confidence medium — the misclassification is proven by telemetry; the exact trigger for the landing-page bounce (session drop vs. a routing edge on relaunch) needs a worker repro. P2: confusing, not blocking, and hard to hit outside repeated re-onboards on one device.

# Feedback case 7c4909e461ec425dac6fece2c9fbb2d4

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-4V
- **Reported:** 2026-06-26T22:06:09Z · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** todo: "PROG-3 level-up (and achievement) celebration doesn't show after a hand"

## Bug description
> i'm randomly not seeing the level up screens anymore not sure what's happening

## IDs
- user: 634704c2-1d9e-458d-b7a4-3904a295ad73 (SlyTen50)
- session: 06195cdc-b033-4aa5-84f6-d197d37e5634
- install: 1ea7630c-6183-4c90-93c1-039c529d9201
- non-MP — solo PlayBots (LocalBotsSession), no room_code.

## Reporter client log (around the level-up)
```
22:05:48 GameEngine: runShowdown hand=1 ... potAwarded ... winner=seat2 amount=10050
22:05:48 ProgressionRepository: Awarded 5 XP for hand 1 (BOTS, ... won=false)
22:05:48 AchievementRepository: Achievement earned: GOOD_FOLD_FIRST (Know when to fold 'em)
22:05:48 AchievementRepository: Achievement earned: POT_5000 (Whale pot)
22:05:48 LevelUpRewardGranter: Granted 1 reward(s) for level 3      <- leveled up to L3, reward granted
22:05:48 HomeViewModel: welcomeGates: ... profile=Authenticated(anon) chips=7150
22:05:53 LocalBotsSession: advanceToNextHand: signal sent (hand=1)
22:05:56 DelegatingRouter: Enqueuing navigation: go back            <- user leaves table to Home
22:05:56 HomeFeatureEntryPoint: Home route entered
... 22:06:09 user files the report ...
```
The whole log has **no** navigation to any level-up / celebration route or dialog (grep for LevelUpRoute / celebration / pendingHighlight returns nothing after the grant). The reward and the two achievements were granted silently; the user saw no celebration and filed the report 21s later.

## Client state at submit
Non-MP report — no client-state.json attached.

## Server activity
No backend telemetry expected: solo PlayBots + the progression/level-up celebration logic are all client-side (`ProgressionRepository`, `LevelUpRewardGranter`, the celebration screen trigger). Nothing in Tempo/Loki bears on the missing celebration.

## Working theory
Real client-side progression-UX bug: the level-3 reward (and the GOOD_FOLD_FIRST + POT_5000 achievements) were granted — data is correct, no loss — but the **level-up celebration screen never presented**. The reward fires from `LevelUpRewardGranter` at hand-end, but whatever drives the celebration's presentation (a queued event / pendingProfileHighlight consumed when returning to Home, or a play-screen celebration trigger) didn't fire, so the user dropped straight back to Home with no fanfare. "Randomly not seeing" suggests an intermittent trigger — possibly the celebration is gated on a state that the hand-end → `go back` to Home transition races past, or the highlight gets cleared before it's shown (cf. the "Cleared stale pendingProfileHighlight" path seen elsewhere). Needs a repro that levels up at hand-end and asserts the celebration route is enqueued. P1 — the reward moment is a core retention beat; the grant is fine but the celebration is silently dropped.

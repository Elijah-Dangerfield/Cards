# Feedback case 8a1a93d404a743e4b7029b2e4b010b0d

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-5Q
- **Reported:** 2026-06-28T12:58:16Z · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** todo: "MP-27 wallet balance stale after opponent-left kick to lobby (only corrects on foreground/background)"

## Bug description
> my opponent left, so I got kicked to lobby. I left the lobby but my chip balance still reflected the buy in being gone. I had to foreground and background again to get it to correct. I fixed most of this issue. Like for the opponent that issue didn't happen. So we might have a BIT more work to do to make sure leaving the game updates the wallet.

(SlyNine76; no email)

## IDs
- user: d3b01b5d-133d-48a4-9d6c-025783f4f3db (SlyNine76)
- session: 92294a68-5d5d-467d-bc9e-40fa0b13c92d
- install: dd4742d5-ae89-4366-b54f-360e326db10a (iPhone17,5 simulator)
- MP context: room 66WUWR (the opponent-left room; not tagged on this non-MP-route carrier — recovered from Loki)

## Reporter client log
Not separately pulled; behavior fully explained by the server timeline below + the reporter's own description.

## Server activity (timeline, Loki session 92294a68)
- 12:56:33 `POST /v1/me/wallet/sync 200` and `POST /v1/me/grants/achievement/FIRST_WIN_BY_FOLD 204` — a hand resolved in room 66WUWR.
- 12:56:43 `Room 66WUWR closed: last member d3b01b5d left` — opponent had already left; the reporter's leave collapses the room (→ kicked to lobby).
- 12:56:55 `POST /v1/me/wallet/sync 200` — a later wallet sync succeeds (consistent with the foreground/background cycle the user did to force the balance to refresh).
- No errors. The server settled and the wallet endpoint returns 200 each time — the authoritative balance is correct server-side.

## Working theory
This is the residual of the #83 "multiplayer money hardening" / MP-23 fix (CARDS-5B, "wallet doesn't reconcile on host back-button leave; only on next foreground"). The reporter explicitly notes "I fixed most of this issue" — the host/normal leave now reconciles, but the **opponent-left-kick-to-lobby** path still leaves the client's displayed balance stale (showing the buy-in as still escrowed) until an app foreground/background forces a `/wallet/sync`. Server balance is correct; the client just doesn't re-pull the wallet when the room collapses out from under it due to the *other* player leaving. Fix: trigger the same wallet reconcile/refresh on the opponent-left / room-closed-under-us path that the explicit-leave path already does, so the lobby reflects the settled balance immediately. Confidence high; matches reporter description + server timeline. Distinct enough from MP-23 (already shipped in #83) to warrant its own todo rather than a duplicate-resolve.

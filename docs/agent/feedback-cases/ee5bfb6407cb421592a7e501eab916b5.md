# Feedback case ee5bfb6407cb421592a7e501eab916b5

- **Sentry issue:** https://elijah-dangerfield.sentry.io/issues/CARDS-55
- **Reported:** 2026-06-27T21:18:12Z · FeedbackRoute · dev-ios-debug · cardse@0.1.0+1
- **Disposition:** todo — "MP-24 reopen: lobby buy-in still renders 0 for a joiner on a post-#74 build"

## Bug description
> the buy in in the lobby is still rendering as 0

(SharpJoker31, no contact email)

## IDs
- user: 79997747-1fe3-41b0-92de-969a3b3feb6e (SharpJoker31)
- session: 016a8a61-83d1-40f0-8214-ee208f829425 (install shared with SlyNine76 / CARDS-5B — same simulator device)
- install: dd4742d5-ae89-4366-b54f-360e326db10a
- MP context (recovered from session-log): room AS4UPA — reporter is the **joiner** (via PrivateJoinRoute), not the host. Buy-in was real (5000): blinds 25/50, both stacks 10000→5000.

## Reporter client log (session-log.txt — full buffer, 49 lines)
```
21:17:37 DelegatingRouter: Enqueuing navigation: navigate to PrivateJoinRoute
21:17:52 RoomSocket: Room socket connected (code=AS4UPA)
21:17:52 recv snapshot members=2
21:18:01 DelegatingRouter: Enqueuing navigation: navigate to FeedbackRoute  (files report ~9s after the snapshot)
```
The buffer is short because the user filed almost immediately after the snapshot rendered the lobby with a $0 buy-in.

## Client state at submit
No client-state.json (FeedbackRoute).

## Server activity
- Loki (session 016a8a61 / room AS4UPA): `POST /v1/rooms/AS4UPA/join 200`, `Socket connected … user=79997747`, both sockets present, `Hand 1 finished (seat 79997747 forfeited)`. Server never saw a $0 room — the buy-in (5000) was applied; stacks dropped 10000→5000. So this is a client lobby-render bug, not a server value.

## Working theory
**MP-16 reopen.** MP-16 (the $0 lobby buy-in) was fixed in PR #74 (merged 2026-06-26 07:29) via `Room.preferRealOver(previous)` / `Room.isPlaceholder (buyIn <= 0)` applied at the create-path seed and the socket-rebound staging boundaries. The MP-16 case file (83ec16e8…) explicitly said: "If a $0 buy-in still appears on a **post-#74** build, reopen — it would mean the create-path seed (not just the socket rebound) is leaking a placeholder." This report is 2026-06-27 ~21:18, a day+ after #74 landed — a post-#74 build — and the $0 still renders, this time for the **joiner's** lobby view (PrivateJoinRoute), not the host's create view. The #74 fix may guard the host's create/rebound staging but not the joiner's lobby snapshot read, where a placeholder/early snapshot (before the real room snapshot resolves) still shows buyIn=0. Fix: apply the same `preferRealOver` / `isPlaceholder` invariant on the *joiner's* lobby room state (the PrivateJoin → lobby snapshot path), with a failing test that joins a real-buy-in room and asserts the lobby never shows 0. Confidence high that it's the same class of bug; exact leak point is the joiner snapshot path rather than the create seed.

<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base app theme with SplashScreen API support -->
    <style name="Theme.Cards" parent="android:Theme.Material.NoActionBar">
        <!-- Splash screen background -->
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <!-- Status bar and nav bar colors -->
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <!-- Window background for after splash -->
        <item name="android:windowBackground">@color/splash_background</item>
        <!-- Light icons on dark background -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash background matches the app's dark theme -->
    <color name="splash_background">#000000</color>
</resources>
